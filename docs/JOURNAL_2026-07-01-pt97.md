# JOURNAL 2026-07-01 (Web pt97) — crachá HERO, guardião, saúde GG, tags canónicas

Snapshot **datado e imutável** da sessão. O estado ACTUAL vive nos docs vivos
(`TECH_DEBTS`, `PENDENTES`, `REGISTO_CONCEITO`, `TAGS_CANONICO`). Sessão longa,
tudo LIVE em produção (Railway backend + frontend verdes).

## O que entrou em PRODUÇÃO hoje (hashes confirmados no git log)

| Commit | O quê |
|---|---|
| `7b55ab3` | **Crachá HERO só no Hero real** — amigos (Karluz/flightrisk) deixam de levar "HERO". A causa: `isHero = is_hero \|\| HERO_NAMES_ALL.has(nome)` e `HERO_NAMES_ALL` inclui os amigos. Fix (Opção B): fallback passa a `nome==='hero'` (só o marcador anónimo), sem lista de nomes. Backend confirmado limpo: `is_hero` em **exactamente 1** lugar/mão (0 com 2). **Fix latente incluído:** a linha MESA usava `p.is_hero` mas o `parseHH` devolve `p.isHero` → dependia 100% da lista; corrigido para `p.isHero`. 6 sítios (handParser 117/145, HandHistoryViewer 169/285, HandDetailPage 231). |
| `a38466a` | **Secção "Mãos suspeitas" (guardião de validação v1)** — fila viva read-only (só GG 2026) de 2 venenos PUROS: **bounty abaixo de metade** (coroa < base÷2; reusa `detect_bounty_below_half` extraída de `queue_export`) e **nome do Hero num vilão** (par no `anon_map` com chave≠'Hero' e valor∈HERO_NAMES; a chave 'Hero' é SEMPRE excluída — senão 708 falsos positivos). `GET /api/suspicious-hands`. Contagens à data: bounty<½ ≈91 · nome-do-Hero =6. |
| `00b6b96` | **Captura table-SS inline no `HandDetailPage`** (só GG) — painel "📷 Captura da mesa (table-SS)" por baixo da MESA/ações, imagem via `context_table_ss_id` (clica p/ ampliar); aviso "Sem captura" quando não há. Winamax/PS/WPN não mostra. Frontend-only (`GET /hands/{id}` já traz `context_table_ss_id`). |
| `4884f82` + `617d33c` | **Lista canónica de tags — fonte única `services/tags_canonical.py`** (une `normalize_tag_key` + `IT_FOLDER_TAGS` + `hm3_tag_aliases` + `tag_family_key`). Pastas novas reconhecidas (NKO Pos→pos-nko, ICM FT→icm-ft, Speed Racer FT→speed-racer-ft, NKO Pos FT→pos-nko-ft; NPKO Pos antigo mantido). **Normalização na ESCRITA** nos pontos de entrada (import HM3, 1-clique triagem, PATCH/TagEditor, folder_tag do IT). Só ACRESCENTA reconhecimento — passthrough do que não reconhece; `nota ex` NÃO funde. Endpoint `POST /api/hands/admin/canonicalize-tags` (`617d33c` = aceita Bearer). **Backfill de 19 grafias tortas HM3 aplicado em prod** (13× `icm pko ft`→`icm-pko-ft`, 6× `pos pko FT`→`pos-pko-ft`; `nota` preservada; 0 `nota ex`; idempotente). Ver `docs/TAGS_CANONICO.md`. |
| `a3deb74` | **Secção "Saúde das mãos GG" — Fase 1 (só mostrar)** — `routers/gg_health.py`: `GET /gg-health/summary` + `/list` (por imagem, paginada). UNION Gold (`entries.img_b64`) + IT (`table_ss_processing_log.img_b64`), só GG. **928 imagens / 714 mãos.** Grupos: saudável = Gold-casou 332, IT-desanon 514; precisa-de-ti = Gold-sem-tag 202, órfãs 82, suspeitas-de-troca 209 (sinal bruto nº≠mão, **não veredicto**), conflito-de-tags 2 (LIVE pela canónica + 2 regras). Página `/gg-health`, item "Saúde GG" na sidebar; a "Marcadas/captura" **fica** (Fase 2 absorve). |

## Sem commit (foi endpoint em produção — registo como facto)

- **4 mãos "nome do Hero num vilão" corrigidas via `POST /api/table-ss/set-anon-map`:**
  `GG-6080253564`, `GG-6104058222`, `GG-6105040361`, `GG-6105042698`. O mapa
  final veio da simulação da âncora Hero+botão (Hero=Lauro Dermio, amigo
  recolocado). Verificado read-only: Hero=Lauro, 0 vilão-Rui, grupo "nome do
  Hero num vilão" **6→2**. **Não há commit** — foi escrita via endpoint (regra:
  writes de prod via endpoint). As 2 restantes (`GG-6104057685`, `GG-6105043278`)
  são **captura TROCADA**, não erro de nomes → ver `PENDENTES`.

## Aprendizados integrados nos docs canónicos (esta sessão)

Ver, no sítio onde um operador os procura: `DESANON_ANATOMIA §2/§3.3` (hash
segue o jogador entre mesas; Gold premium vs IT frágil; alcance da propagação e
da Gold; acoplamento tag↔captura), `docs/TAGS_CANONICO.md` (novo — vocabulário +
2 regras + significados), `REGRAS_NEGOCIO` (fluxo de tagging do Rui),
`REGISTO_CONCEITO` (2026-07-01), e as correcções no `CLAUDE.md` (nome do
ficheiro Gold vs IT).

## Docs desta sessão
`JOURNAL_2026-07-01-pt97.md` (este) · `TAGS_CANONICO.md` (novo) · `PENDENTES`
(secção pt97) · `DESANON_ANATOMIA` (§2/§3.3) · `CLAUDE.md` (nome do ficheiro +
sumário) · `REGRAS_NEGOCIO` (fluxo de tagging) · `REGISTO_CONCEITO` (2026-07-01) ·
`POR_ONDE_COMECAR` (ponteiro TAGS_CANONICO).
