# JOURNAL — 18 Julho 2026

Dia de **validação e cura do detetor de bounties recuperáveis** + **forense da 6570**.
Segue o fecho de 17 Jul (coroas + detetor Etapa 1). Tudo LIVE em `main`.

> Nota de relógio: o sistema reportou "14 Jul", mas `2026-07-14…07-17` já são
> journals existentes. Este trabalho **segue** o fecho de 17 Jul → datado **18 Jul**.

## ★ 1. Régua do resto-em-BB — PROVADA (3/3 nas imagens do Rui)

O detetor (Etapa 1) marcava bust por `all-in ∩ "and lost"` no SUMMARY. O Rui validou
5/6 casos e chumbou o **#3 (GG-6137938668)**: não havia eliminação. Diagnóstico da HH:
`dca5d5eb` (PUGLIFE, BTN) foi all-in a **cobrir** o Hero, **25.500 devolvidos** → ficou
com 25,5 BB → **vivo**. A coroa NULL era **leitura falhada de um jogador VIVO**, não verde-KO.

A 1ª assinatura proposta ("houve `Uncalled bet returned`", binária) estava **errada** —
removia também o #1 (2491, confirmado). A assinatura **certa** é **quanto sobra ao all-in
perdedor vs a big blind**: resto ≈ 0 (coberto) = bust real; resto ≥ 1 BB = sobreviveu. O Rui
verificou **3/3 sobreviventes na imagem** (2491 M Dziubdzie.. · 6139654663 Bons Habitos ·
6090110628 rickzera) — **todos com placa própria** → a régua confirma-se, e **corrige a
confirmação anterior do #1** (só a imagem arbitra, LICAO 14 Jul).

## ★ 2. Detetor re-medido + fix LIVE (`161c7a3`)

`classify_hand` (puro) passa a separar por resto-em-BB; o router acrescenta a **contraprova
da mão-seguinte** (o eliminado não se senta na mão a seguir da mesma mesa/torneio; defensiva —
sem mão-seguinte, fica pela régua). Novo balde `misread` ("re-ler placa"). Split **por-jogador**
(mãos multi-bust caem nos dois baldes). Painel com o balde novo + contador; cards → `/hand/<id>`.

**Sweep prod (488 KO/PKO com Gold):**

| Balde | N | Destino |
|---|---|---|
| BUST REAL (resto <1 BB + contraprova) | **33** | verde × 2 (31 confirmadas pela mão-seguinte, 2 sem-mão-seguinte) |
| RE-LER PLACA (vivo) | **10** | re-ler placa própria (inclui **1042** despromovido — 0,69 BB, sentou-se na mão seguinte) |
| falha real (group2) | 18 | balde das coroas |
| over-read | 11 | à parte |

A contraprova provou o valor: apanhou o 1042 que a régua sozinha deixava passar. Testes **9/9**
crown; suite completa **1474 passed** (5 falhas = `psycopg2` sem Postgres local, 0 AssertionError).

## ★ 3. Fix dos cards do painel (`ba74e25`)

Os cards linkavam para `/hrc-results/hand/<id>` (página **Wizard**, que exige `result_zip`);
a maioria das mãos do worklist tem Gold mas nunca foi resolvida pelo HRC → rebentava "sem
result_zip". A validação é sobre a **IMAGEM** → cards passam a `/hand/<id>` (página normal,
`HandDetailPage`). Regra reafirmada: mãos ao Rui **por link + nº GG**, nunca id interno.

## ★ 4. Forense da 6570 (GG-6104058222) — FECHADA

- **Culpado:** o re-apply do table-SS — `reconcile_table_ss()` no **modo default** (`success`
  incluído) → `_deanon_after_match` re-preenche coroas da **leitura table-SS guardada**
  (`table_ss_processing_log.vision_json`). Mais o relink de órfãs e os escritores manuais.
- **Quando:** a cada import (fire-and-forget), no reimport (`#HRC-REIMPORT-REDEANON-CASADAS`
  re-desanoniza TODAS as `success`), e em relink. **Com que leitura:** a table-SS guardada —
  se pré-cura (a chama) ou stale, é o que "volta".
- **Raiz:** **zero flag de proveniência por-coroa.** Existe `verified_by_user` (mão, só nomes) e
  `bounty_source='green_ko'` (por-seat, só cura-verde), mas **as correções manuais de coroa não
  levam marca** → nenhum re-apply as *pode* poupar. O fill tem guard `>0 continue` (não pisa
  coroa positiva viva), mas um re-deanon que reconstrói o apa do raw perde a correção e re-enche
  da leitura guardada.
- **Cura proposta (princípio do Rui: carimbo = intocável):** reusar `bounty_source`.
  **A (Web recomenda):** `bounty_source='manual'` por-seat; todo o re-apply salta seats com
  `bounty_source ∈ {manual, green_ko}`. **B:** `crowns_locked` à mão inteira (mais grosso).
  **Tem de aterrar ANTES de qualquer escrita nova** → a Etapa 2 nasce protegida.

## ★ 5. ADENDA da chama fóssil — caminho vivo LIMPO
Verificado: prompt do replayer curado (`screenshot.py:256-300`, `vpip_pct` e `bounty_value_usd`
separados), parse separa, **zero** cópias chama→coroa, e o wipe de 8 Jul saldou as leituras
pré-cura. A única porta é o re-apply (§4) atropelar uma correção manual — daí a cura do carimbo.

## Estado no fecho
- Etapa 2 **BLOQUEADA** — portões: (a) cura do carimbo A/B (decisão do Rui) + (b) olho do Rui
  nos 3 do balde 1. Estreia da escrita: 2480 (AVRELIY $265.62 · Phil $375).
- Nenhuma escrita de bounty nesta sessão. Verificações contra prod = **read-only** (`~/.pokerapp_db_ro.env`).

## Commits do dia (main)
`ba74e25` cards → página normal · `161c7a3` detetor: split resto-em-BB + contraprova + balde re-ler placa.
