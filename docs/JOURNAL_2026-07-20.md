# JOURNAL — 20 Julho 2026

> Nota de relógio: o sistema reportou "15 Jul"; segue o fecho de 19 Jul → datado **20 Jul**.

Dia à volta de duas frentes seladas: a **física do verde-KO** (decisão do Rui, arbitrada
pela imagem) e a **prova do selo de nomes** (a reincidência da GG-6177132682, dada por morta).
Tudo LIVE em `main` (`14cd808`…`caf2349`). Escritas de dados em prod por endpoint (Bearer) —
sem commit.

## ★ 1. FÍSICA DO VERDE — a coroa da vítima = VERDE × 2 (`58ec6d4`)
Decisão do Rui pela **imagem** (GG-6132507189, Ward E): o verde na coroa de quem elimina =
**METADE** da coroa da vítima → a coroa da casa (o instantâneo, unidade da BD) do eliminado =
**verde × 2**. O "tal-e-qual sem ×2" (17-18 Jul) foi **erro de tradução** da mecânica e morreu.

Prova dura que fechou o caso: Ward E carregou coroa **$125 em ~10 leituras** do torneio inteiro
(BH HR $525, base $250, base÷2=$125) e nunca outro valor; a leitura tal-e-qual gravara $62.5
(=verde, metade).

- **Fix no chokepoint único** `eliminated_bounty.resolve_seat_bounty` (`GREEN_TO_CROWN_FACTOR=2.0`,
  `round(verde*2,2)`) → cobre a Etapa 2 `/suggest` **E** o scrub `green_ko` de ingest.
- **Conceito "migração de unidade" extinto** (os valores altos antigos eram COROAS CERTAS; a
  "camada de unidade antiga" nunca existiu) — removido do detetor (`crown_recovery`) e do frontend;
  endpoint novo `POST /drops/undismiss` reabre as dispensas feitas sob a premissa falsa.
- **Downstream inalterado:** `bounty_value_usd` continua o INSTANTÂNEO; o HRC/queue_export faz o
  seu instantâneo→total ×2 sobre o gravado (os dois ×2 são independentes, não acumulam).
- **Dados (endpoint):** os **9 `derived_green_ko` re-carimbados a dobrar** (Ward E $125 · Curtis
  $185.5 · Phil $375 · shadow97 $5 · Totokoning $35 · qazwsxedc $125 · JohnnyStar8 $78.75 ·
  dinofarm69 $131.25 · A Ovechkin $179.38) **+ os 2 `green_ko` de ingest** (Cornel Zahiu $40 ·
  Lauro Dermio $204.54), todos selados. Os 6 cards de queda dispensados (premissa falsa) reabertos.
  Read-back: os 11 coerentes nos 2 stores, todos ≥ base÷2. Censo: 33 `manual` + 11 `derived_green_ko`,
  0 `green_ko` residual.
- Suite **1482 passed** (5 falhas = `connection refused` sem Postgres local, 0 AssertionError).

## ★ 2. EDITOR DE NOME POR-LUGAR — o que faltava (`08d753e` → `702d96a`)
O lápis da página só editava a coroa; não havia onde corrigir o NOME de um seat. Exposto:
- **backend** `POST /api/table-ss/set-seat-name {hand_id, position, real_name}` — acha o seat pela
  POSIÇÃO, faz merge por cima do anon_map atual (preserva os outros) e reusa `/set-anon-map` (mesmo
  enrich + guardas + villain_rules) → carimba `verified_by_user`.
- **frontend** `NameEditor` (✎) ao lado do nick de cada vilão. **Armadilha apanhada em produção:** a
  página da mão renderiza a MESA via **`HandTimeline`** (não `HandHistoryViewer`), que só importava o
  `CrownCell` → o ✎ só apareceu depois de exportar o `NameEditor` e ligá-lo TAMBÉM ao `HandTimeline`.
  Diagnóstico pelo **bundle SERVIDO** (grep do JS que o browser recebe), não pelo repo.

## ★ 3. PROVA DO SELO DE NOMES — reincidência GG-6177132682 MORTA (ao lado da 6570)
O Rui corrigiu o HJ pelo editor novo: **`16170930` (HJ) → `fungus_among_us`**, `verified_by_user=True`,
`match_method=table_ss`. Forçados os **3 processos automáticos** que historicamente pisavam a correção
(os 3 ghosts do `8cb9fe4`) — **o nome sobreviveu aos três**:

| Processo | Endpoint | Resultado | Guarda |
|---|---|---|---|
| **reconcile** (votação do torneio) | `POST /table-ss/reconcile` | HJ = `fungus_among_us` (intacto) | `table_ss_deanon.py:818` `if pn.verified_by_user: continue` (skip silencioso) |
| **re-link Gold** (ghost c) | `POST /screenshots/orphans/2312/relink` | `already_enriched` → HJ intacto | `screenshot.py:1701` `_is_verified` → preserva nomes |
| **re-deanon** | `POST /table-ss/redeanon` | `skip_verified_by_user` (explícito) | `table_ss_deanon.py:498` |

Endpoint novo `POST /api/screenshots/orphans/{entry_id}/relink` (`caf2349`, Bearer) para forçar o
re-link Gold de uma mão já ligada (o `/rematch` só apanha órfãs `status='new'`, e é cookie-only) —
maintenance + prova do ghost (c).

**A reincidência da 6570-com-nomes está fechada.** Espelha a prova do **selo das coroas** (6570 /
GG-6104058222, `JOURNAL_2026-07-19` §1): o que o Rui valida fica selado; nenhum automático o pisa.

## 4. Auditoria de tags — o buraco só-folder-tag (read-only)
GG 2026: 26 342 mãos · 434 com tag · 25 908 sem. Das sem-tag: **673 = BURACO reimport-flat**
(table-SS que perdeu a folder-tag no achatamento do `done` de 10-11 Jul; 1140 capturas sem ft nessas
datas) · **25 235 = Gold-lane** (a Gold nunca taga, esperado) · **0 anomalias** (nenhuma table-SS COM
tag por aplicar) · 0 sem-captura. O fix `done`-preserva-subpasta (9 Jul) trava o achatamento futuro;
as 673 já perderam o sinal no disco (recuperação = re-tag manual ou re-import com a pasta).

## Commits (main)
`14cd808` selo-em-detetores (fecho 19 Jul) · `58ec6d4` física do verde ×2 + extinção migração-unidade ·
`08d753e` editor de nome por-lugar · `702d96a` NameEditor no HandTimeline · `caf2349` endpoint relink
(prova do selo). Escritas de coroa (11 seats) e a prova dos 3 processos por endpoint (Bearer), sem commit.

## Dívidas / próximo
- Rotação dos segredos (lado do Rui): `~/.pokerapp_db_ro.env` (re-exposto num erro de shell hoje) +
  a chave HRC `~/.pokerapp_watcher.env`.
- Os 2 `green_ko` ficaram `derived_green_ko` (única fonte selada escrível por endpoint) — no reimporte
  regeneram-se como `green_ko` ×2 pela fórmula nova; sem conflito.
