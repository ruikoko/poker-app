# JOURNAL — 2026-07-16 (sessão 16.2)

> Sessão longa de CURA DAS COROAS GG: CANON dos degraus, porteiro à entrada, lei do
> cruzamento (conflitos) endurecida à raiz, e os 4 baldes do Vivo-$0. Tudo LIVE em `main`.

## ⏭️ PENDENTES DE AMANHÃ (topo — 1ª coisa)
1. **Os 24 do OLHO** (conflitos): cada card tem proveniência + campo livre + "Mantém $X"; falta o Rui decidir.
2. **Releitura das 14 mesas-toda-$0** — botão pronto (Vision, custo à vista, Cancelar); correr + carimbar os cards.
3. **NONE (1 seat)** — `GG-6113716239` seat "NONE": re-ler o seat ou limpar o fantasma.
4. **Card do Anton Efimov** — fora-de-grelha apanhado pela grelha apertada; falta carimbo.

## CANON dos bounties — anexos e regras novas
- **Tabela dos degraus** (`docs/CANON_BOUNTIES_DEGRAUS.md`, anexo do CANON): valores possíveis de
  coroa até ~3 KOs, por B — degraus **simples** (1, 1.5, 1.75, 1.875, 2, 2.25, 2.5, 2.75, 3, 3.5) +
  **splits°** (1.25, 1.625…); dólares pelos 9 B confirmados. 3 notas-lei: splits° fora da grelha do
  detetor (olho arbitra); só descodifica **sabendo o torneio** (valores colidem entre formatos);
  acima de ~3 KOs a régua perde poder (bisturi nos 2 primeiros KOs).
- **Tolerância de cêntimos** (`_JITTER=$1`): duas leituras a diferir **< $1** = a mesma coroa
  (tremura) → não é conflito; sela, vitrine informativa. Aplicada nos conflitos, no AUTO/OLHO e no
  detetor de quedas (já usava >=$1).
- **Regra da chama** (`_is_flame_candidate`): a chama (VPIP) é **SEMPRE inteiro 0-100** → um valor
  com **cêntimos NUNCA é chama** (é dígitos largados / swap / oclusão). A exclusão só auto-descarta
  chamas inteiras; um valor decimal que morreria → **olho** (`off_nao_chama`).

## Porteiro de coroas à entrada
- Já existia (`_guard_suspect_crowns`, decisão A do Rui): NULLa coroa **< KO inicial (base÷2)** +
  marca "por rever". Funil único: SS (`table_ss_deanon`), Gold/position_v3 (`screenshot._enrich`),
  releitura (`gg_health`). **Buraco fechado:** o **"Sugerir"** (`crown_recovery_suggest`) passa a
  gatear as coroas < KO inicial + expõe `no_floor` quando não há TS (passa, não rejeita às cegas).
- **Histórico medido: 0 violações** do chão na BD (limpo). O porteiro é preventivo.

## Grelha (detetor fora-de-grelha + exclusão)
- **Confirmado: multiplica sobre `B = KO inicial = buy_in_bounty ÷ 2`** (nunca a base) em TODOS os
  callers (`crown_recovery:207`, `gg_health floor=base/2`). Exemplos: $108→B$25, $525→B$125 — batem
  a tabela.
- **Tolerância apertada** de `max($1,…) <=` para **< $1 estrito** → `$26` (chama, a $1 de $25) fica
  fora-de-grelha; `$25.66` (tremura) fica dentro. **Fina até 2B, livre acima** (nota #3 do CANON —
  tentei discretizar [2B,3.5B] mas marcava 124 acumulações legítimas → revertido, decisão do Rui).
- Splits° presentes num conflito → **olho** (`_on_split_grid`, não auto-exclusão cega).

## Conflitos do cruzamento — endurecidos à raiz
- **O lote cego MORREU.** A amostra AUTO chumbou: `GG-6104056565` WhereIsMyBeer o AUTO escreveria
  **$40** mas a placa diz **$20** — o $40 era a **chama VPIP 40** que calha em 2×B (nenhuma régua a
  apanha). → **Seleção em lote** construída (33 cards pré-marcados + valor editável + 2 imagens zoom
  + torneio/B) → o Rui varreu e **selou os 33**.
- **A régua "crescimento óbvio" (B) morreu na raiz:** as leituras de um conflito são **todas da MESMA
  mão** → a coroa é **FIXA** nesse momento; não há crescimento nem "mais recente". A diferença é
  sempre misread → **≥2 valores possíveis → OLHO** (`ambos_possiveis`), com as leituras à vista.
- **Hora do Gold corrigida:** usava `entry.created_at` (= dia do **reimport**, 07-10) → passou a ler
  a **FOTO** (`file_meta.date/time`, ex. 06-21 19:04). O "mais recente" comparava relógios trocados.
- **Cards do olho:** **proveniência** do gravado (fonte + "visto em N mãos do torneio") + botão
  **"Mantém $X (selar)"** + **campo livre** "Corrigir a $___ (selar)". Prova: SaipraLaUrubu $75.78
  (4 mãos) e wakefulcheong $121.87 (3 mãos) = crowns reais; a SS é o outlier.
- **Estado: EXCLUSÃO 71 (auto, física certa) · OLHO 24 pendentes** (15 ambas-possíveis + 6 split° +
  3 off-não-chama) · exclusão informativa na vitrine.

## Vivo-$0 — os 4 baldes construídos e provados
- **Balde 1 — cross-hand do bust LIVE** (`_tournament_last_seen`): hash que não reaparece numa mão
  posterior = eliminado. **Prova: vGVSv (`GG-6117282604`) apanhado.** 15 seats / 3 mãos saem do
  painel → `GET /live-zero/eliminated`.
- **Balde 2 — releitura das 14 mesas-toda-$0** (todas só-SS): background (Vision, prompt atual) com
  Cancelar + custo à vista, **sem escrita** → cards de confirmação p/ o carimbo. Botão pronto.
- **Balde 3 — NONE** (1 seat): `GET /live-zero/none` + secção com imagem.
- **Balde 4 — painel só-genuíno:** exclui eliminados + NONE + mesas-toda. **Painel: 102 → 4 genuínos**
  (o Rui carimbou-os → painel a **ZERO**).

## Fora-de-grelha
- **Anton Efimov** apanhado pela grelha apertada (< $1) — pendente carimbo.

## Commits principais (main)
`662f4a7` degraus doc · `0f1ec5d` porteiro+cêntimos · `88a58ac`/`dd1211f` grelha (exclusão geral +
tolerância <$1) · `e872d0c` seleção em lote · `294fcd2` coroa-fixa + hora-foto · `88d4d8c` proveniência
+ campo livre + regra da chama · `851c2ce`/`cc1c8ad`/`95237ef` Vivo-$0 baldes 1-4 · `292e026` rótulos.

Boa noite.
