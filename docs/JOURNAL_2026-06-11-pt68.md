# JOURNAL pt68 — wipe total + re-teste faseado + Saúde do Import + incidente watcher + gate (11 Junho 2026)

Sessão longuíssima e multi-frente. Resumo dos arcos, por ordem.

## 0. Regras novas + correcções de estado (início)

- **`docs/FLUXO_DE_TRABALHO.md`** criado como leitura obrigatória de início de sessão.
- **Regra dos 25 BB CONFIRMADA pelo Rui** (`REGRAS_NEGOCIO §17`): sizing por stack
  efectiva — `eff ≤ 25 → [size, ALLIN]`; `eff > 25 → [size]`; open 2 BB só `eff > 8` e
  não-blind. Stack efectiva = `min(stack raiser, MAIOR adversário activo)/BB` por acção.
  → gate do `#IMPLICIT-LINES` aberto deste lado.
- **Regra de âmbito de disco (`FLUXO §11`)**: o Code só lê/toca os paths listados
  (intocáveis + `Batmen\`); procura fora exige autorização escrita. **As HH/TS GG vêm
  do BACKOFFICE do Rui** (fora do PC) — nunca vasculhar o disco por elas.
- **Desktop do Beelink = zona limpa** (`CLAUDE.md`): o `instala` passa a varrer
  `.bak`/`.backup` além de `.exe`.

## 1. IT formato antigo + Saúde do Import

- **`feat(appimport)` (`45e555f`)** — o router do `it\` aceita agora **ambos** os formatos
  (`Shot<N>-<Site>-<ts>` → MESA, site+captured_at do nome; backend já o suportava). +5
  testes (17 PASSED). Appimport-only, sem deploy.
- **★ Saúde do Import v1 (`6259fd4`)** — página `/import-health` + `GET /api/import-health
  ?day=YYYY-MM-DD` (janela dia-de-jogo 15:00→15:00). Por pipeline (hands/mesa/lobby/hh_ts/
  inbox): contagens + falhas/sem-match com motivo. Lista os **buracos de logging** (HM3,
  Vision replayer, SKIP appimport, parse HH por mão). Instrumento de validação da Etapa 1.

## 2. Wipe total + Etapa 1 (sessão de dia 4)

- **Wipe APP** (`_local_only/reset_pt68/`): backup logical **restore-verificado** (46,4 MB,
  23 tabelas OK) → `TRUNCATE 18 RESTART IDENTITY CASCADE` atómico (assert-zero, preserva
  `users`/`stat_ideals`/`monthly_stats`/`gto_*`). Cobertura cross-checked vs
  `information_schema` (zero gaps). Estado: 22 tabelas a 0, `users`=1.
- **Movimento de ficheiros** (reversível, logado): GG 3+3 de `done\` → `gg_hh/gg_ts`;
  descoberta de que a "sessão 04-05" era de facto **3-4 Jun** (horas reais dentro das zips,
  não mtime) e que **GG (3-4 Jun) e IT (5+ Jun) não se sobrepõem** → Etapa 1 fixada na
  sessão de **dia 4** (com a GG do backoffice). Lobby de dia 5 estava nas Capturas
  (`Screenshots`), não no pool IT.
- **Etapa 1 (corrida do Rui):** **6 canos validados** (HH/TS/mesa/lobby/HM3/Discord);
  **~5044 mãos** (4608 hh_import + 334 hm3 + 102 discord); **123/127 órfãos** re-anexados;
  TS inserted=65/updated=20; table-SS a casar com mãos HM3 + Discord.

## 3. 502 do `/api/import` = timeout síncrono (import COMPLETO)

`POST /api/import` devolveu **502 "Application failed to respond"** no zip GG. Diagnóstico
(com fonte): **não foi crash/memória/formato** — o zip são 7,6 MB / 103 ficheiros; foi
**timeout do processamento síncrono** (~4710 mãos numa request → edge da Railway desiste,
servidor completa). **Verificado mão-a-mão: as 4710 mãos (4-9 Jun) estão TODAS na BD**
(`distinct hand_id` GG = 4710, 0 em falta) → **import completo, resposta perdida**.
Re-try **não necessário**; se feito, idempotente (`_insert_hand:304` salta `hand_id` já
existente). Debt **`#SYNC-ENDPOINTS-SYNCHRONOUS-TIMEOUT`** (2 instâncias: este +
`sync-and-process`; fix = job background) + quick-win: escrever `import_log` no início.

## 4. ★ Gate server-side da fila HRC v1 (`c10e303`)

A fila nasce **FECHADA**: `GET /api/queue/hrc` só serve mãos **libertadas** (`hrc_queue_
release`) e não-done. `POST /trigger?count=N` ("Disparar tudo/lote de N") + `GET /gate`
(estado). Auto-fecha no consumo. Per-mão `/hand/{id}` não-gated. **Zero alterações no
Beelink** (adapter idle em vazio). Página HRC Queue com chip 🔴/🟢 + botões.

## 5. Incidente do watcher (madrugada) + mini-auditoria

- **Rendimento:** 3 done (`GG-6041693873/898` + `GG-6041861838`) + **1 setup-failed**
  (`GG-6041693835`, 1ª mão, cold start). **Pré-check de offset (linhas implícitas) → as 3
  done VERDES** (offset enviado = correcto; nenhuma suspeita `#IMPLICIT-LINES`).
- **Padrão = DEGRADAÇÃO PROGRESSIVA** (setup-failed 02:14 → 3 OK → derail ~02:35; ritmo
  3→9 min). **Causa confirmada na fonte:** o watcher abre wizard/aba por mão mas **NÃO
  fecha a anterior** → abas acumulam (Rui viu 3 abas = 3 sucessos) → fuga de memória.
- **Decisão do Rui:** **suspensão CONSCIENTE e temporária da lei "fila fechada"** durante a
  fornada da noite (teste de carga real; resultados `#IMPLICIT-LINES` → quarentena quando o
  fix chegar). Não parar nada por iniciativa própria.
- **Consola perdida → log-em-ficheiro** subido a prioridade.

## 6. ★ Novo build do watcher pt68 (3 fixes) — exe construído + publicado

Ordem do Rui, diffs validados:
1. **`_close_hand_tab`** — **Ctrl+F4** (o **Ctrl+W é chord de nova-mão no HRC — NUNCA
   usar**, achado empírico do Rui) → diálogo SWT "Save Resource" → **Don't Save** via Win32
   BM_CLICK. Guard: HRC sumiu → relança.
2. **`_restart_hrc`** a cada **5 mãos** (captura o path do `hrc.exe` a correr, robusto) +
   **`_wait_hrc_responsive`** (wpos estável 3 ciclos + grace 8s) no cold start E
   pós-reinício → mitiga o setup-failed cold-start.
3. **`_ensure_file_logging`** — Tee stdout/stderr → `C:\hrc\watcher_logs\` (1/sessão,
   mantém 14).
Tudo em `setup_hand` + helpers; loop principal intocado. **swap_and_smoke ALL OK** (48
funcs, 40 consts). `.exe` **SHA256 `222fc48d…3f57`** (12,90 MB). **Release `watcher-pt68`
publicada via API REST** (push creds; gh ausente) + `instala_pt68.bat` — **validada**
(download anónimo 200 + SHA round-trip exacto). **Instalação no Beelink pendente.**
Source: `feat(watcher) 17628ee`.

## 7. ★ Multi-select "Enviar ao HRC" v1 — backend live

Aprovado o desenho (release forçado independente do cabaz; estados por mão; guardas com
motivo). **Backend (`ff1fba2`):** `POST /api/queue/hrc/release` {hand_ids} (release
FORÇADO, salta não-exportáveis com motivo via `build_queue_zip` per-mão, idempotente) +
`POST /api/queue/hrc/states` {hand_ids} (nada/na fila/concluída/falhou + exportável?).
Testado + verificado LIVE. **Frontend (Estudo, `Hands.jsx`) = 1º item da próxima sessão.**

## Builds de infra desta sessão

- **Pin Python 3.12** (`2c0823b`) — o mise da Railway não tinha binário p/ 3.13.14; +
  `audioop-lts` só-3.13 (`bdb3c68`). Desbloqueou todos os deploys.

## Features futuras registadas (desenho adiado)

- **Gate por botão** (`#QUEUE-NO-SERVER-SIDE-GATE`) — **ENTREGUE** nesta sessão.
- **Multi-select "Enviar ao HRC"** — backend entregue; frontend próximo.
- **Avaliação automática Herói vs HRC** — registada (`REGISTO_CONCEITO`/`PENDENTES`).

## Pendente à entrada da próxima sessão

Ver `PENDENTES.md` (actualizado). 1º item: **frontend do multi-select** (backend live à
espera). Depois: instalar o exe pt68 no Beelink + smoke do reinício-a-cada-5 e do fecho de
abas.
