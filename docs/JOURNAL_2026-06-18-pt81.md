# JOURNAL — pt81 (18 Junho 2026)

## `#WN-LOBBY-NO-AUTO-RETRY` — diagnóstico, limpeza do lote 16-Jun, e fecho da raiz

**Backend-only.** Três peças: diagnóstico (read-only) → limpeza do lote 16-Jun
(escrita scoped via endpoint) → **fecho da raiz** (gatilho reconcile no caminho HM3).

### 1. Diagnóstico (read-only) — porque é que os lobbys WN não se auto-religam

Apesar de o reconcile-no-import já existir, os 14 lobbys WN de 16-Jun ficaram
`tm_not_found`. Raiz:

- **posted_at = 16-Jun (CORRECTO)** — dia do torneio, não data de import/agora.
- **As 49 mãos WN de 16-Jun entraram por `origin='hm3'` (`hm3_synthetic`)** — pelo
  **.bat do HM3 (`import_hm3`)**, importadas 18-Jun 12:47.
- O gatilho `reconcile_lobby_logs` (fire-and-forget) só vivia no `import_.py`
  (`/api/import`) e no `tournament_summaries` — **NÃO no `import_hm3`**.
- Timeline: lobbys processados 12:33 (antes das mãos), mãos por HM3 12:47 (sem
  gatilho), `attempt_count=1` → nunca re-tentados.
- `dry_run` resolve-os agora → **anchor bom; raiz = gatilho/timing, não dados**.
- Os 3 de 15-Jun (EXPLORER/GRAVITY/GALACTICA) resolveram-se com **re-upload pela
  página** (data 15-Jun certa, já depois das mãos), não com data errada.

→ Raiz: **(b) o caminho de import (HM3) não dispara o reconcile** + (c) timing.

### 2. Limpeza do lote 16-Jun (escrita scoped via endpoint)

Como o reconcile global varre **18** pendentes (14 WN do lote **+ 4 GG** a tratar à
parte), precisava de scoping para escrever só o lote.

- **`POST /api/lobbys/reconcile` ganha `message_ids`** (commit `ba7a13d`, deployed):
  param opcional → lote específico; omisso = global de sempre. `reconcile_lobby_logs`
  já o suportava. +4 testes.
- **Escrita scoped** (via endpoint deployado + cookie, regra de prod — não script
  local): `dry_run=false`, `message_ids` = os 14 WN. Resultado: **12 escritos**
  (7 torneios, source `reconcile_lobby_vision:`), **2 still**:
  - `LATE REGISTRATION` — misread da Vision (não há torneio com esse nome);
  - `ZENITH 15-Jun` — sem instância ZENITH de 15-Jun nas mãos (só a de 16-Jun).
- **Precedência OK** (`skipped_prec=0`; 0 `manual:`/`backoffice_vision:` sobrescrito;
  os 5 `file_lobby_vision:` são os re-uploads de 15-Jun, torneios diferentes), **4 GG
  intactos**.

### 3. Fecho da raiz — gatilho reconcile no caminho HM3

`import_hm3` (hm3.py) passa a disparar o **mesmo** gatilho fire-and-forget que o
`import_.py`:

- O **relink de SS de mesa órfãs** já lá estava (`_table_ss_relink_async`).
- **NOVO:** `_lobby_reconcile_async` → `await asyncio.to_thread(reconcile_lobby_logs)`
  (espelho de `import_.py:506-519`). Agora, quando as mãos WN entram pelo `.bat`, o
  reconcile re-corre sobre os lobbys pendentes → **auto-retry**, caminho HM3 igual aos
  outros.
- +3 testes (`test_import_hm3_triggers.py`, inspeção de source — padrão de
  `test_order_convergence_pt50::test_fix_a`): gatilho reconcile cablado, relink mantido,
  e **alinhamento** import_hm3 ↔ import_file.

### Deploy

Push **isolado** (cherry-pick a partir de `origin/main`): só este arranjo vai ao
origin; os 3 commits do watcher (`#HRC-RESTART-POST-WINDOW-FAILURE`) **ficam locais**,
atrás do gate do re-smoke. `origin/main` = FT + reconcile-scoped + este.

### Pendentes mexidos

- `PENDENTES` pt78 item 1 (`#WN-LOBBY-NO-AUTO-RETRY`) → ✅ **RESOLVIDO**.
- `PENDENTES` pt78 item 3 (religação a sério dos 16-Jun) → ✅ **FEITO**.
- Continua aberto: item 4 (GG `tm_not_found`: ₮109 Kickoff, 2 Bounty Hunters,
  William Harding — misreads/triagem) + os 2 WN still (LATE REGISTRATION, ZENITH 15-Jun).
