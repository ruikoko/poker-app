# JOURNAL 2026-06-19 (pt84) — Vigia de mão pendurada + set-aside da 125360 + re-smoke Passo 1

Sessão centrada no **`#HRC-HANG-WATCHDOG`**: diagnóstico do pendurar do watcher
numa OOM/freeze do HRC, construção do **vigia**, **set-aside** da mão-veneno
(125360) e prova do **Passo 1** do re-smoke (reinício-após-falha). Os 5 commits do
watcher seguem **locais, atrás do gate do re-smoke** (vigia por validar nos
Passos 2/3).

## Arco do dia

1. **Revisão da corrida noturna.** O solve de madrugada fechou **4 mãos**
   (`GG-6083184245` 02:52, `WN-…581-17` 02:57, `GG-6083293633` 03:19,
   `GG-6083293771` 03:32 UTC), árvores validadas ponta-a-ponta. Depois **pendurou
   ~5h** na **`GG-6083125360`** (id 681): o HRC deu **OOM/freeze** no Hand Setup e
   o watcher ficou cego ao congelamento (`Activas: ['GG-6083125360']` até ao fim
   do log). 2ª vez no dia que esta mão trava o HRC.

2. **Diagnóstico (medido no código).** (a) O `-Xmx` (heap JVM) **não vive no
   watcher** — `Popen([HRC_EXE])` sem args; vive na config do HRC no Beelink
   (`hrc.l4j.ini` / `_JAVA_OPTIONS`). (b) A **hygiene-5 nunca disparou**: o
   contador `_HANDS_DONE_SINCE_RESTART` é **resetado no restart-por-wizard-falhado**
   (`patched_funcs.py:2290`) — confirmado. Mas **não foi a causa** (a OOM é
   single-hand, não acumulação cross-mão). (c) **Buraco real = sem deteção de
   freeze**: as esperas longas (run-wait 7200s; finish-wait) não detectavam um HRC
   congelado → espera indefinida.

3. **Decisão (Rui):** (A) **vigia** que apanha o freeze a meio de uma mão; (B)
   subir o **`-Xmx`** (Beelink 32 GB → alvo **12288m**), **segurado** para depois
   da prova do vigia.

4. **Vigia construído** — commit local **`8e460f0`** (`#HRC-HANG-WATCHDOG`),
   **+14 testes** novos (`test_watcher_hang_watchdog.py`), **127 testes watcher
   PASSED**. Em `tools/watcher_src/patched_funcs.py`, tecido nas esperas via
   `_watchdog_sleep` (throttle 5s) em `_wait_for_run_completion` (Fases A+B) e
   `_wait_for_finish_ready` (Hand Setup). **3 sinais** (`_watchdog_reason`):
   (1) diálogo fatal Java/OOM por título (`getAllWindows`); (2) `IsHungAppWindow`
   **sustido ≥ 20 min** (guard contra falso-positivo em solves legítimos ~78 min);
   (3) `HRC_WATCHDOG_SMOKE_FORCE` one-shot (prova de recuperação sem OOM real).
   **Recuperação** (`_watchdog_trip`): `mark_failed(reason)` ANTES de propagar (o
   reason sobrevive — `try_setup.except` não re-marca) → `.failed` → adapter POSTa
   failed → `hrc_jobs` failed → re-queue; depois `_restart_hrc` + levanta
   `HRCWatchdogError` → a fila avança. Desligável por `HRC_WATCHDOG_DISABLED`.
   Âmbito v1: esperas tecidas do `setup_hand` (Hand Setup + runs); **não** cobre
   esperas não-tecidas nem o `zip_is_ready` do main-loop Baltazar (follow-up).

5. **`-Xmx` 12 GB** — alvo `-Xmx12288m` (Beelink 32 GB). **Segurado** até o vigia
   provar; serve também de Ângulo B (reduz a frequência da OOM na origem).

6. **Set-aside da 125360 (id 681).** Endpoint novo **`POST /api/queue/hrc/set-aside
   {hand_ids, note}`** — commit **`b4e01cc`** (em origin, **deployed** nos 2 apps).
   Inverso do `/release`: **des-libertar** (DELETE `hrc_queue_release`) + **nota de
   auditoria** (UPSERT `hrc_jobs` failed, `meta.set_aside=true`). **NÃO
   re-enfileirável** (un-released → fora do `/sent` → sem botão "Re-pôr na fila" =
   sem foot-gun). Dry-run read-only → escrita via endpoint (auth cookie, regra
   "escrita em prod só por endpoint"). **Estado final verificado:** `released=False`,
   `SERVIDA=False`, `andar1=True` (re-libertável), `hrc_jobs=failed`+nota, fora do
   `/sent`. Beelink: a **pasta stale** `Teste completo\GG-6083125360\` apagada pelo
   Rui (essencial — o watcher processa o que está na `QUEUE_DIR`, não o que o
   backend serve); limpeza da entrada no `state.json` do adapter = preventiva,
   opcional (o re-queue no re-release trata do dedup).

7. **Passo 1 (re-smoke do reinício-após-falha pt79) PASSOU** (log 130855):
   `HRC_WATCHER_SMOKE_FAIL_FIRST=1` → smoke-fail em **`GG-6083680228`** → **`[HRC-RESTART]`**
   em **`GG-6083680253`** (logo após) → segue normal.

8. **Descoberta — churn do chord morno (pt70, já diagnosticado).** O chord de abrir
   wizard (Ctrl+W,M, multi-stroke SWT) **falha em HRC já usado**: depois do fecho de
   aba (Ctrl+F4 + Don't Save), o `_restore_hrc_main_focus` repõe o *foreground* mas
   **não o contexto do chord** ("necessário, não suficiente") → só o **cold start**
   (rung-2 restart) é 100% fiável → **1 restart por mão**. Recupera sempre; é churn +
   latência. **É por isto que a hygiene-5 nunca acumula** (cada mão é cold start na
   prática — liga ao ponto 2b). **Baixa prioridade** — parqueado; alavanca = o
   contexto do chord (clique real na área de conteúdo / New Hand pelo menu via Win32 /
   PostMessage ao widget SWT), não testado.

9. **6 mãos reais resolvidas+exportadas** no re-smoke (todas `done` 12:15→12:34 UTC,
   árvores em `C:\hrc\trees` com nome bonito, **adapter novo confirmado** pelo Rui):
   `GG-6083680253`, `GG-6083680311`, `GG-6083680532`, `GG-6083680638`,
   `GG-6083048016`, `GG-6083048023`. (`GG-6083680228` = smoke-fail, sem tree.)

10. **Gate.** Os **5 commits do watcher** seguram **juntos**, locais, atrás do gate
    (headline `8e460f0` vigia + `d2981ec` restart-pós-janela pt79 + 3 docs/runbook
    pt79). **Vigia (Passos 2/3) por validar** antes de despachar; só depois **`-Xmx`
    12 GB**. Argumento para não separar o pt79: o restart-entre-mãos do rung-2 (churn
    do ponto 8) já previne a cascata cross-mão → o pt79 sozinho adiciona pouco agora;
    o **vigia** é a proteção genuinamente nova (OOM/freeze a meio de UMA mão, que o
    restart-entre-mãos NÃO cobre).

## Estado no fecho
- **origin (deployed):** `b4e01cc` (set-aside endpoint). [+ trabalho anterior do
  arco já em origin: tree-persist + hero-cards fix + sent/requeue/Enviadas.]
- **local (atrás do gate, NÃO pushed):** 5 commits do watcher
  (`8e460f0`/`421a4fa`/`cf7a533`/`5300c14`/`d2981ec`). Release de smoke publicada:
  `watcher-pt84` (1 exe serve as 3 validações por env; SHA `5E141481…713A`).
- **Fila:** limpa — 125360 de lado; restantes mãos em `por_resolver`/`resolvida`;
  2 WN `cancelada` (candidatas a re-queue após `-Xmx` maior).

## Parqueado / próximo
- **Passos 2/3** do vigia (camada 2 `HRC_WATCHDOG_SMOKE_FORCE` + OOM real com `-Xmx`
  minúsculo p/ calibrar o título do diálogo) — **gate do push dos 5 commits**.
- **`-Xmx` 12288m** no Beelink — depois da prova do vigia.
- **Chord morno fiável** (evitar 1 restart/mão) — baixa prioridade.
- **Re-try da 125360** + das 2 WN — re-libertar no fluxo normal quando a memória subir.
