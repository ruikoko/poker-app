# JOURNAL — 19 Julho 2026

> Nota de relógio: o sistema reportou "15 Jul"; segue o fecho de 18 Jul → datado **19 Jul**.

Dia longo à volta das **coroas recuperáveis (Etapa 2)**, do **selo de nomes** e da **saga do
$437.5**. Tudo LIVE em `main` (`46e5f11`…`3029241`). As correções de coroa em prod foram por
endpoint (`/set-bounties`, Bearer) — sem commit.

## ★ 1. Cura do carimbo — SELO das coroas/nomes (invariante do Rui)
`46e5f11` — "o que o Rui valida fica selado; nenhum automático escreve por cima". Fonte única
`eliminated_bounty.is_bounty_sealed` (`bounty_source ∈ {manual, green_ko, derived_green_ko}` ou
`bounty_confirmed`). 7 caminhos automáticos ganharam "selado?→salta" (apa `_enrich`, players_list
`_merge_sealed_crowns` no deanon, guarda ½-base, funil scrub, `/crowns/reread`, `force`-refresh,
guarda 3-b de nomes). `/set-bounties` carimba `manual` nos 2 stores. **Provado ao vivo na 6570**
(GG-6104058222): forçado o re-apply, 8/8 valores sobreviveram (log `[crown-seal]`). Lei em
`docs/LEI_SELO_COROAS_NOMES.md`. Teste `test_crown_seal.py` (8/8). Suite 1482 passed.

## ★ 2. Gate >3× EXTINTO + validade de coroas
`5dea94a` — coroa alta = acumulação legítima (a coroa do mesmo hash só sobe). Removido o gate
`bounty_above_3x_unconfirmed` do export (libertou 112 mãos). **Grelha de validade:** física
(B, 1.5B, 1.75B split, …); mas **não é bom detetor** (misreads caem em valores de grelha) →
guardas duras = `<½base` + **"não-desce"**; grelha = sinalizador leve. Mecânica PKO registada em
`REGISTO_CONCEITO`: coroa = metade do total (unidade da BD); verde tal-e-qual SEM ×2.

## ★ 3. Etapa 2 dos bounties recuperáveis (LIVE)
`4768e9b` (sugerir só-ao-verde + carimbar `derived_green_ko`) · `fce4af1` (worklist "revisão de
quedas") · `d099095` (`/suggest` lê AMBOS verde+dourada numa chamada + lote "Sugerir todos" com
progresso/cancelar) · `9f485d9` (fix `/drops` mostrava 0 = corrida de deploy, `import json` +
Bearer; **cache das sugestões pagas** — não evaporam no refresh; card multi-KO com última coroa +
soma; **lei universal do lightbox** `ZoomImg`) · `5ce23e0` (input de correção **VAZIO** — nunca a
referência como palpite; botão **Dispensar (legítimo)**; marca **migração de unidade** total→coroa).
Custo real no botão: **$0.014/leitura** (sonnet-4-6 $3/$15, imagem 1280px). Detetor: 33 quedas +
7 fora-de-grelha.

## ★ 4. SELO DE NOMES — 3 fantasmas curados (reincidência GG-6177132682)
`8cb9fe4` — a cena do crime tinha `verified_by_user=None` (a correção nunca era selada). 3 caminhos
fora dos guards, agora fechados:
- **(a)** `/set-anon-map` (o fluxo de correção) escrevia sem `verified_by_user` → carimba SEMPRE.
- **(b)** `reconcile_tournament_deanon` re-escrevia todas as mãos `table_ss` do torneio pela votação
  → salta verificadas.
- **(c)** `_enrich_hand_from_orphan_entry` (re-link Gold): guard só cobria v2/position_v3 → após
  `/set-anon-map` (=table_ss) o re-link revertia → `verified_by_user` entra no guard.
Com a guarda 3-b (deanon por-mão), **4 caminhos** respeitam o selo. **PROVA PENDENTE:** o Rui
corrige o HJ da GG-6177132682 uma última vez → forço reconcile + re-link + re-deanon → o nome
sobrevive. Só então fecha.

## ★ 5. A SAGA DO $437.5 (GG-6090481210) — misread provado pelo OLHO do Rui
"Queda" $437.5→$421.87 no mesmo torneio. Construí 3 teses (mecânica GG de −B/8; indexação; match de
stacks 6/7) e dei "queda real, placas confirmadas" **sem ninguém ter olhado a placa**. O Rui abriu a
imagem (entry 1720): **a placa diz $421.87, não $437.5** — o gravado era **misread da Vision**, não
havia queda (421.87→421.87). Corrigido a **$421.87 selado `manual`** (ambos os stores). **LICÃO
(`3029241`):** só a imagem arbitra o VALOR de uma coroa; stacks/dyadic/mecânica validam a MÃO, nunca
o número; "placa confirmada" só quando alguém olhou mesmo. (1º link caiu no dashboard por estar no
domínio do frontend — a imagem vive no backend `/api/screenshots/image/<entry>`.)

## Auditoria de tags (read-only, iniciada — retoma amanhã)
tn 291226445: 181 mãos, **0 capturas table-SS**, 24 capturas **Gold** (entries), todas **reimportadas
10 Jul** de nomes **flat** (sem subpasta de tag), `discord_tags=[]`. Quadro BD: table_ss com
`folder_tag` em 8-9 Jul e 12-13 Jul; **10-11 Jul = 1143 capturas SEM folder_tag** (o reimport pós-wipe).
GG 2026: **25 908 mãos sem tag** / 434 com. Contrato atual (≥12 Jul): 194/204 table_ss COM folder_tag
(o cano vivo cumpre). Hipótese do `done` **plausível** mas falta destrinçar Gold-lane (nunca tagou?)
de reimport-flat. **Retoma: fechar o tamanho do buraco só-folder-tag.**

## Commits (main)
`46e5f11` selo · `5dea94a` gate>3× + conceito · `4768e9b` Etapa 2 · `fce4af1` worklist quedas ·
`d099095` suggest-ambos+lote · `9f485d9` /drops fix+cache+multiKO+lightbox · `8cb9fe4` selo-nomes ·
`5ce23e0` input-vazio+dispensar+migração · `3029241` LICÃO. (Correções de coroa da 6570 e da
GG-6090481210 por endpoint, sem commit.)
