# Sessão pt13 — 4 Maio 2026

Sessão curta de housekeeping. 1 tech debt fechado.

## Commits
| Hash | Mensagem |
|---|---|
| `554cafb` | chore(#B-NOVO-2) assert defensivo + fechamento + journal pt13 |
| `6ee5e8e` | docs(pt13) hash do commit anterior |
| `d478b68` | chore(#B29) remove paths sem Q6 guard (single source of truth = apply_villain_rules) |
| `5cd5440` | docs(pt13) hash do #B29 |
| `b455ff5` | docs(#B31) MAPA_ACOPLAMENTO actualizar para refactor #B23 + #B29 + vilão principal |
| `9be07a2` | docs(pt13) hash do #B31 |
| `407d001` | docs(pt13) preencher hash 9be07a2 na tabela de commits |
| `5bd3bfc` | chore(docs) apagar HANDOFF.md stale + TECH_DEBTS §5-§10 plano pré-pt8 |
| `8131e17` | docs(pt13) limpeza documental |

## #B-NOVO-2 — fechado

### Contexto
Tech debt aberto em pt10 (refactor #B23) marcado como "parcialmente
resolvido por #B32, merece investigação dedicada se re-aparecer".

### Investigação
Code investigou o estado actual:
- Bug original #B32 (pt10, commit `5fe2201`): apa placeholder-only
  fazia `_build_anon_to_real_map` retornar `{}` silenciosamente,
  e `match_method='v2'` era gravado na mesma. Hands ficavam presas
  com hashes em vez de nicks.
- Fix #B32 cobriu set + guard de idempotência.
- 23/23 hands afectadas re-curadas em pt10.

### Verificação em prod
Query directa BD prod:
- `degenerate_count` = 0 (esperado)
- `total_gg_enriched_2026` = 24 (bate com snapshot pt12)

### Defesa adicional
Assert defensivo em `_enrich_hand_from_orphan_entry` antes da
chamada a `_build_anon_to_real_map`: levanta `ValueError` explícito
se apa só tem `_meta`. Torna eventual recidiva visível em vez de
silent skip.

### Resultado
#B-NOVO-2 fechado. Backlog reduz de 13 para 12 tech debts abertos.

## #B29 — fechado

### Contexto
Tech debt aberto em pt10 marcado como "Funcional latente —
contadores hands_seen possivelmente inflados em refix".

### Investigação
3 sítios incrementam `villain_notes.hands_seen`:
- A) `services/villain_rules.py:_persist` (canónico, com Q6 guard) ✓
- B) `routers/mtt.py:_create_villains_for_hand` (legacy bulk archive,
  sem guard) ✗
- C) `routers/mtt.py:re_enrich_all` (UPSERT manual após enrich,
  sem guard) ✗

Sítios B e C podiam causar double-count quando hand passava por
mais de um path.

### Estado em prod
- `total_villain_notes` = 42
- `hands_seen>0` = 29 (corresponde a hand_villains únicos)
- inflação total = 0 (curado por `recalculate_hands_seen` em pt12)
- código continua armado para futura inflação

### Fix (opção α)
Remover os 2 blocos UPSERT desprotegidos:
- `mtt.py:_create_villains_for_hand:793-801` (8 linhas)
- `mtt.py:re_enrich_all:2045-2081` (37 linhas — bloco + dead code
  associado)

Substituídos por comentários explicativos `#B29 pt13` documentando
o que foi removido e porquê.

Net: −32 linhas. `apply_villain_rules` é a única fonte de verdade.

### Verificação pós-fix
- `hands_seen>0` = 29 (mantido)
- `hand_villains` = 34 (mantido)

Bate 1:1 com snapshot pt12. Mudança em código sem efeito em dados.

### Resultado
#B29 fechado. Backlog reduz de 12 para 11 tech debts.

## #B31 — fechado

### Contexto
Tech debt aberto em pt10 marcado como "MAPA_ACOPLAMENTO actualizar
para reflectir refactor #B23 (§7.4-§7.5 stale)". Risco: regra de ouro
do CLAUDE.md ("mexer sem ler causa regressões") era minada quando o
MAPA referia funções apagadas.

### Investigação
§7.4 documentava `_create_ggpoker_villain_notes_for_hand` (apagada
em Onda 6 #B23). §7.5 documentava `_create_hand_villains_hm3` (apagada
em Onda 6 #B23). Outras 7 secções tinham cross-refs ou descrições
stale: §2.1 Armadilhas, §2.8 Comportamento, §5.2 HM3 ingest, §5.4 SS
upload, §6.3 Vilões header, §7.3 `_link_second_discord_entry`, §8.1
Índices UNIQUE.

Achado lateral: `VILLAIN_ELIGIBILITY_CONDITION` (`villains.py:67-85`)
ainda lista A∨B∨C no SQL filter da UI, mas branch B (showdown sem tag)
é dead code desde #B8 (pt7) — `_classify_villain_categories` retorna
A∨C∨D. Distinção UI filter vs classification logic precisava ser
explicitada.

### Escopo do fix
Substituições (2):
- §7.4 nova: `apply_villain_rules` — função canónica (algoritmo,
  signature, regras A∨C∨D, Q6 guard, filtro vilão principal por street
  máxima, armadilhas conhecidas, cross-refs).
- §7.5 nova: Call sites de `apply_villain_rules` (8 em produção +
  2 scripts ad-hoc + caller pattern recomendado).

Cross-refs actualizadas (7 secções):
- Índice (linhas 57-58): renomear entries.
- §2.1 Armadilhas: invariante GG → `apply_villain_rules:73-76`.
- §2.8 Comportamento: regra B eliminada em #B8; descrever via
  `_filter_to_furthest_street`.
- §5.2 HM3 ingest: `_create_hand_villains_hm3` → `apply_villain_rules`.
- §5.4 SS upload: `_create_ggpoker_villain_notes_for_hand` →
  `apply_villain_rules`.
- §6.3 Vilões: distinção UI filter (A∨B∨C dead branch B) vs
  classification (A∨C∨D) com nota explicativa.
- §7.3 `_link_second_discord_entry`: `_create_villains_for_hand` →
  `apply_villain_rules`.
- §8.1 Índices UNIQUE: cross-refs canónicas.

### Decisão sobre `VILLAIN_ELIGIBILITY_CONDITION`
**Opção α** adoptada — documentar dead branch B em vez de mudar SQL.
Branch B continua presente no filtro UI mas é inerte (JOIN com
`hand_villains` limita a hands realmente classificadas A∨C∨D, logo B
filtra zero hands incrementais). Mudar SQL exigiria backfill +
validação prod; documentar não.

### Verificação
Grep pós-fix por nomes de funções apagadas no MAPA:
- 6 hits restantes — todos referências históricas explícitas
  (substitui... / REGRAS §8 limitação...). ✓

### Resultado
#B31 fechado. Backlog reduz de 11 para 10 tech debts abertos.

Stat: 1 ficheiro tocado, 174 insertions, 36 deletions.

## Limpeza documental — pt13

### Contexto
Parte C do plano "A+C" da pt13 — limpeza de documentação stale após
fechamento de #B-NOVO-2, #B29, #B31. Identificada durante re-leitura
canónica do contexto.

### O que foi apagado

**HANDOFF.md** (raiz, 92 linhas, datado 21-Abr-2026 madrugada):
- Conteúdo factual stale: números BD pré-pt7 ("1233 mãos, 1413 villains"
  vs realidade actual de 24 GG enriched + 34 villains).
- Lista scripts ad-hoc já apagados em #B30 (`reparse_corrupted.py`,
  `reparse_missing_showdown.py`, `reparse_selective.py`,
  `backfill_villains.py`, `test_parser_fix.py`).
- "Por fazer" referia trabalho já feito (expor `has_showdown` na UI)
  ou abandonado (SharkScope continua parado, sem progresso).
- "Como começar próxima sessão" duplicava CLAUDE.md com info menos
  completa.
- CLAUDE.md (153 linhas) cobre a parte canónica ainda relevante (stack,
  URLs, regras de ouro Claude). Manter HANDOFF.md era manter mentira
  documental.

**TECH_DEBTS_INVENTARIO.md §5-§10** (~122 linhas, plano sequencial
pré-pt8):
- §5 Análise dependências: referia #22, #18, #15, #B7, #12, #13c, #B12
  como pendentes — todos fechados pt7-pt10.
- §6 Plano sequencial Blocos A-F: ~16 items, quase todos resolvidos
  (#15, #22, #18, #10, #B1, #B2, #B4, #B8, #UX1, #12, #13c).
- §7 Esforço total: tabela de estimativas para o plano §6 (obsoleto).
- §8 Riscos identificados: 4 riscos sobre fixes já implementados.
- §9 Decisões pendentes Rui: 5 items, 4 resolvidos. Item 3 (#11
  blacklist persistida vs re-criar villain) preservado inline na row
  de #11 do backlog actual.
- §10 Notas próxima sessão: referia #22, Pipelines 4/5, integration
  test específico para #22 — todos resolvidos ou obsoletos.

### O que foi preservado

- **Comentário histórico HTML** no sítio de §5-§10 a explicar o quê,
  porquê, e ponteiro para git history. Garante que próxima Claude que
  procurar §5-§10 percebe imediatamente o que se passou.
- **TODO comment** no topo de §2 a marcar limpeza separada futura
  (entries ✅ RESOLVIDO misturadas com bugs ainda abertos em §2).
- **Decisão #11** movida inline para a row de #11 no backlog actual.

### Verificação
- TECH_DEBTS_INVENTARIO.md: 478 → 365 linhas (−113 net).
- HANDOFF.md: 92 → 0 (apagado).
- Total: −243 linhas líquidas de docs stale.

### Resultado
Documentação canónica fica focada no estado actual + histórico válido
(fechados pt8/9/10/12/13). git history preserva todo o conteúdo apagado
para arqueologia futura.
