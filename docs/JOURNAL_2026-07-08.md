# Journal 2026-07-08 — Frente FT construída por inteiro (F1–F5) + teste do invariante dos hashes

Sessão longa (7–8 Jul). Construiu-se a **propagação de FT (mesa final)** ponta-a-ponta —
motor, guardas de payout, UI de quarentena, gatilhos e fluxo de aprovação — e correu-se o
**teste sistemático do invariante dos hashes GG** (evidência para a decisão do core, NÃO
APROVADA). Tudo LIVE em produção. As operações read-only contra a BD de prod passaram a usar
um ficheiro local `~/.pokerapp_db_ro.env` (fora do repo, só-leitura) — ver `#RAILWAY-TOKEN-SHORT-LIVED`.

## Commits (main, todos deployed)

| Commit | O quê |
|---|---|
| `cda19e7` | **F2** — motor gateado por `open_tab='Info'` + cross-check HH + Vision `open_tab`/`final_table_size`. |
| `5fe9ed9` | **`#LOBBY-INFO-NO-PAYOUT`** — print da aba Info nunca escreve payouts (gate em `process_lobby_message` + `reconcile_lobby_logs`). |
| `47a0d35` | **`#PAYOUT-COHERENCE`** — guarda de sanidade (prémio>pool / soma>pool×1.05 / escada não-monótona) em todas as fontes Vision. |
| `659d6f9` | **`#LOBBY-FORCE-REVISION`** — `force=true` no upload refresca só o `vision_json` (não toca payouts). |
| `1eedd8f` | **snap-to-N** + coerência só na cauda pós-pico (política da fronteira, 5 casos reais). |
| `f989836` | **fonte (0)** — tag `-ft` MANUAL (`folder_ft_source='manual'`) no topo da cascata `(0)→(a)→(b)→none`. |
| `35e421e` | **F3** — tabela `ft_boundary_review` + `GET /ft/preview`, `POST /ft/confirm|correct|promote` (emenda: fixar ≠ promover). |
| `bd13921`/`48a9390` | **F4** — UI da quarentena na Saúde GG; **gate apertado** (`players_left<=FT_CAP` no ramo IT → 99→6); duas secções "Precisam de ti" / "Prontas a aprovar". |
| `3df84df` | **F4 afinações** — imagens no ensaio (`_ft_images`) + **Dispensar** (`POST /ft/dismiss`, só toca a review). |
| `874bd7e` | **F5** — gatilhos de cálculo (`refresh_ft_boundaries` F&F nos imports/reconcile, respeita decisões) + fluxo de aprovação. |

## Política da fronteira (fechada com 5 casos reais)

- **Cascata:** `(0)` tag `-ft` manual → `(a)` lobby Info (`open_tab='Info'`, N=`final_table_size`) →
  `(b)` capturas coerentes → `none`. Fonte (0) vazia NÃO mata (cai na salvaguarda).
- **SNAP-TO-N:** a fronteira computada cai ~1 mão TARDE (o sinal chega 38–51 s depois da FT arrancar);
  o snap recua até 3 min à 1ª mão real (`sentados==N` que inicia a drenagem). Sem mão ==N → fallback.
- **Coerência só na CAUDA PÓS-PICO:** o pico do `players_left` = fecho do late-reg; subida antes do pico
  é vida normal, não incoerência.
- **`#FT-FOLDER-TAG-ORIGIN-LOST` (veredicto):** o circuito NÃO degrada `ICM FT`→`icm` (dá `icm-ft` desde
  1 Jul, ou `None` antes — nunca `icm`). Prova: 28 mãos com `folder_ft_source='manual'` intactas das pastas
  `ICM PKO FT`/`PKO Pos FT`. A fonte (0) é segura; nasce vazia no histórico pré-convenção (import-lag).

## F3/F4/F5

- **F3:** `ft_boundary_review` (tn PK; status/boundary/source/n/seats; decision pending/confirmed/corrected/
  promoted/dismissed; override). Preview LEVE (lista) vs FULL (`?tn=`, com imagens + mãos que mudam + HRC stale).
- **F4:** grupo `ft_quarantine`; secção **"Precisam de ti"** (só decisão: none-com-sinal/mismatch/…; `/summary`
  conta só isto) e **"Prontas a aprovar"** (match limpo; cobertura parcial N=5 à vista). **Dispensar** ("sem FT")
  escreve SÓ na review (guarda testada: mãos/tags intactas); reversível com sinal novo forte.
- **F5:** `refresh_ft_boundaries` fire-and-forget em `import_`/`import_hm3`/`tournament_summaries`/reconcile —
  recomputa e sincroniza a review **respeitando decisões** (promoted não recalcula; confirmed/corrected
  fixados; dismissed só renasce com sinal novo; pending → snapshot). Idempotente; gate apertado → não pesa
  nos imports. Fluxo **Aprovar** → confirm + plano dry-run → **Escrever** explícito → `promoted`.

**Validação do Rui (LIVE):** 4 promovidos (295219051, 293351226, 290775452, 293321688) + 2 dispensados
(290775453, 292235768 = bolha, o Rui rebentou). **F5(b) confirmado read-only:** os 2 dispensados sem sinal
novo (`has_new_ft_signal=False`) → refresh SKIP; os 4 promovidos → SKIP. Nota: a promoção converteu **0 tags**
nos 4 (`n_changes=0` — as mãos de FT na lab data estão sem tag base ou já `-ft`); é o esperado, não bug.

## ★ Teste do invariante dos hashes GG (read-only — evidência para o core, NÃO APROVADO)

**Universo:** 25 torneios GG 2026 com ≥100 mãos — **4198 mãos, 38 488 aparições de hash**.

1. **INVARIANTE POSICIONAL (o mesmo hash = a mesma pessoa, mesmo mudando de mesa): AGUENTA-SE.**
   **ZERO violações** (nenhum hash em 2 mesas à MESMA hora) em 38 488 aparições. **40 hashes seguem o
   jogador entre mesas** (ex.: `4deef1ff` nas mesas 125/5/94, **326 aparições**; `e72a4704` em 17/6/7,
   104 aparições; `a757ec79` em 118/125/94, 176). É a confirmação directa e sistemática da teoria
   (cross-ref `REGISTO_CONCEITO 2026-07-01`, que tinha 1 confirmação). Cobertura: 19 torneios com Gold,
   21 com SS IT.
2. **NOMES da desanon: INCONSISTENTES em 2 dos 4 promovidos — mas é o BUG DA DESANON, não do hash.**
   Separando truncação (mesmo nome cortado) de conflito real: 290775452 limpo (só truncação); 293351226
   **2 hash→2 nomes + 2 nome→2 hashes**; 293321688 **4 hash→2 nomes + 3 poison** (ex.: `de6cc7ae`→
   "Daniel Filipe"/"Mikhail Petrov"; nome "TaWebon"→2 hashes). Como o invariante posicional é sólido,
   estes são a **desanon a atribuir o nome ERRADO ao mesmo hash** (o veneno tipo 90abe28/V Bartko), não
   hashes reusados.
3. **CASO PRÁTICO 293321688:** 8 mãos FT (2 com desanon, 6 Gold-esquecida). **TODAS têm 4/4 hashes já com
   nome no mapa do torneio → 100% resolúveis por propagação pura de hash.** Ressalva: 3 desses hashes têm
   nome CONFLITUOSO no mapa → a propagação dá cobertura total mas propagaria o conflito se não se resolver
   o nome-por-hash primeiro.

**Síntese (factos, sem recomendação):** indexar por **hash** é sólido (âncora melhor que o nome); a
fragilidade é o **NOME por hash** (desanon); a propagação por hash é poderosa (100% cobertura) mas amplifica
o que houver → pré-requisito é **um nome autoritário por hash** (Gold-vence / voto / manual). Bate certo com
a desconfiança do Rui (`APA §B.4`, nível-mão [hash] vs nível-jogador [nome]). **NÃO implementado — decisão do Rui.**

## Infra

- **`#RAILWAY-TOKEN-SHORT-LIVED`** (investigado): access token TTL = 1 h, CLI 4.40.0 inalterado desde Abr,
  1 só config.json; o *refresh* está partido (invalid_grant) — provável mudança server-side recente + agravado
  por chamadas concorrentes. **Fix:** DB reads via `~/.pokerapp_db_ro.env` (fora do repo, só-leitura, rodar as
  credenciais se aparecerem num output); deploys por OpenAPI/liveness (railway CLI dispensado do fluxo).
- Novos tech debts registados: **`#NORAISE-ANCHOR-POSITION-MISLABEL`** (pré-existente, `queue_export`),
  **`#LOBBY-VISION-CHIPS-AS-PRIZES`** (fichas lidas como prémios no print -101).

**Próximo:** F6 (re-solve das HRC stale) dorme (0 solves afectados hoje). O **wipe+reimport** é o teste de
aceitação de toda a frente FT (as tags `-ft` entram a sério quando as mãos de FT chegarem com tags base). A
decisão do CORE (apa-por-hash) fica com o Rui — a evidência está acima.
