# Journal pt41 — 25-26 Maio 2026

**Tema:** 2 fixes HIGH shipped (bounty base via TS + anchor lobby pré-start) + re-disparo
lobby validado + reversão da guarda. Suite **651 → 661 → 666 PASSED**.

SHAs cronológicos: `a942ac7` (fix bounties) → `0707978` (tech debt betting, docs) →
`6409b19` (fix anchor) → redeploy reversão da guarda (mesmo commit, só env var).

---

## 1. Track B testado em produção (smoke real do Rui)

O Rui correu um smoke real do Track B (`#HRC-PER-HAND-DOWNLOAD`, pt40) no HRC:
- Download per-mão de **Bounty Hunters Hyper Special $108** → o HRC importou a HH com
  **bounty €50 correcto** (já com o fix de bounty da pt41) e gerou a árvore (~23k nós).
- **Validação do workaround `€`=`$`:** o Rui confirmou de novo empiricamente que o
  **HRC aceita `€` em HHs GG** (rejeita `$`). A moeda `€` no converter é intencional e
  mantém-se — o valor é a magnitude USD do bounty, o símbolo é `€` (workaround do parser).

## 2. ✅ #HERO-BOUNTY-FROM-TS-DERIVATION (`a942ac7`, suite 651→661)

Bounty base por torneio passa a vir de `tournament_summaries.buy_in_bounty`:
- Hero `max(Vision acumulado, base TS)`; vilões `base TS` (Vision real se SS-matched);
  `€` mantido. Hardcode `_HERO_BOUNTY_DEFAULT_USD=250` **removido**.
- **Gate Andar 1 GG-only** (`hrc_queue.select_andar1_rows`): PKO/SuperKO/KO exigem TS
  com `buy_in_bounty`; **Mystery KO excluído** do /hrc; vanilla sem token de bounty
  (Opção A); Winamax/PS passam (bounty na HH crua, passthrough).
- `lookup_bounties` (espelho de `lookup_payouts`) + `bounty_by_key` em `build_queue_zip`;
  defensiva 422 `pko_without_ts_bounty` no per-mão; manifest audit
  (`hero_bounty`/`hero_bounty_source`/`bounty_format`/`starting_bounty`).
- `GET /api/hrc/pending-ts` + banner D1 no painel `/hrc` (mostra mãos escondidas por
  falta de TS).
- Pré-requisito operacional: o Rui re-importou os 2 TS PKO antigos sem bounty
  (`281400622` $50, `282066003` $40) antes do deploy → 127 mãos PKO passam limpas.

## 3. #HRC-BETTING-SCRIPT-IMPROVEMENTS aberto (`0707978`, docs)

Tech debt 🔴 HIGH (foco pt42) cobrindo 2 aspectos do gerador de `script.js`:
1. **Variante pré-flop + flop only** (turn/river sem betting — só check).
2. **Alternativas estratégicas para o Hero.** Regra-base fixada: *stack effective do
   Hero > 8 BB num spot de open → o script inclui SEMPRE mini-raise 2 BB*, além da
   acção real. Outros sizes/spots ficam para sessão futura.

## 4. ✅ Track A — #LOBBY-ANCHOR-PRESTART-REGRESSION (`6409b19`, suite 661→666)

Resolver **source-aware** (`tournament_resolver.py`):
- Novo `anchor_mode='during_play'` (default) | `'prestart'`. `lobby_sync` passa
  `'prestart'`; `table_ss`/`tournament_results` ficam no default.
- **TIER 0** (`_query_summaries`): selecção **closest** (`ORDER BY abs(start−anchor)`)
  em vez de `start≤anchor DESC`. Janela por modo — prestart `[anchor−12h, anchor+2h]`;
  during_play `[anchor−24h, anchor]` (≡ anterior, table-ss inalterado).
- **TIER 1/2** (`_decide_window`): **ramo-1 forward 2h→4h** (1ª hand importada entra
  ~1-2h depois do start); **ramo-2 source-aware** (prestart → `[posted−12h, posted+2h]`).
- Cobre **TIER 0 + TIER 1/2** com a mesma raiz "anchor assumia SS-durante-jogo".

**Re-frame do âmbito (investigação read-only):** o "anchor pré-start no TIER 1/2" tinha
**impacto quase nulo** — as Winamax (caso TIER 1/2) **têm `start_time_iso`** → usam o
ramo-1 (start-centered, já funcionava) e desbloqueiam-se com **re-run** (hands já
importadas), não com o anchor. O valor real do TIER 1/2 foi **alargar o forward do
ramo-1 (+4h)**. O TIER 0 anchor é que fecha as ~10 GG-com-TS + mata o mis-resolve.

## 5. Re-disparo lobby (real run via painel /discord, janela 30d)

12 candidatos, **7 resolveram, 5 falharam** — todas as 5 são **(a) falta de TS**
(`Daily Hyper $80` edições de **05-09** sem TS importado; a Vision leu tudo bem).

### 🟢 Validação empírica do fix anchor (achado forte)
Para a msg de **05-09 13:38** (`Daily Hyper $80`, sem `start_time_iso`):
- **Código antigo** (`start≤posted`, `[posted−24h, posted]`): o TS de **05-08 16:45**
  (dia anterior) caía na janela → **mis-resolve silencioso** (escreveria
  `tournament_payouts` errado).
- **Código novo** (prestart `[posted−12h, posted+2h]`): exclui o 05-08 → **`tm_not_found`
  honesto**.

O fix **transformou um mis-resolve silencioso num `tm_not_found` correcto** — evidência
empírica directa de que está a funcionar.

## 6. Reversão da guarda

`DISCORD_LOBBY_AUTO`: `false` → **`true`** (`railway variables --set` disparou o
redeploy automaticamente; mesmo commit `6409b19`, env-var only; **SUCCESS** ambas as
instâncias `poker-app`). Handler real-time do `#lobbys` **de novo activo**, agora com o
anchor `prestart`.

## 7. Achado lateral — #LOBBY-SYNC-PAGINATION-LIMIT (🟡 MED, novo)

O probe esperava 34 `tm_not_found` (12-24 Mai); o `sync-recent` com janela 30d só apanhou
**12** candidatos. `gather_candidates` (`lobby_sync.py`) usa `channel.history()` **sem
paginação explícita** → o discord.py assume `limit=100` por defeito; num canal activo com
janela larga, as mensagens mais antigas no log não são puxadas. **O Rui confirmou que
NÃO apaga mensagens no Discord** → não é apagamento, é **limitação técnica** da
paginação. Solução futura: paginação explícita / `limit` maior. Não-bloqueante.

---

## Tech debts

| Debt | Estado |
|---|---|
| `#HERO-BOUNTY-FROM-TS-DERIVATION` | ✅ FIXED (`a942ac7`) |
| `#LOBBY-ANCHOR-PRESTART-REGRESSION` | ✅ FIXED (`6409b19`) |
| `#RESOLVER-TIER12-WINDOW-NO-START` | ✅ FIXED (`6409b19`, via `_decide_window` source-aware) |
| `#HRC-BETTING-SCRIPT-IMPROVEMENTS` | 🔴 HIGH aberto (`0707978`) — foco pt42 |
| `#MYSTERY-KO-DUAL-SUPPORT` | 🟡 MED aberto (`a942ac7`) |
| `#LOBBY-SYNC-PAGINATION-LIMIT` | 🟡 MED aberto (novo) |
| `#META-START-TIME-IS-FIRST-HAND-NOT-SCHEDULED-START` | 🟡 MED aberto |

## Pendente operacional (não-bloqueante)
Importar os TS GG das edições que deram `tm_not_found` (ex. `Daily Hyper $80` 05-09) para
essas SSs resolverem num próximo sync. Workflow normal, não automatizado — mesmo após o
re-import massivo do Rui, edições antigas continuam por importar.

Docs desta sessão: `JOURNAL_2026-05-25-pt41.md` (novo); `TECH_DEBTS_INVENTARIO.md`
(secção pt41); `PENDENTES.md`; `CLAUDE.md` (bloco pt41).
