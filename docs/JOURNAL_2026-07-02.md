# JOURNAL 2026-07-02 — Frentes de QUALIDADE DE DADOS GG (editor Saúde GG Fase 2 + coroas/chama + Gold baralhada)

Snapshot **datado e imutável** da sessão. O estado ACTUAL vive nos docs vivos
(`TECH_DEBTS_INVENTARIO.md`, `PENDENTES.md`, `DESANON_ANATOMIA.md`, `LICOES.md`,
`REGISTO_CONCEITO.md`). Tudo LIVE em produção (Railway backend + frontend verdes). Journal escrito
**em fecho diferido a 7 Jul** (a sessão não foi jornalizada na altura; commits e hashes confirmados
no `git log`).

> **Enquadramento:** continuação directa do arco de qualidade de dados GG que arrancou na **noite
> de 1 Jul** (pt97 já fechara às 19:30 com `0627d67`). O arco 1-Jul-noite→2-Jul→3-Jul-madrugada
> não tinha journal; este ficheiro cobre o **dia 2 Jul** (o 3 Jul, com o pivô de estratégia, vai
> em `JOURNAL_2026-07-03.md`).

## Lead-in — noite de 1 Jul (mesmo arco, contexto)

Matcher IT em cascata + robustez do painel Saúde GG (ainda 1 Jul, pós-journal pt97):
- `d3b5aa8` — miniaturas do Saúde GG carregam sempre (via `API_ROOT`) + lightbox na miniatura.
- `dda4cf5` — **`#IT-MATCHER-CASCADE`**: matcher IT em cascata (número→física→mesa) + Gold-manda +
  tratamento de colisões. `e704223` — `/reconcile` aceita Bearer HRC (dual-auth) para trigger operacional.
- `4c5b541` — "Suspeitas de troca" trata número truncado como **prefixo** (`#IT-MATCHER-CASCADE`).
- `87962b0` — capturas **SECUNDÁRIAS** (duplicados de colisão) deixam de ser contadas como "órfãs"
  (`#IT-MATCHER-COLISOES`).

## O que entrou em PRODUÇÃO em 2 Jul (hashes confirmados no git log)

| Commit | O quê |
|---|---|
| `9b73366` + `f8cac84` | **Ações Fase 2 do Saúde GG (`#GG-HEALTH-ACTIONS`)** — tagar (multi-select), linkar capturas órfãs, e swap-review; backend + frontend (`GGHealth.jsx` + `client.js`). |
| `953868d` | **`#SWAP-ACCEPT-GUARD`** — o botão ACEITAR passa a guardar mesmo quando a mão do número indicado **não existe** (antes falhava em silêncio). |
| `c3aaa85` + `5610c17` | **`#FIELD-BOUNTY-PCT-MISNAMED` — eliminação do `bounty_pct` (a CHAMA laranja = VPIP).** A chama só criava confusão com a coroa (bounty). Removida da página da mão (`c3aaa85`) e do refactor dos vilões (`5610c17`). O bounty real vive em `bounty_value_usd` (a coroa). Ver a armadilha "chama ≠ bounty" no topo do `CLAUDE.md`. |
| `6edf785` | **`#GOLD-BOUNTY-CARRY`** — a desanon Gold por `position_v3` passa a levar **também os bounties** (a coroa segue o nome, não só a cadeira) + backfill das coroas nas mãos Gold já processadas. |
| `d8a3fb5` | **`#DESANON-GOLD-SCRAMBLE`** — re-enrich das mãos Gold **baralhadas** (nomes trocados) + **gate por fichas FINAIS** (só desanonimiza quando as fichas batem no estado final). **34 escritas.** |
| `ce4ff1a` | **`#GOLD-CROWN-CARRY-NAME-TRUNCATION`** — a passagem da coroa por nome fica **tolerante a truncagem/OCR** (o nome na Gold aparece cortado/mal lido). **124 coroas recuperadas.** |
| `95a71ee` | **`#DESANON-HERO-FRIEND-NICK-ACCEPTED`** — os friend-heroes (Karluz, flightrisk) saem da whitelist de **verificação do Hero** (um amigo sentado não deve disparar o alarme de "Hero num vilão"). **2 mãos corrigidas.** |
| `a9b50f1` | **Caixa de pesquisa global por nº de mão** no painel Saúde GG (localizar uma mão directamente pelo `GG-…`). |

## Correcções manuais em produção (via endpoint, sem commit — registo como facto)

- **`GG-6083771298`** consertada à mão — origem do **`#DESANON-SITTING-OUT-NPLUS1-NO-UNIVERSAL-GUARD`**
  (`1c31be5`): um sitting-out (N+1) não tem guarda universal; consertada caso-a-caso.
- **`GG-6116735459`** resolvida à mão — `885023a9` = **Haarlem 91** (`784cfce`).

## Docs / tech debts registados nesta sessão

- `43f4ef9` — **fecho das 5 pendências parqueadas + 2 buracos novos do editor Saúde GG** (em
  `PENDENTES.md` + `TECH_DEBTS_INVENTARIO.md`). Os 2 buracos: (1) **excluir jogador que não jogou**
  do `players_list` (a guarda de suspeitas lê `players_list` → falsa `bounty_below_half`);
  (2) **confirmar coroa <½-base como legítima** (falta flag persistido `bounty_confirmed`). Ambos
  ficam para o **editor por-mão do Saúde GG** (à espera de aprovação do Rui — não codificar antes).
- `1c31be5` / `dd0b773` / `6b3267f` / `7ad38c2` — registos dos tech debts acima em
  `DESANON_ANATOMIA.md`, `LICOES.md`, `TECH_DEBTS_INVENTARIO.md`.

## Nota (fecho diferido)

Os ficheiros de teste do editor Saúde GG só chegam em **3 Jul** (`fa0415e` Fase 1 + `28e5f08` Fase 2);
ver `JOURNAL_2026-07-03.md`. Em 7 Jul detectou-se que esses 2 ficheiros de teste tinham sido
**apagados por acidente** na árvore de trabalho (código-fonte intacto) — **repostos do HEAD e
verificados verdes** (35 testes: 17 do editor + 18 do `ft_boundary`).
