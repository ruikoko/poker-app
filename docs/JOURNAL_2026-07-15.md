# JOURNAL — 15 Julho 2026

Dia grande e de arco único: **auditoria do betting script → LEI DE SIZINGS v3 ditada
do zero pelo Rui → implementação completa (Fases 0-4) → smoke de 5 cenários lançado ao
robot**. Tudo LIVE em `main` (`119713e` → `ac71787`). O Beelink fica a resolver as 5 mãos
durante a noite; o **"VALE" final da lei v3 depende do relatório tree-a-tree de amanhã**.

## ★ 1. Auditoria completa do betting script (o novelo destapado)

Inventário read-only, regra a regra, do que estava ATIVO na construção dos scripts HRC —
método de auditor (ler o template de cima a baixo e DESCOBRIR, não confirmar a lista
conhecida). Resultado consolidado:

- **17 regras ativas** (opens per-posição, §17 array por efetiva, §19 all-in implícito,
  Regra 1 colapso ≤9, Regra 3 shortie PKO, §18.1 blind-open, §18.4 3-bet dinâmico, bónus
  KO, squeeze, 4-bet/5-bet, preserveRealRaise, Max Players §15, offset §16, postflop
  cortado, flatting, applyAllinThreshold, bounty/coroa).
- **13 pedaços de "texto silencioso"** (lógica sem § escrito): `applyAllinThreshold`
  (clamp), `PREFLOP_ADD_ALLIN_SPR=7`, `SQUEEZE_INCREASE_PER_CALL`, `ALLOWED_FLATS_PER_RAISE`,
  config postflop, all-in duplicado nos opens (§19 sem dedupe), fallbacks defensivos,
  count_lines legacy, count_lines SB→30, clamp BB→pot-fraction, "primeira ganha", etc.
- **12 mortos/dormentes** (B1/§18.3 sem call-site, CASO A/B pt42b, mirrors, arrays inertes,
  tetos 40/45, §14 tag-based equity NÃO implementada, FT §16 futuro, `_HERO_BOUNTY_DEFAULT`
  removido, conversão Seat WN pt42c revertida, etc.).
- **9 divergências letra-vs-código** — a mais grave (**D2**): **três definições de "efetiva"
  coexistiam** (§17 remaining+max-opp · Regra 1 total+max-opp · §18.4 total+só-opener), cada
  uma a bater com o seu § mas divergentes entre si → fonte de surpresas. Também: squeeze
  "arrays fixos" (docs) vs override+incremento (código); §15 span vs participantes (bug
  conhecido); B1 documentado mas dormente.

Esta auditoria foi a base para o Rui **reescrever a lei do zero**.

## ★ 2. LEI DE SIZINGS v3 — ditada de raiz pelo Rui (`docs/LEI_SIZINGS_2026-07-15-v3.md`)

Carimbada na íntegra (linha a linha). Quadro completo:

- **Regra de Ouro:** o size real da HH substitui o default na posição respetiva.
- **Régua única da efetiva (mata o D2):** `eff = min(REMAINING de quem age, REMAINING do
  adversário do confronto)` — open/squeeze vs o campo (maior remaining vivo); 3-bet/4-bet vs
  o agressor anterior; squeeze = o MAIOR entre opener e callers. **Bordas ao escalão de baixo.**
- **Opens:** base 2bb (IP) / tabelas SBvsBB e BB-vs-limp por eff; **colapso** (IP ≤9, blinds
  <10) → jam; **linha de all-in por EFETIVA** (IP≤25 · SB≤30 · BB≤25) — morre o "individual".
- **3-bet:** 4 BLOCOS (IP/SB/BB/SBvsBB) × open por escalão; `[size,ALLIN]`; abaixo=jam;
  **bónus KO +0.5 só no 3-bet.**
- **4-bet:** × 3-bet (to-amount), 4 blocos, **all-in incondicional** (SPR≤7 morre); abaixo=allin.
- **Squeeze:** tabela nova **IP/OOP** × open; <20 jam; **+1bb por caller morre.**
- **5-bet:** mantém 0.4/0.5 pot. **Sobre open-allin:** 3-bet comum + CALL garantido (B1 enterra-se).
- **MORREM:** R6 (blind-open §18.1) · B1 · SPR7 do 4-bet · +1bb do squeeze · as 3 réguas velhas
  · CASO A/B · arrays/helpers inertes · tetos 40/45 · buckets antigos.

## ★ 3. Implementação Fases 0-4 (backend-only; `.exe` intacto; fila fechada)

- **F0+1** — régua única `effVsFieldBB` + opens v3 + `count_lines`/offset em **lockstep** +
  **dedupe de all-in por construção**. Portão 1 **carimbado** (opens+offset, 6 trees de prova).
- **F2** — 3-bet 4 blocos runtime + bónus KO + Regra 3.
- **F3** — 4-bet ×3-bet (all-in incondicional, SPR7 morto) + squeeze IP/OOP (+1bb morto) + 5-bet.
- **F4** — limpeza JS do sizing legacy (**net −135 linhas**). Helpers Python mortos ficam
  **inertes** (`build_sizings_overrides`→`{}`), removê-los é passagem dedicada (~40 testes).

Suite **1450 passed** (8 falhas DB-local, **0 AssertionError**, zero não-DB). Mirrors Python
v3 (test-only) para 3bet/4bet/squeeze. Docs: LEI v3 (novo) + banner "SUPERSEDED" em
`REGRAS_NEGOCIO §17`.

**Fio 9-10 nas blinds:** decisão do Rui — opção (b), a letra manda (blinds <10 jam; IP >9=2bb).

## ★ 4. Smoke de 5 cenários lançado ao robot

5 mãos **distintas, elegíveis e que exportam 200 OK** (verificado com `build_queue_zip`
read-only — `/eligible` ≠ exportável; 2 GG iniciais caíam em `bounty_above_3x`), 1 por
cenário: **colapso · open-c/-allin · 3-bet (GG PKO, testa KO) · 4-bet · squeeze**. Entregues
por **link direto** (`<frontend>/api/queue/hrc/hand/<id>` = ⬇ HRC num clique).

- **★ 1 aborto apanhado pela coluna da versão:** o Rui clicou antes do deploy → zips com
  etiqueta **`2026-07-11-3bet-v2`** (lei velha). Causa dupla: (a) timing do deploy; (b) **eu
  não tinha bumpado a `SIZING_RULES_VERSION`** — os zips sairiam com label errado mesmo pós-deploy.
  Fix `ac71787`: bump → `2026-07-15-sizings-v3` + exposto em `GET /` (público) para confirmar o
  deploy. **Deploy v3 confirmado ATIVO** (root: `sizing_rules=2026-07-15-sizings-v3`).
- **Cleanup dos v2:** nada a limpar no backend — `hrc_queue_release`=0 e `hrc_jobs`=0 para as
  5 (o download per-mão não cria registo de "enviada"; os v2 eram só ficheiros locais).

**O robot fica com a noite** — 5 mãos a resolver no Beelink.

## ★ 5. Caso `/hand/6570` + censo do veneno silencioso

Chamas pré-guardas curadas por re-leitura (verificação do Rui). **Pedido: censo das coroas
pré-pt95 plausíveis** (resposta em dívida) — quantas leituras de coroa antigas podem estar
silenciosamente erradas sem cair na quarentena. Fica para amanhã.

## Regra permanente registada
**Mãos para o Rui = SEMPRE por link clicável ou nº como aparece na app; nunca fragmentos
internos do hand_id.** Verificar export 200 antes (eligible ≠ exportável). Painel HRC 370
linhas **sem pesquisa** → Ctrl+F falha. Memória `feedback_hand_refs_by_link`.

## ⚠️ Erro do dia (registado)
Ao inspecionar o env imprimi o **URL read-only da BD** (com password) num comando. É
só-leitura, mas **o Rui deve rodar o `~/.pokerapp_db_ro.env`** por precaução. Não repetir
(memória `reference_railway_cli_auth`: "nunca imprimir/rodar se vazar").

## Commits do dia
```
2c056f5  feat(hrc-sizings): LEI v3 — régua única + opens/3bet/4bet/squeeze runtime
ac71787  chore(hrc): bump SIZING_RULES_VERSION -> 2026-07-15-sizings-v3 + expõe no root
```
(+ o commit deste fecho: JOURNAL + PENDENTES.)
