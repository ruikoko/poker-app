# Sessão pt42d — 28 Maio 2026

**Foco:** `#WN-BOUNTY-NULL-IN-HRC-PIPELINE` **v2** — pt42c v1 entregou um
fix parcial (Seat lines WN→PS + patch bountyType no payouts) mas smoke
real expôs que o HRC continuava a fazer ICM puro (Instant=0%) apesar do
`bountyType="PKO"` correcto no payouts.json. Investigação revelou causa
raiz dupla: (a) 4 hints top-level extra no payouts.json (HRC rejeita
campos extra); (b) `structures[i].name` sem sufixo `"  #<tn>"` (colisão
na biblioteca persistente HRC).

**Suite:** 730 (fim pt42c) → **734 PASSED** (+5 T1, −3 T2, +2 T3, +1 T4+T5;
9 modificados T4+T5; 1 modificado T2). 0 regressões.

**Estado de entrega:** backend + watcher source + adapter aplicados a
disco; `.exe` recompilado (SHA `cdfc7247...3262`). Smoke real pendente
(uvicorn local + cópia Beelink). T12 commit/push aguarda smoke OK do Rui.

---

## Descoberta empírica (pré-T1)

1. **Smoke real pt42c (manhã 28-Mai):** Rui descarregou zip WN PKO
   pós-deploy `b2a2116` (pt42c) → `payouts.json` patched com
   `bountyType: "PKO"` + `progressiveFactor: 0.5`. Importou no HRC →
   **Instant=0% (ICM puro)**. Inconsistência inesperada.
2. **Diff Web read-only** entre `payouts.json` GG funcional (Bounty
   Hunters Special $150 dos Downloads) e o WN pt42c → identificou 4
   hints top-level extra no WN (`equity_model`, `max_players`,
   `script_path`, `aggressor_real_action`).
3. **Investigação profunda do filesystem HRC** revelou biblioteca
   persistente em `%USERPROFILE%\HoldemResources\.metadata\.plugins\
   net.holdemresources.calculator\structuredata\custom.json` — 55
   structures, **23 com `bountyType="PKO"`, 0 com `"None"`, 32 sem o
   campo de todo**. A entry `GRAVITY #1101080235` (tournament da mão
   smoke) estava lá **2x duplicada, ambas sem bountyType**.
4. **Re-validação empírica do formato HRC-aceite** (Rui inspeccionou
   Bounty Hunters Special em Downloads):
   - Top-level: APENAS `name`, `folders`, `structures`.
   - `structures[0].name`: `"<TournamentName>  #<tournament_number>"`
     (2 espaços + `#tn`).
   - `structures[0].chips`: number bruto (float OK).
   - `structures[0].prizes`: dict com NÚMEROS brutos (não strings).
   - `structures[0].bountyType`: `"PKO"`.
   - `structures[0].progressiveFactor`: `0.5`.

## Decisões product (Web/Rui)

### Path B (sessão completa)

Backend + watcher source + adapter recompilados na mesma sessão. Rui
prefere evitar a degradação temporária do robot (2ª run desactivada se
watcher só lesse `payouts.json` sem hints).

### 6 decisões técnicas confirmadas

1. `meta.json` como destino dos 4 hints.
2. Reverter Seat lines conversion WN→PS (T2 pt42c) — HRC lê formato WN
   nativo.
3. Manter `_extract_winamax_seat_bounties` + `compute_hero_bounty_from_hh`
   para audit no manifest.
4. `#WATCHER-SOURCE-READ-HINTS-FROM-META` NÃO aberto — resolvido nesta
   sessão.
5. Progressive factor 0.5 hardcoded mantido (Rui confirma WN PKO 50%
   universal — válido desde pt42c).
6. Prizes/chips passam como vêm do parsing (sem helpers de formato
   string 2dp ou chips int — JSON HRC Ninja aceita números brutos).

---

## Implementação T1-T10

### T1 — `_format_winamax_structure_name` + 5 testes unit

`queue_export.py`: helper `_format_winamax_structure_name(name, tn) →
"<Name>  #<tn>"` (**2 espaços** literais). Defensivo: name None → None;
tn falsy → name original. 5 tests (basic, two_spaces_not_one, missing_tn,
None_name, special_chars). Suite 730 → **735 PASSED**.

### T2 — Reverter Seat lines conversion + branch WN PKO

`queue_export.py`: remover `_inject_bounties_winamax_to_ps_format`
(~35 linhas) + branch WN PKO em `convert_gg_hh_to_pokerstars_compatible`
→ passthrough total para non-GG. Docstring actualizada.
`test_queue_export.py`: −4 tests T2 pt42c; 1 test convert WN PKO
renomeado/invertido para passthrough. Suite 735 → **731 PASSED**.

### T3 — `_patch_winamax_payouts_bountytype` aceita `tournament_number`

Kwarg-only com default None (backward-compat). Aplica
`_format_winamax_structure_name` ao `structures[i].name` quando passado.
Docstring expandida com causa raiz (colisão na biblioteca HRC). +2 tests
(with_tn / without_tn). Suite 731 → **733 PASSED**.

### T4+T5 fundidos — payouts.json limpo + meta.json com hints

**Decisão (b):** fundir atomicamente para manter suite verde.

`queue_export.py:build_queue_zip`:
- `payouts.json` no zip = **APENAS** `{name, folders, structures}`
  (sem merge com hints). Patch passa `tournament_number=tnum`.
- Defensivo se sem blob: `{name: "/", folders: [], structures: []}`.

`queue_export.py:_build_hand_meta`:
- +3 kwargs novos: `max_players`, `script_path`, `aggressor_real_action`
  (4º hint `equity_model` já existia como posicional).
- Schema actualizado.
- Docstring expandida.

`build_queue_zip` caller passa os 3 kwargs novos via `hints.get(...)` +
variável local `aggressor_real_action`.

`test_queue_export.py`: **10 tests modificados** (9 que asseram hints em
payouts → agora em meta + 1 e2e WN PKO actualizado para novo formato
incluindo `name == "INTERSTELLAR  #999111"`); 2 tests renomeados
(`_hints_in_meta_not_payouts`, `_in_manifest_and_meta`); **+1 test novo**
(`payouts_json_in_zip_has_only_three_keys`). Suite 733 → **734 PASSED**.

### T6 — `hrc_queue.py` comentário pt42d

Comentário Andar 1 reescrito reflectindo nova arquitectura (HH WN
passthrough; payouts.json HRC-native; hints em meta.json). Sem alteração
ao SQL. **Bug encontrado e fixed:** chavetas `{name, folders, structures}`
no comentário foram interpretadas como f-string var expansion (NameError);
escapadas com `{{...}}`. Suite mantém 734 PASSED.

### T7 — Marco de confirmação backend final

Suite **734 PASSED, 0 FAIL**. Backend em estado estável. A partir deste
ponto, alterações ao watcher (T8) e adapter (T9) são conceptualmente
separadas.

### T8 — Watcher source: 4× `_payouts.get → hand_meta.get`

`tools/watcher_src/patched_funcs.py`:
- Linhas 1786, 1787, 1790, 1984: substituição. Defaults preservados
  (`'multi_table_icm'`, `None` fallbacks downstream).
- **Opção (b) limpeza:** load `_payouts` órfão removido (linhas
  1776-1791 reescritas). `_payouts` ficaria como dead code; melhor
  remover.
- −15 / +9 linhas líquidas. Suite mantém 734 PASSED (tests do watcher
  mocká tudo; sem dependência em `_payouts`).

### T9 — Adapter: `rewrite_script_path_in_payouts → _in_meta`

`tools/hrc_adapter/payouts_helpers.py`: função renomeada; target file =
meta.json; docstring + comment pt42d.
`tools/hrc_adapter/hrc_adapter.py`: import + 4 substituições no caller.
`backend/tests/test_hrc_adapter_helpers.py`: 1 import + 5 renames + sample
keys actualizadas (de payouts → meta).

Suite mantém 734 PASSED.

### T10 — Recompilação `.exe` do watcher

Build flow pt35 base:
1. `swap_and_smoke.py` → trampoline `.pyc` swapped em `repacked/`
   (142 817 B). **Smoke harness in-process partiu** em sub-test que
   exercita `_wait_for_finish_ready` (mock Win32 ausente) — pre-existente
   pt30. Tech debt `#SMOKE-HARNESS-WAIT-FOR-FINISH-MOCK-MISSING` (LOW)
   aberto. Não-bloqueante.
2. PyInstaller via venv local: `_local_only/watcher_decompile/build_pyi/`
   → `dist/hrc_watcher.exe`. 1 warning não-crítico
   (`_append_data_to_exe` retry attempt #1 PermissionError → attempt #2
   OK; antivirus).

**SHA256 novo:** `cdfc7247711614fd775e808e2cb0d3abfbd80afdda11c3223bec58fb37363262`
**SHA256 pt35:** `33eae43aa0e4ab0f331b880ee217efe6d52369b4190cc07fb3be7fb647c53c4f`
**Tamanho:** ~13 MB (similar a pt35).
**Path:** `_local_only/watcher_decompile/build_pyi/dist/hrc_watcher.exe`.

### T11 — Docs (esta sessão)

6 docs: TECH_DEBTS (secção pt42d + 2 entries), JOURNAL (este),
HRC_ANATOMIA §12.10 (reescrita), MAPA_ACOPLAMENTO (referências),
PENDENTES (foco), CLAUDE (anchor + secção).

### T12 — Commit + push (após smoke real OK)

Smoke real entre T11 e T12: Rui corre backend local via uvicorn,
descarrega zip pós-pt42d, copia `.exe` novo + `payouts_helpers.py`
para o Beelink, valida no HRC (Instant=50%) + corre robot (1ª + 2ª run).

---

## Verificação local

```
$ python -m pytest --tb=no -q
734 passed, 3 warnings in 79.51s
```

Sem regressões. Sem warnings novos.

---

## Não feito (smoke real pt42d)

- **uvicorn local** (`cd backend && uvicorn app.main:app --port 8000`)
  para o Rui descarregar zip pós-pt42d.
- **Cópia para o Beelink:**
  1. `_local_only/watcher_decompile/build_pyi/dist/hrc_watcher.exe` (SHA
     `cdfc7247...3262`).
  2. `tools/hrc_adapter/payouts_helpers.py` (T9).
- **Validação no HRC:**
  - `payouts.json` no zip = `{name, folders, structures}` only.
  - `structures[0].name` = `"<TournamentName>  #<tn>"`.
  - HRC mostra Instant=50% (não ICM puro).
- **Validação no robot Beelink:**
  - 1ª run com hints em meta.json (`equity_model`, `max_players`,
    `script_path`).
  - 2ª run dispatcha (gate `aggressor_real_action` em meta.json).

---

## Tech debts

- **`#WN-BOUNTY-NULL-IN-HRC-PIPELINE` v2** ✅ FIXED RESOLVIDO. Detalhe em
  `docs/TECH_DEBTS_INVENTARIO.md` secção pt42d.
- **`#SMOKE-HARNESS-WAIT-FOR-FINISH-MOCK-MISSING`** (LOW, novo pt42d) —
  smoke harness in-process partido em sub-test `setup_hand`.
  Não-bloqueante.
- **`#POSITION-LABELS-PYTHON-JS-DRIFT`** (LOW, pt42b) — sem alteração.
- **`#OPEN-COUNT-DRIFT-HRC-NODE-OFFSET-LATENT`** (LOW, pt42) — sem
  alteração.
