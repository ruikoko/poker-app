# JOURNAL — 13 Julho 2026

Sessão longa e contínua (madrugada, na sequência do fecho de 12 Jul). **Batismo da app
nova PASSOU** — a leva ao-vivo de hoje entrou e fechou 100% — mais três curas de core
(reconciles robustos, guarda de stacks, detetor de rotação), o painel "Golds por ler", e a
recuperação das 78 Golds nunca-lidas. Tudo LIVE em `main` (`f8ce806` … `751cd3c`).

## ★ Leva de hoje — importada e 100% fechada

**88 (import ao-vivo) + 76 (HH WN via HM3):**
- **703 mãos GG** (1 HH zip, 18 torneios) + **76 mãos WN** (via `.bat` do HM3: EXPLORER 6 ·
  GRAVITY 35 · INTERSTELLAR 32 · ODYSSEY 3).
- **table-SS 51/51 casadas** (após reconcile: 21→51; os 30 no_match eram WN à espera da HH).
- **Lobbys 17/17 resolvidos** (7 GG diretos + 10 WN após reconcile; edições coladas, 0 quarentena).
- **16 Golds premium** — todas `position_v3`, `vision_done=true`, **0 no vão**, 0 anónimas.
- **Tags por pasta 21/21** aplicadas (icm-pko 6 · pos-pko 6 · nota 5 · icm 3 · pos-nko 1),
  **0 Marcadas/captura novas** (raiz sem tag = 0).

## ★ Batismo da app nova — PASSOU

O pipeline novo (prompt das placas de $ reescrito, guardas base÷2, âncora com verdade
conhecida, tags-por-pasta) processou a leva ao-vivo **sem intervenção** e sem regressões: as
coroas leram a placa (não a chama), a desanon ancorou no Rui, as tags entraram pela pasta.

## ★ Cura `#HM3-IMPORT-NO-RECONCILE-REDISPATCH` (`610284e`)

**Achado (Rui):** a HH WN de hoje deixou os 30 prints de mesa em `no_match` e os 10 lobbys
em `tm_not_found` até se correr o reconcile **à mão**. Raiz: o `import_hm3` (e o GG zip)
disparavam os dois reconciles via **`asyncio.create_task` separados** — padrão frágil que se
**perde** em imports sync-pesados / timeout do request. Cura: helper único
`trigger_import_reconciles()` (`lobby_sync`) num **daemon thread robusto** (sobrevive ao
request, não bloqueia o event loop; mesmo padrão de `trigger_name_propagation`/TS-tardio),
usado em `import_hm3` **e** `import_` (GG zip). +2 testes.

## ★ Detetor de rotação — corrigido (`10b42b6` → `751cd3c`)

Nascido para os 6 conflitos de nomes que eram **1 captura podre** (tn 292179612, ctx 782:
a âncora rodou a roda → cada seat recebeu o nick do vizinho; os stacks desmentiram-na 8/8).
Curas base (`b726ee6`): **guarda de stacks** no de-anon (`build_anon_map_by_hero_button` →
alarme `stack_map_mismatch`; teria apanhado a 782) + **prune de quarentena stale** (a captura
revertida faz cair os N conflitos). Detetor no painel (`10b42b6`): `/names/rotation-scan` →
"Reverter captura podre" (1 ação, não N × "confirmar o forte").

**Falso positivo em massa (Rui):** o detetor contava nome **truncado** ('Tobias Schwecht'→
'Tobias Schw..') como troca de vizinho → **7 de 9 "podres" eram truncação**. Fix (`751cd3c`):
`_same_name_trunc` (prefixo, mín 4 chars) **antes** de acusar; rotação exige nomes DIFERENTES
entre si. **Scan pós-fix: 9 → 0.** **0 reversões** feitas.

## ★ Triagem de nomes do Rui (verificação por stacks — método Pedro Borges)

- **R Romanovsky** confirmado (GG-6108969587): os stacks batem (84.3≈84.5bb) → mesmo lugar,
  NÃO rotação; o forte (`position_v3`) ganha o desacordo de nome no mesmo seat.
- **Gooddecision91** confirmado.
- **SuchAGooodBoy** [se confirmado pelo Rui].
- **Caso `Andre Figue..` ABERTO** — duas variantes TRUNCADAS do mesmo nome; o stack confirma
  o lugar mas não escolhe a grafia → **melhoria de UI pedida** (opção "nome manual / fundir
  variantes truncadas").

## ★ Golds nunca-lidas + painel "Golds por ler" (`43d0fd4`, `8a90e2f`)

**Raiz:** a Vision de background do upload (`background_tasks.add_task`) falhou/nunca disparou
(créditos/rate/timeout) → **78 Golds** ficaram `vision_done=false` sem recuperação (worker
normal só `img_b64 IS NULL`; revision-replayers só `replayer_link`). Endpoint novo
`gold-vision-run` (1ª leitura por hand_id) → **78/78 recuperadas 100%** via `position_v3`.
Cura no core: **painel "Golds por ler"** (cartão em "Precisa de ti" + "Ler agora"/"Ler todas")
— a app passa a **encontrar o padrão sozinha**. `_gold_rows` enriquecido; **"Gold sem tag"**
ganhou metadados por linha + filtros (formato/Speed Racer/torneio/data) + seleção-em-lote
(`8caa625`). Endpoint+botão **remove-tag** (`a80bcc7`) para limpar tags espúrias.

## ★ 52 Speed Racer etiquetadas em lote

Fases verificadas (players_left da SS ÷ total do TS), **0 FT**. Tabelas de fase entregues
(12 + 52 mãos). Lista das 315 "Gold sem tag" atuais exportada (`_local_only`).

## ★ Censo 26/06–02/07 (varrido pelo Rui)

Linha do tempo única de todas as imagens (fusos convertidos e declarados). Rui varreu à vista:
**4 pares SS recasados** (SS na mão vizinha errada → mão da Gold, via `resolve-owner`;
`8120b537 = Pedro Borges` etc.), **2 falsos** (matches certos, não tocar), **78 Golds
nunca-lidas recuperadas 100%**.

## ★ Hero-alheio (3→0) + ferramentas (`f8ce806`, `dba2024`)

Ângulo cego do swap: `pn.hero ∉ HERO_NAMES_ALL` (o nome do Rui AUSENTE, não num vilão).
Scan novo = 3 (todas pré-guarda 11 Jul); 2 cosméticas (apa já Lauro), 1 veneno (515, revert).
**Guarda 4** da âncora (`#DESANON-ANCHOR-REQUIRES-HERO-IN-IMAGE`: exige o Rui nos seats) +
veneno 3 no painel Suspeitas + endpoint `revert-to-anon`. **Marcadas 432** respondido (origem
das tags: todas raiz/não-ordenado, 0 falha de propagação).

## Commits do dia (main)
`f8ce806` guarda âncora + veneno 3 + revert-to-anon → `dba2024` docs Marcadas/hero-alheio →
`a80bcc7` remove-tag → `8a90e2f` gold-vision-run → `bc4b425` LICOES retração → `43d0fd4`
painel Golds por ler → `8caa625` Gold sem tag filtros → `b726ee6` guarda stacks + prune →
`10b42b6` detetor rotação → `610284e` reconciles daemon robusto → `751cd3c` detetor tolerante
a truncação.
