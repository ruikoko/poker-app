# JOURNAL pt75 — 17-18 Junho 2026

Desanon GG por **POSIÇÃO** (`position_v3`) construída, provada e deployed; lane de
ingestão das **gold images** por pasta + dedup `file_hash`; fix do re-link (a ordem
deixou de importar). 5 PRs em main, todos deployed.

## A. Arco

1. **Prova nas 41 gold images de Junho** (Documents, read-only). A GG **não desenha
   rótulo de posição por seat** — as siglas (UTG/MP/CO/BTN/SB/BB) vivem no **LOG DE
   ACÇÃO** da gold image (descarga completa da mão pelo botão do replayer). O 1º teste
   apontou a Vision ao seat (errado) → inferência. Corrigido o alvo para o log →
   crachás lidos em 41/41. Âncoras SB/BB **81/81**; consistência entre mãos **54/55**
   (1 OCR, 0 inconsistências); 0 nomes errados. Os "erros" do oráculo eram o **painel
   SB/BB mal lido ~10/19** (o crachá é mais fiável que o painel — que é a âncora do
   stack antigo).

2. **`position_v3` (PR #2 `d8ee3c4`)** — `_build_gg_vision_prompt`/`_parse_vision_response`
   lêem a sigla do log; `_build_anon_to_real_map_by_position` + `_canon_position`
   (inclui LJ); verificação do Hero (`hero_unverified` — nunca pôr vilão no seat do
   Hero); guard-rails de lacuna honesta (sigla em falta/colisão/não-participante → seat
   por mapear). Branch por-mão no enrich; fallback stack para entries sem sigla.

3. **Lane gold + dedup (PR #3 `2c575db`)** — `tools/appimport` lê `GOLD_DIR` externa
   (Documents) em `process_gold_dir` → POST `/api/screenshots` → `done/gold` no 2xx, sem
   filtro de mês. **Dedup server-side `file_hash`** em `/api/screenshots` (2ª importação
   → `duplicate`, sem entry/Vision). A jusante reusa Vision→match `GG-{tm}`→enrich.

4. **Smoke ao vivo** (deploy+dedup ✅; com-HH→`position_v3` nomes certos ✅; órfã sem
   HH→placeholder sem erro ✅) destapou o **gap do re-link**: imagem-primeiro→HH-depois
   dava **stack**, não `position_v3`.

5. **Fix do re-link.** Achado: o re-link imagem-primeiro **não** passa pelo
   `_enrich_hand_from_orphan_entry` — passa pela **promoção do placeholder em
   `hand_service._insert_hand`** (quando a HH é importada), que fazia desanon por stack.
   - PR #4 `895ed90` — `_enrich_hand_from_orphan_entry` recupera posições do entry
     guardado (cobre o caminho de rematch).
   - PR #5 `3722c13` — **`hand_service._insert_hand`** (o caminho real) usa a branch
     por-mão → `position_v3`.
   - Smoke verde nos **dois sentidos** (GG-6083680253/532: `position_v3`, nomes a
     baterem com a gold image). **A ordem deixou de importar.**

## B. Estado

- `position_v3` = método **primário** das gold images GG; stack-elimination = fallback
  (entries sem sigla) + continua nas **185 table-SS**.
- Suite **992 passed** (3 falhas pré-existentes = Postgres local). Testes novos:
  `test_gold_lane` (5), `test_screenshot_dedup` (3), `test_position_v3_enrich_branch`
  (3), `test_relink_insert_hand_position` (2).

## C. Notas / dívidas (em TECH_DEBTS pt75)

`#POSITION-V3-TWO-RELINK-PATHS` (2 sítios em sync), `#CANON-POSITION-LJ-NO-HH-SEAT`
(latente), `#STACK-LEGACY-TABLE-SS-AND-PANEL` (185 table-SS + painel SB/BB mal lido).
PENDENTES: cobertura de HH Março/Junho do backoffice (operacional — desanon só corre
quando a HH existir).

## D. Achados de investigação (read-only)

- As **89 de Março** em Documents são **MTT à espera de HH** (não cash) — `#TM` não
  existe em `gg_hh` nem na app; blinds do nome falsas.
- 0 hand-ids das gold images (Março/Junho) existem na prod (mãos GG vão até 06-15).

## Processo

Operações na prod (smoke ao vivo: imports de HH de teste, gold images, placeholders)
**não deixam commit** — o Rui apaga e reimporta a seguir; artefactos do smoke
desaparecem no reset.
