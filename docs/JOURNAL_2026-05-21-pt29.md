# Continuidade — Sessão pt29 (poker app)

> Documento criado para retomar o trabalho num novo chat depois de atingir o limite de 100 ficheiros no anterior.
> Data: 2026-05-21, ~03h.

---

## Estado actual (snapshot)

- **pt29-v3 ACABOU DE SER INSTALADO no Beelink.** Smoke por arrancar (Rui ia fotografar o resultado quando o chat encheu).
- **SHA do exe pt29-v3:** `A9599F931F052C6894DEC4DD893D83C42F86FDBF714C469E7C4C3A8D231B6756`
- **Último commit:** `3b9d72c` em main, pushed.
- **Mão de teste:** `GG-5944816316` (97-H Bounty Hunters Daily Main, 8-max, Hero BU 21.8BB, CO Pinduca77 102.7BB, 365 players left).

---

## Próximo passo IMEDIATO

Aguardar fotos do smoke pt29-v3 que o Rui vai mandar quando reabrir o tema. Olhar para:
- Tempos X e Y de cada run no log `[CALC] Terminado em Xs (mem=...MB)`.
- Se Save As funcionou no fim.
- Se houve algum WARN inesperado.

---

## Cascata de bugs resolvidos esta sessão

### pt29-v1 (commit `1b5b388`)
- **Diagnóstico:** robot prosseguia em silêncio após Finish falhar.
- **Fix:** state check pós-click via título "Hand Setup" da janela (WARN-only); activate pré-Finish; logging defensivo.
- **Resultado:** confirmou que activate (foco) funcionava mas wizard persistia mesmo assim. Hipótese de foco descartada.

### pt29-v2 (commit `cb4c520`)
- **Diagnóstico:** HRC (Java) perdia eventos de `pyautogui.click()` instantâneo (mouse-down+up em <50ms).
- **Fix:** slow-click — `mouseDown()` + `sleep(0.15s)` + `mouseUp()`.
- **Resultado:** VALIDADO — Finish funciona, wizard fecha.

### pt29-v3 (commit `3b9d72c`) — INSTALADO, à espera de smoke
- **Diagnóstico:** `start_calculation` apenas dispara a run e retorna ~1s depois, NÃO espera terminar. A 2ª run começava antes da 1ª acabar. O Save As tentava abrir antes da 2ª terminar.
- **Fix:** `wait_for_calculation()` (Baltazar OG, já existia no namespace mas inutilizada) em 2 pontos:
  - Após 1ª run, antes de navigate/2ª run — **incondicional**.
  - Após 2ª run, antes do export/Save As — **condicionado a `second_run_dispatched is True`** (se a 2ª run não disparou, não há que esperar).
- **Heurística do `wait_for_calculation`:** memória do HRC estável (`>100 MB` e variação `<20 MB`) por 3 ciclos de 10s, sleep inicial 15s, timeout total 300s. Não é sinal directo do HRC, é inferência.

---

## Próximos passos para amanhã (ordenados por prioridade)

### Alta prioridade

1. **Receber resultado do smoke pt29-v3** — Rui vai fotografar quando estiver acordado. Esperar.
2. **Em paralelo (não bloqueia o smoke), confirmar com o Code que os 3 docs do repo foram committados:**
   - `docs/HRC_ANATOMIA_OPERACIONAL.md` v4 (substitui v3).
   - Journal entry de pt29 anexada onde quer que esteja o journal canónico do repo.
   - Tech debts actualizados (entries adicionadas/resolvidas — ver briefing).
3. **Promover `script_da_config_UI.js` como template canónico do repo** — substituir `backend/app/services/hrc_scripts/mtt_advanced_canonical_2026.js`. Briefing trivial ao Code. Este script foi validado no PC principal a dar **6 GB / 786620 nodes** para a mão de teste com Hand Mode Max 6.

### Média prioridade (se o smoke pt29-v3 der bem)

4. **Limpeza dos .exe watcher antigos no Beelink** — Rui acumulou 4 (.pt28v3.bak, .pt29v1.bak, .pt29v2.bak, actual) e queria limpar.
5. **Investigar e fechar `#HRC-BOUNTY-HARDCODED-50PCT`** — novo tech debt registado: o robot Baltazar OG tem PKO 50% hardcoded; tem de ler do `tournament_format` parsed do TS para suportar PKO 25% e Mystery KO.
6. **2ª entrada Discord para Tournament Markers duplicados** — bug aberto há sessões: canal 2 não é adicionado a `discord_tags`, regra C de villain não dispara.
7. **Uniformização tags Discord/HM3** — URGENTE para o Estudo. 3 opções: renomear canais, dict hardcoded, ou admin UI central de tags. Decisão pendente.

### Baixa prioridade

8. **GTw → pos-nko backfill** (25 rows, dry-run pendente desde pt19).
9. **Calibração de coords restantes do robot** — `SCRIPTING_TAB`, `SCRIPT_FOLDER`, `BTN_NEXT`, `OK button no popup Nash` continuam como LACUNA na §11 do doc HRC. BTN_FINISH foi preenchido em pt29.
10. **Afinar thresholds do `wait_for_calculation`** se aparecerem casos de declaração prematura ou esperas exageradas — só com dados reais de vários smokes diferentes.

### Issues técnicas abertas (de sessões anteriores)

- `#HERO-BOUNTY-FROM-TS-DERIVATION`
- `#HRC-180-ENTRIES-PAYOUT-LIMIT`
- `#HRC-CONTEXT-MISMATCH-PLAYERS-LEFT`
- `#HRC-BOUNTY-HARDCODED-50PCT` (novo, registado nesta sessão)

---

## Mecânica de entrega de exes ao Rui (importante para o próximo Claude Web)

1. Code constrói o exe em `_local_only/watcher_decompile/build_pyi/dist/hrc_watcher.exe` no PC principal do Rui (`_local_only` está no `.gitignore`, logo o exe **não está no repo público do GitHub** — não tentar pedir ao Rui para descarregar de lá).
2. Rui faz `git pull` no PC principal — o exe aparece localmente porque o Code o construiu lá.
3. Rui transfere o exe do PC principal para a pasta `Downloads` do Beelink (método "alma de Deus" — qualquer canal funciona: USB, OneDrive, partilha de rede, etc.).
4. Web fornece `instala_ptXX.bat` ao Rui via outputs deste chat. Rui descarrega para o **Desktop** do Beelink.
5. Rui dá duplo-clique no `.bat` → SHA-check + backup do exe antigo + instalação automática.
6. Rui dá duplo-clique no `hrc_watcher.exe` do Desktop para arrancar o smoke.

**Template da `.bat`:** consultar a `instala_pt29v3.bat` que já está em outputs. Para nova versão, basta trocar a SHA esperada e o nome do backup.

---

## Regras de comunicação (manter sempre)

- **PT-PT plain.** Jargão técnico traduzido na primeira ocorrência por resposta ("UI" → "ecrã da app", "endpoint" → "rota no servidor", "deploy" → "publicar").
- **Nunca especular** — toda a afirmação factual verificada antes de a fazer, ou marcada como hipótese.
- **Admitir erros sem rastejar.** O Rui está cansado, fricções acontecem — reconhecer, não inflar.
- **Curto e directo.** Sem secções formais em conversas operacionais. Sem commit hashes na prosa a não ser que o Rui precise.
- **Briefings ao Code sempre em bloco markdown** claramente identificado.

## Estado do Rui à hora deste documento

Sessão começou ~19h do dia anterior, ele continua a esta hora (~03h). Cansado mas determinado. Já rejeitou explicitamente sugestões de pausa ("eu é que sei quando vamos parar mas obrigado"). Fricções operacionais reconhecidas e seguidas: ficheiros `.ps1` causaram problema na janela fechar — passámos a `.bat` com `pause`. Frase do Rui "alma de Deus" (resignação leve) usada como marcador do método de transferência PC→Beelink (qualquer canal serve).

## Transcripts anteriores

Para detalhes históricos:
- `/mnt/transcripts/2026-05-19-23-57-40-poker-app-pt27-pt28-context.txt`
- `/mnt/transcripts/2026-05-20-16-30-36-poker-app-pt28-hrc-debugging.txt`
- `/mnt/transcripts/2026-05-20-17-12-09-poker-app-pt28-hrc-debugging.txt`
- `/mnt/transcripts/2026-05-20-20-38-28-poker-app-pt28-pt29-hh-format-tree-size.txt`
- `/mnt/transcripts/2026-05-21-02-14-34-poker-app-pt29-hrc-tree-script.txt`
