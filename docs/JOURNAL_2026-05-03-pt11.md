# Sessão pt11 (3 Maio 2026, retrospectivo)

> Documentado a 4 Maio 2026 (pt12 inicial). Contexto da sessão original
> perdeu-se antes do journal ser escrito. Reconstruído read-only a partir
> de: git log, ficheiros em `%TEMP%`, snapshots BD via `railway run`, e
> timestamps em `discord_sync_state.last_sync_at`.

## Sumário executivo

Pausa ~2 dias desde pt10 (1 Maio madrugada → 3 Maio 14:11 UTC). pt11
focou nas **Ondas 9 e 8 do refactor #B23**. Onda 9 (validação manual
SS↔HH) executou-se antes do wipe — Rui validou 3/3 hands canal nota
visualmente: hand 1070 (GG-5891123077), hand 261 (GG-5892779222),
hand 878 (GG-5890155840). Algoritmo SS↔HH confirmado correcto em
prod (anchors+stack elimination). Após validação, avançou-se para
Onda 8 (delete+re-import), que expôs regressão #B33: 1/7 nota-hands
— `GG-5891409707` (id=2297) — ficou sem `discord_tags`, sem
`match_method` e sem villains, apesar da entry Discord respectiva
existir em BD (id=30, `replayer_link`, criada 2026-05-03 14:11:06).

Sem commits novos (sessão BD-only). Git em `22d6d1d` antes e depois.

---

## Estado BD pós-Onda 8 (snapshot 3 Mai ~14:11 UTC, IDs novos pós-re-import)

| Métrica | pt10 (1 Mai) | pt11 (3 Mai) | Delta |
|---|---|---|---|
| Hands 2026 totais | 1172 (1127 GG + 45 Winamax) | 1172 (1127 GG + 45 Winamax) | ✓ idêntico |
| GG enriched (`mm=anchors_stack_v2`) | 23 | **22** | ⚠ −1 |
| `anon_map` populado / total v2 | 23/23 | 22/22 | ✓ #B32 não regrediu |
| `hand_villains` rows | 47 | **45** | ⚠ −2 |
| `hand_villains` categories | múltiplas | só `nota` | ⚠ outras categorias desapareceram |
| Hands canal `nota` com match | 7/7 | **6/7** | ⚠ regressão |
| Entries Discord 2026 | 27 | 27 | ✓ |
| Range `hands.id` para 2026 | 261..1148 | 1410..2297 | (esperado pelo wipe) |

---

## Onda 9 — validação visual SS↔HH (executada antes do wipe)

Rui escolheu 3 das 7 hands canal nota e validou visualmente
comparando nicks atribuídos pelo algoritmo com screenshots originais
no Discord:

- hand 1070 (`GG-5891123077`, Speed Racer Bounty $32, 5 players,
  3 villains: PESCADOR NI.., Poteto!, YuuShou) ✓
- hand 261 (`GG-5892779222`, Bounty Hunters Deepstack $88, 8
  players, sem showdown, 2 villains: BornToFight, pokerking87) ✓
- hand 878 (`GG-5890155840`, Bounty Hunters Deepstack $32, 8
  players, cross-post 2 canais, 1 villain: ModoHard) ✓

3/3 correctas. Algoritmo SS↔HH validado empiricamente em produção.
**ONDA 9 FECHADA ✓**

---

## Onda 8 — teste regressão (executada parcialmente)

### Sequência (inferida)

1. Delete/wipe de hands GG.
2. Re-import GG ZIP (mesmo input que pt10 Fase 3).
3. Última actividade BD: 2026-05-03 14:11 UTC (timestamp em
   `discord_sync_state.last_sync_at` para 17 cursores).

### Resultado

Pipeline reproduziu **a maior parte** do estado pt10 mas com 2
divergências:

- 1 GG hand a menos enriched (22 vs 23).
- 1 nota-hand sem cobertura Regra C — `GG-5891409707` (id=2297) ficou
  com `discord_tags=[]`, `match_method=NULL`, 0 villains. As outras 6
  recompuseram-se: ids 1410, 1439, 1510, 1741, 2027, 2219 com
  2/2/2/1/1/3 villains respectivamente.

### 7ª hand — diagnóstico read-only

| Campo | Valor BD actual |
|---|---|
| `hands.id` | 2297 |
| `hands.hand_id` | `GG-5891409707` |
| `hands.played_at` | (4ª pt10 referida — mantém-se) |
| `hands.study_state` | `new` |
| `hands.match_method` | `NULL` |
| `hands.discord_tags` | `[]` |
| `hand_villains` rows | 0 |
| Entry Discord referenciando TM 5891409707 | `entries.id=30`, `source=discord`, `entry_type=replayer_link`, `created_at=2026-05-03 14:11:06`, `raw_text=https://gg.gl/t5sgb` |

A entry Discord existe e referencia explicitamente este TM. O pipeline
de auto-rematch (`_enrich_hand_from_orphan_entry` / `import_mtt`) não
fez a ligação. Causa raiz **não diagnosticada** nesta sessão — fica
para pt12 sob #B33.

---

## Bug descoberto: #B33 (a abrir formalmente em pt12)

**Sintoma:** após delete+re-import GG ZIP, 1/7 nota-hands não recebe
`discord_tags`/villains apesar de existir entry Discord
(`replayer_link`) que a referencia por TM number.

**Hipóteses a investigar (read-only) antes de fix:**

1. **Ordem de chegada:** as 6 OK podem ter HH a chegar **depois** do
   sync Discord (auto-rematch dispara via
   `_enrich_hand_from_orphan_entry`); a 7ª pode ter chegado **antes**
   (esperaria que o Discord-side a apanhasse via
   `_link_second_discord_entry_to_existing_hand` ou
   `_apply_channel_tags`).
2. **Tipo de entry:** entry id=30 é `replayer_link` (link `gg.gl/...`).
   Verificar se o pipeline trata `replayer_link` igual a `hand_history`
   text para efeitos de cross-post de tags.
3. **Caminho de inserção da HH:** `import_mtt` vs `_insert_hand` —
   confirmar qual dos dois processou esta HH e se ambos chamam
   `apply_villain_rules` + propagação de `discord_tags`.

**Ficheiros prováveis:** `screenshot.py`, `mtt.py`, `discord.py`,
`hand_service.py:_insert_hand` (lógica de placeholder GGDiscord).

---

## Tech debts: estado pós-pt11

Sem alterações ao backlog pt10 excepto adição de #B33 (acima).
Pendentes operacionais para pt12:

- **Onda 8 — completar:** investigar regressão da 7ª nota-hand antes
  de declarar a Onda 8 fechada.
- **#B33 (novo):** a abrir formalmente como tech debt em pt12.

---

## Observações

- **Wipe foi executado mas não há registo do comando.** Inferido pelo
  shift de IDs (261-1148 → 1410-2297) e pelos 17 cursores Discord
  resetados ao mesmo timestamp.
- **Categories de `hand_villains` ficaram só `nota`.** Em pt10 havia
  `nota` + `friend` (Karluz, flightrisk via Regra D). Re-import perdeu
  as `friend`. **Verificar em pt12** se é regressão ou se as hands com
  esses nicks simplesmente não estavam no ZIP re-importado.
- **Algoritmo `_build_anon_to_real_map` continua funcional** — 22/22
  com `anon_map` populado, sem #B32 latente.

---

Última sessão fechada: pt11 retrospectiva (3 Maio 2026, BD em `22d6d1d` em git mas com 1172 hands re-importadas a 3 Maio).
