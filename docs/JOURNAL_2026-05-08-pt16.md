# Journal pt16 — 2026-05-08

## Sessão pt16 — 8 de Maio 2026

### Trabalho realizado

- **#5 (TAGS Discord vazia em Estudo)**: verificado já resolvido em sessão anterior; backlog desactualizado.
- **#6 (status inconsistency Discord new vs resolved)**: badge unificado de 5 estados (Pendente / Arquivo / Órfã / Nova / Revista) + regra invariante "linkagem é precondição para Estudo". Backend (CASE em 8 endpoints) + frontend (`StateBadge` unificado, botão Revista guarded). Commit `be0b9c3`.
- **Bug "Copiar HH" rejeitado pelo HRC**: regex de split em `gg_hands.py:536` com lookahead bugado (`Poker\s+?` matcheia 2× por hand). Fix: split ancorado em `^Poker\s+Hand\s*#` MULTILINE. Commit `0d18c52`. Wipe 15.815 hands GG + 88 `hand_villains` + 305 entries Discord revertidas para `new`. Re-import: 15.811 hands restauradas com prefixo correcto.
- **Bug HRC concatenar BB+ante**: confirmado externo (HRC tem bug ao parser `Level12(3,500/7,000(1,000))` — agrega BB+ante = `70001000`). Não é da app. Reportar a `support@holdemresources.net` no futuro se quiseres.

### Discussão sem implementação

- **Feature IRE (Índice de Redução de Equity / Bounty Power)**:
  - Conceito: redução de equity necessária para call profitable em torneios bounty (em vez de pot odds standard).
  - Fórmula: `IRE = bounty / (4×call + 2×bounty)`.
  - Tabela W3cray: específica para SI/KO=4 (BBG $215 e similares); para outros ratios, regra de 3.
  - Investigação salas: GG não publica bounty no HH; Winamax e PS publicam; WPN nada.
  - Vision já extrai `bounty_pct` do SS GG (badge %). Falta bounty inicial em fichas (configurável manual por torneio).
  - UI proposta: badge no `HandRow` (entre board e blinds) + painel no modal detalhe (com os 6 painéis existentes reduzidos a metade).
  - Status: discussão pausada antes de plano final. Retomar em pt17.

### Pendências carry-over para pt17

- **Feature IRE**: clarificar com Rui o fluxo de cálculo + decidir tabela inicial bounty/SI por torneio + iniciar implementação.
- **Discord re-sync** (operacional, Rui): 319 entries em `status='new'` à espera de re-processamento.
- **Botão "copiar HH"** funcional (mas depende do HRC corrigir bug deles para validação 100% end-to-end).

### Estado da BD pós-pt16

- 16.604 hands totais (GGPoker 15.811 + Winamax 597 + PokerStars 107 + WPN 89).
- 100% das hands GG com `raw` começando em `"Poker Hand #"` (validado).
- 319 entries Discord em `status='new'`.
- 412 `hand_villains` preservados (perdas: 88 das hands GG apagadas).
