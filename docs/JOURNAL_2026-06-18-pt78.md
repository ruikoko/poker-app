# JOURNAL — pt78 (18 Junho 2026)

## Arco: pacote HRC vazio → payouts Winamax → upload de lobby SS

Sessão de **diagnóstico read-only** (BD de produção via proxy público, padrão pt37+)
seguida de **uma operação manual do Rui** (upload de lobby SS pela página). Nenhuma
escrita de código nesta investigação; o único `git` desta secção é este registo.

> **Antecedentes desta sessão (read-only, mesmo dia):** antes deste arco, a sessão
> correu diagnósticos à **desanon GG** que destaparam dois achados provados (ainda **sem
> fix**, registados para não se perderem):
> 1. **`position_v3` é sobrescrito pelo `table_ss`** — `table_ss_deanon._REAL_MATCH_METHODS`
>    = `{"anchors_stack_elimination_v2"}` **não inclui `position_v3`** (escrito antes do
>    pt75). Last-write-wins → o feltro clobbera a desanon por posição. Prova: `GG-6083680253`
>    (gold 12:30 → `position_v3` 4/4; table_ss 12:37 → sobrescreveu). Fix de 1 linha
>    (`+ position_v3` no frozenset).
> 2. **23 mãos gold-órfãs** (GG, `position_v3`, **sem tag**) no dia 16 — desanonimizadas
>    pela gold mas a tag-da-pasta (`folder_tag`) só viaja pelo caminho `table_ss` →
>    falham o gate `STUDY_VIEW_HAS_STUDY_TAG` → fora do Estudo **e** fora da triagem
>    (`capture_triage` é `table_ss`-only). Recuperação por-spot medida (âncora na SS da
>    mão imediatamente anterior): **13 recuperáveis** (12 tag única + 1 ambígua), **10 sem**
>    (2 a SS é da vizinha, 8 sem SS). Decisão de fix deferida ao Rui.
> Estes dois **não estão no PENDENTES** (fora do âmbito que o Rui pediu registar); ficam
> aqui no journal.

---

## 1. O que foi feito e descoberto (por ordem)

1. **Diagnóstico do pacote vazio.** Adapter OK (pt61, instalado em `C:\hrc\adapter\`).
   O gate `GET /api/queue/hrc` **serve** as 5 mãos libertadas (`hand_db_id` 1065, 1072,
   1076, 1085, 1116; batch `20260618T154924Z`): estão em `select_andar1_rows` ∩
   `hrc_queue_release` ∩ não-`done`. **Mas** correndo `build_queue_zip(include_no_payout=False)`
   — exactamente como o adapter puxa — o `manifest` dá **`hands_included: 0`, `skipped: []`,
   `missing_payouts: 5`**, todas com `reason="no_row_in_tournament_payouts"`
   (`queue_export.py:1341-1348`). **Zip vazio.**

2. **As 5 = 3 torneios WN PKO de 15-Jun, sem `tournament_payouts`:**
   - **EXPLORER** `1112550697` (€50) — 2 mãos (1065, 1072)
   - **GALACTICA** `1112554472` (€50) — 1 mão (1076)
   - **GRAVITY** `1129267954` (€250) — 2 mãos (1085, 1116)
   Nenhum tem linha em `tournament_payouts`; nenhum tem TS (parser TS é **GG-only**).
   `tournament_payouts` Winamax: **0 linhas** no total.

3. **Mismatch `include_no_payout` (released mas não puxável).** O `POST /hrc/release`
   valida cada mão com `build_queue_zip(..., include_no_payout=True)` (`queue.py:186`) →
   uma WN PKO sem payout **entra** no zip de validação (`total_in_zip>0`) → é libertada.
   O `GET /api/queue/hrc` usa `include_no_payout=False` (default, `queue.py:96→124`) →
   a mesma mão cai em `missing_payouts` → **dropada**. Logo: o release diz "exportável",
   o adapter puxa vazio.

4. **Mecanismo de payouts.** A escada de prémios **não vem de HH nem de TS de nenhuma
   sala**; constrói-se a partir de **SS** (lobby ou backoffice) via **Vision**, ou por
   inserção **manual**. Precedência **manual > backoffice_vision > (discord/file)_lobby_vision**
   (D11). **Não existe parser TS→payout.**

5. **Summary Winamax NÃO traz a escada de prémios** (confirmado pt38, reafirmado): só
   prize pool + field + resultado do jogador. Logo o TS WN nunca poderia alimentar
   `tournament_payouts` — só a SS de lobby (ou backoffice/manual).

6. **Bug: lobby SS Winamax nunca ligaram.** As 13 lobby WN foram processadas em **16-Jun**,
   **antes** das mãos WN existirem na app (re-import foi 18-Jun) → resolver deu
   `tm_not_found` (tn=NULL, 0 payouts escritos) → **sem auto-retry efectivo** quando as
   mãos chegaram.

7. **Origem das 13 lobby WN existentes (só o que está no log).** Todas **UPLOAD**:
   `discord_message_id` = **sha256 do conteúdo** (hex-64), `channel_id` = **NULL**
   (`lobbys.py:94/124`, `source_prefix='file_lobby_vision'`). **Zero via Discord #lobbys**
   (esse seria snowflake + `channel_id`). O log **não distingue** página Lobbys de
   appimport-pasta (mesmo endpoint, filename não guardado). *(Fora do log: os nomes em
   `done/lobby/` são IT-format `Winamax.exe-Winamax-<ts>-NN.png` → appimport.)*

8. **`captured_at` é o make-or-break.** Vira `posted_at` = **âncora prestart** do resolver
   (`lobbys.py:84-92`). As 16-Jun ancoraram a 16-Jun **porque o timestamp estava no nome IT**
   → resolveram para as instâncias de 16-Jun. A **página Lobbys** usa `file.lastModified`
   do ficheiro; **ausente → `now`** → âncora errada → não-resolve / mis-resolve.

9. **Dry-run `reconcile_lobby_logs` (read-only).** Re-corre o resolver sobre os
   `tm_not_found` com o `vision_json` guardado: **resolve 7 torneios WN (todos de 16-Jun)
   → 15 das 22 mãos WN PKO da fila** saem do "pacote vazio". **Mas os 3 torneios dos 5
   libertados (15-Jun) NÃO** — não há lobby SS de 15-Jun no log (só 08-Jun ×2 + 16-Jun ×13).
   Resolução verificada como correcta (16-Jun lobby → 16-Jun torneio; o anchor temporal
   separa instâncias homónimas). **Escrita pendente** (dry-run só).

10. **Solução aplicada (o Rui executou).** Upload das **lobby SS de 15-Jun pela página
    Lobbys** (data de ficheiro = 15-Jun → `captured_at` correcto) → resolveram **EXPLORER,
    GRAVITY, GALACTICA** (+ bónus **INTERSTELLAR**, **BATTLE ROYALE**) → **payouts escritos**
    → as **5 destravaram** → **adapter puxou** → **watcher a correr no Beelink**.

---

## 2. Erros, constrangimentos e soluções

| # | Erro / constrangimento | Solução |
|---|---|---|
| E1 | **Pacote vazio garantido**: mãos libertadas sem payout (mismatch `include_no_payout` release=True vs adapter=False). | Carregar o payout (SS de lobby) → mãos passam a entrar no zip. **Latente no código** → pendente (alinhar/avisar). |
| E2 | **Lobby WN `tm_not_found`** (processadas antes das mãos) sem auto-retry. | `reconcile` (dry-run feito; resolve 7/16-Jun) + re-upload das de 15-Jun com `captured_at` certo. Pendente: auto-retry no import. |
| E3 | **`captured_at` errado** → âncora prestart = `now` → não/mis-resolve. | Upload com **data de ficheiro = dia do torneio** (página lê `file.lastModified`) / timestamp no nome IT. |
| E4 | **Summary WN não tem escada de prémios** → sem caminho TS→payout para WN. | Estrutural: payout WN só por **SS de lobby** (ou backoffice/manual). |
| E5 | **`limpa_scratch.bat`: `arquivo\` access denied** ao mover. | **POR RESOLVER.** Provável: perfil `riand` sem permissão na pasta do `Administrator`, ou handles abertos (watcher/HRC). A investigar: correr o `.bat` como `Administrator`, fechar HRC/watcher antes, ou trocar o `move` por `robocopy /MOVE` com retry. |

---

## 3. Pendentes (registados em `docs/PENDENTES.md`, secção pt78)

1. **WN lobby não religa sozinha** quando as mãos chegam — falta **auto-retry** do
   `tm_not_found` no fim do import de HH (o `reconcile` existe mas não cobriu este caso).
2. **Avisar à entrada** quando um torneio entra **sem payout** (não-pronto p/ HRC), em vez
   de se descobrir por pacote vazio. (Inclui resolver o **mismatch `include_no_payout`**
   entre o guard do release e o pull do adapter.)
3. **Religação a sério dos 15 de 16-Jun** (dry-run feito; **escrita pendente**).
4. **`tm_not_found` por resolver:** 2 GG Bounty Hunters, **ZENITH 15-Jun**, **William
   Harding** (possível misread da Vision).

---

## Evidência (números-chave)

- Released: 5 (`hand_db_id` 1065,1072,1076,1085,1116), batch `20260618T154924Z`.
- `build_queue_zip(include_no_payout=False)` → 0 incluídas / 5 `missing_payouts`
  (`no_row_in_tournament_payouts`).
- `tournament_payouts` WN: 0 (pré-upload).
- andar1 WN PKO sem payout: **22 mãos / 10 torneios**. Dry-run reconcile: **7 torneios /
  15 mãos** resolvem (16-Jun); 3 torneios / 7 mãos (15-Jun) ficam — exactamente os dos 5.
- Lobby WN no log: 13, **todas UPLOAD** (hash + `channel_id` NULL), 0 Discord.
