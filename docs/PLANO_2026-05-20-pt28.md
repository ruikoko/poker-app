# Plano pt28 — 20-21 Maio 2026

Definido por Rui no closeout pt27 (manhã 20 Maio). Sequência obrigatória: **Bloco C primeiro** (fix funcional do `.exe` watcher), depois bónus / decisões product se houver tempo.

## Contexto

Pt27 fechou backend HRC com 3 fixes (commit `7de8df6`). Mão baseline `GG-5944816316` agora chega ao watcher com `meta.json` correcto (`max_players=6 / target_node_offset=4 / ci=10.0`). O bloqueador para smoke real fim-a-fim é exclusivamente do lado watcher: `start_calculation_selected_subtree` não dispara o popup Nash em runtime (`#START-CALC-SELECTED-SUBTREE-NO-POPUP-OPEN`, CRIT pt26).

Smoke A pt27 (rollback `.exe` pt25d) confirmou que voltar ao pt25d **não** é caminho — a baseline pt25d tem 40 de 41 mãos pulled sem chegar a `done` em 14 Maio. O fix tem de ir para a frente.

## Bloco C — Fix popup + finalize + recompilação `.exe` pt27-v2 (CRÍTICO)

Ordem dentro do bloco — cada passo dependente do anterior:

### Passo 1 — Diagnóstico smoke devagar dedicado do popup Nash

Antes de mexer no código, isolar a causa de `_wait_for_nash_popup` devolver `None` por timeout 5s. 4 hipóteses a despistar (pt26 §"#START-CALC..."):

- **(a) Calculate clicou mas popup não abriu.** Verificar visualmente: o click acerta no botão verde? HRC ficou em estado que aceita o click (não bloqueado por outro modal)?
- **(b) Timing — popup demora >5s.** Cronometrar manualmente. Se 6-15s, aumentar `_NASH_POPUP_WAIT_TIMEOUT_S` resolve. Se >30s, há outro problema (HRC ainda a finalizar 1ª run?).
- **(c) Calculate button coord `(204, 59)` errado para estado pós-1ª-run.** Wireframe pode mudar quando 1ª run completou (Strategy Table populada, layout shift). Re-capturar coords no estado pós-1ª-run com `pyautogui.position()`.
- **(d) `start_calculation` legacy abre e fecha popup Nash da 1ª run; segundo Calculate vai a outro lado.** Verificar se a 1ª run usa o mesmo popup Nash internamente. Se sim, o "Calculate" da 2ª run pode ter de aparecer de outra forma (button diferente, atalho de teclado, ou state machine não-óbvia).

Rui executa step-by-step no Beelink, regista qual hipótese se confirma.

### Passo 2 — Fix `start_calculation_selected_subtree` em `tools/watcher_src/patched_funcs.py`

Dependendo do diagnóstico do Passo 1:

- **(a)** verificar/recalibrar `CALCULATE_BUTTON_X/Y`.
- **(b)** ajustar `_NASH_POPUP_WAIT_TIMEOUT_S` (e.g. 5s → 15s) — fix de uma linha + teste.
- **(c)** recalibrar coords e/ou adicionar wait pós-1ª-run antes do click.
- **(d)** maior — talvez precise de detectar estado do popup (já-aberto vs fechado) antes de re-disparar, ou usar atalho de teclado em vez de click.

Cada caso tem unit test correspondente em `tests/test_watcher_set_scope.py` (ou ficheiro novo se for caso novo).

### Passo 3 — Fix `finalize_after_second_run` (`#FINALIZE-NEVER-FIRES-ON-NO-OP`)

Independente do Passo 2 (pode ser feito em paralelo): `start_calculation_selected_subtree` deve devolver `bool` de sucesso. `setup_hand` só chama `finalize_after_second_run` se retorno `True`. Senão `finalize` da 1ª run com warning explícito. Evita zip parcial (só 1ª run) quando Selected Subtree falha silenciosamente.

### Passo 4 — Recompilação `.exe` pt27-v2

Mesma trampoline strategy do pt26 (`swap_and_smoke.py` em `_local_only/` gitignored). Aplicar SWAPs + APPENDs para as funções modificadas em Passo 2/3. Smoke harness `swap_and_smoke.py` valida mecanicamente (deve dar 14/14 PASS ou superior).

### Passo 5 — Smoke real fim-a-fim no Beelink

Mão de referência: **`GG-5944816316`** (a mesma de pt27 — todos os 3 valores do `meta.json` correctos; falta validar a recepção do watcher).

Pipeline esperado:
1. Watcher `GET /api/queue/hrc` → zip com 4 ficheiros (`hh.txt`, `script.js`, `payouts.json`, `meta.json`).
2. Adapter desempacota em `C:\hrc\adapter\queue\GG-5944816316\`.
3. Watcher abre HRC, paste do `hh.txt`, configura equity model (Multi Table ICM, max_players=6), corre 1ª run com CI=10.
4. Após 1ª run completar: navegação por setas (`navigate_to_target_node(wpos, offset=4)`).
5. Click Calculate (2ª run).
6. Popup Nash detectado → fill CI=10 → set scope Selected Subtree → OK (Enter).
7. 2ª run corre em Selected Subtree (tempo significativamente menor que Full Tree).
8. `finalize_after_second_run` exporta zip → POST `/api/queue/hrc/results` com Bearer auth.
9. `hrc_jobs.status='done'` em prod com `result_zip` populated.

Cronometrar 2ª run. Comparar com baseline pt25d (~76 min Full Tree CI=10).

### Critério de sucesso Bloco C

- Smoke real passa fim-a-fim (mão `GG-5944816316` ou outra equivalente).
- Pelo menos 1 mão em `hrc_jobs` com `status='done'` + `result_zip` legítimo.
- 2ª run em Selected Subtree mais rápida que Full Tree (mínimo 2× speedup esperado).
- Decisão: promover `.exe` pt27-v2 para produção operacional (Rui usa diariamente) **vs** rollback se algum sub-passo falhar.

## Bónus (se sobrar tempo após Bloco C)

### B1 — Investigar `#VISION-LOBBY-API-FAILURE`

Tech debt aberto pt26 (CRIT). Bloqueador upstream do `#HRC-CONTEXT-MISMATCH-PLAYERS-LEFT`. Auditoria pt27 mostrou ~34% taxa de falha (10 em 29). 4 sub-perguntas:

- (a) SS do torneio `WN-4690815078549684227-208-1778279040` estava associado à mão no canal Discord da tag HM3?
- (b) `lobby_processing_log` tem entrada para esse `tournament_number`? Erro registado?
- (c) Vision call site em `services/lobby_vision.py` só faz `logger.error/warning + return None` em todas as failure paths — não há retry nem propagação de erro além de None. **Fix conceptual:** tornar Vision API failure observable (não silenciosa) + popular `lobby_processing_log.reason_detail` mesmo em erro de API.
- (d) Quotas/limits API Anthropic — auditoria 19 Maio mostrou só 5 dias de uso em Maio (plenty de quota); investigar outros factores (timeouts, rate limits, lobby SS malformado).

### B2 — Decisões product pendentes (acumuladas pt26+pt27)

Cada uma precisa de input Rui antes de implementar:

**B2.1 — `study_state` migration (`#STUDY-STATE-REGRESSION-HH-IMPORT`).** Fix `import_.py:311/335` + migration one-shot ~25k mãos. **Pergunta para Rui:** ainda quer a spec original "bulk imports invisíveis na página Mãos" ou mudou de ideia (querer ver tudo)?

**B2.2 — FT auto-classification.** Pt26 abriu 4 opções (A status quo / B `players_left ≤ FT_size` / C `tournament_summaries` / D flag UI). Descartada após re-classificação do bug original. Reavaliar quando `#HRC-CONTEXT-MISMATCH-PLAYERS-LEFT` for atacado em paralelo.

**B2.3 — Winamax `tournament_summaries` pipeline (`#WINAMAX-TOURNAMENT-SUMMARIES-PIPELINE`).** Implementar parser + endpoint + UI espelhando GG. Resolve parte do gap G1 (Winamax sempre falha no resolver TIER 0). **Pergunta para Rui:** vale o esforço dado volume Winamax ~5%?

### B3 — Limpeza tech debts LOW

- `#AUTH-SCHEME-MIGRATION-UNDOCUMENTED`: 5 min de grep em README/ONBOARDING/MAPA para confirmar que não há menção a X-API-Key e remover se houver.

## Tech debts em fila (sem deadline definido)

- `#HRC-MTT-OTHER-TABLES-INFO` — só fecha quando `#VISION-LOBBY-API-FAILURE` + `#HRC-CONTEXT-MISMATCH-PLAYERS-LEFT` fechados.
- `#PT25D-WATCHER-FRAGILE-CLIPBOARD-OR-RESTORE` — não-urgente; só relevante se for necessário voltar ao pt25d como fallback.
- Outros (Bug J `prune_action_on_line`, Bug I, KB5087051 monitor, change-password feature, Gyazo, hand_notes, screenshot_url) — todos pré-pt27, sem mudança de prioridade.

## Estimativa de duração

- Bloco C: 4-8h (dependente do diagnóstico Passo 1; se for hipótese (b) timing, ~1h; se for (d) state machine não-óbvia, pode ser 6+h).
- Bónus B1: 2-3h se atacado.
- Bónus B2.x: 1-2h cada se Rui dá aprovação product na sessão.

Sessão alvo: **20 Maio (Bloco C smoke devagar + diagnóstico) + 21 Maio (recompilação + smoke real + closeout)**. Pode esticar se Passo 1 revelar caso (d).

## Sequência obrigatória

1. **Bloco C antes de tudo.** Sem watcher funcional, todo o resto é desperdício de ciclos.
2. Bónus só depois de Bloco C atingir critério de sucesso ou ser declarado bloqueado.
3. Decisões product pedem Rui presente — não atacar sem confirmação.
