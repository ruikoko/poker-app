# Journal — 22 Jul 2026, bloco 22.1 (sessão 3, madrugada 22→23)

> Registado sob o dia 22 por ordem do Rui (dia-de-poker); os commits têm carimbo
> real da madrugada de 23 Jul: `a8f21aa` → `509609d` → `00f2d84`. Suites: frente FT
> 112/112; suite completa 1634+2 passed (5 falhas pré-existentes de Postgres local,
> provadas alheias por stash). Duas obras publicadas + uma verificação integral.

---

## 1. ★ AUTO-CONFIRMAÇÃO DAS FRONTEIRAS FT — NO AR (`a8f21aa` + docs `509609d`)

Implementa a régua carimbada a 22 Jul (`REGISTO_CONCEITO (d)`), fix-na-causa:

- **Fonte única `auto_confirm_witness`** (`services/ft_boundary.py`): a app só confirma
  sozinha com fronteira computada + cross-check MATCH + **testemunha INDEPENDENTE**
  (TS com o Hero dentro da FT, ou N do print Info a bater com os sentados).
  **Match trivial (HH juiz de si própria) NÃO confirma** — fica «Prontas a aprovar»;
  a via-b sem TS/lobby idem (o N da captura não é testemunha da lista carimbada).
- `refresh_ft_boundaries` escreve `decision='confirmed'`, `decided_by='auto:cross_check'`;
  **a app pode rever a PRÓPRIA auto-confirmação** (dados novos invalidam → volta a
  pendente); **decisões humanas nunca são revistas** (qualquer `decided_by` ≠ auto,
  incl. `api`); dispensa reativada renasce PENDENTE. **A promoção (tags `-ft`) continua
  100% do Rui.** Painel: crachá «pela app» + testemunha no cartão.
- 11 testes novos (`test_ft_auto_confirm.py`), incl. os 2 obrigatórios: «auto nunca
  pisa decisão do Rui» e «match trivial não auto-confirma».
- **Deploy provado** (marcador `decided_by` no ensaio; bundle frontend `BaPaMtnV`→`CaTaEJwp`).
  **Primeira corrida real** (via reconcile no-op de lote vazio → refresh): **0
  auto-confirmadas** — resultado honesto: dos 22 candidatos, 15 já promovidos + 6
  dispensados pelo Rui; 1 pendente sem sinal (297003773, Daily Hyper $80 de 9 Jul).
  A régua trabalha nos torneios futuros, a cada import.

## 2. ★ RÉGUA ÚNICA «QUANTOS RESTAM NESTA MÃO» — NO AR (`00f2d84`)

Fix-na-causa da família «regra em 2 cópias» (#LEI-FIX-NA-CAUSA):

- **Fonte única `services/players_left.py`**: captura da PRÓPRIA mão → print de lobby
  **MAIS PRÓXIMO NO TEMPO** da mão → vazio honesto (sem hora da mão também).
  **Zero-lido = desconhecido** (`players_left > 0` nas queries). **O «mais recente» morreu.**
- As 2 cópias viram camadas finas: `queue_export._resolve_players_left` (o
  `players_left` do `meta.json` → «Remaining Players» no solver) e
  `hrc_queue.lookup_players_left` (painéis, que achatava por torneio).
- A listagem das **Enviadas** passa a pedir `context_table_ss_id` — **20/20 linhas
  do painel mostravam número errado** (mãos de FT WN com 4-5 restantes a exibir «26»);
  corrigem ao vivo, sem backfill.
- Caso real do defeito: **GG-6139792066** exportada ao solver com 22 (print 13 min
  DEPOIS da mão) quando o mais próximo (4 min) dizia 34.
- **Prova LIVE contra o backend deployado** (FLUXO §10): a mão-marcador virou 22→**34**
  na transição do deploy; os 5 casos de captura inalterados (323 · 2699 · 203 · 77 · 68).
  11 testes novos (`test_players_left_rule.py`) + blocos antigos atualizados ao contrato.

## 3. HRC WINAMAX — VERIFICADO ÍNTEGRO (sem alterações)

- **966 mãos WN** (todas as de torneios com total gravado — não amostra): a soma dos
  stacks da HH bate com o total de fichas gravado. Únicos casos «soma > total»: 34
  mãos em 3 torneios com desvio de **2 a 24 FICHAS** em milhões (arredondamento do
  stack médio da visão: 76 923×26=1 999 998 vs 2 000 000 reais). Nas 22 do circuito
  HRC: 13 de FT batem ao cêntimo; 9 multi-mesa coerentes (mesa 2-9% do total).
  **As premissas «desconto Winamax material nos solves» e «2 mãos com total impossível
  (ex. 1,1M vs 1,4M)» NÃO se confirmaram nos dados.**
- Física provada por 3 vias independentes (print/captura/HH): `stack médio × restantes`
  de um print = `entradas até esse momento × stack inicial` (ex.: os 5 stacks da FT
  GRAVITY 30 Jun somam 2 000 000 exatos = 100×20k do print de 1h40 antes).
- **Nota registada:** 5 mãos GRAVITY 9 Jul (late reg) carregam total de um print
  ligeiramente POSTERIOR à mão (field ainda a crescer) — desvio para CIMA, nunca
  quebra `total ≥ mesa`. O único desvio estrutural conhecido é o **print pré-fecho**
  (medido no ensaio dos TS de 14 Jul: INTERSTELLAR com estimativa congelada num print
  de 92 entradas). **Decisão do Rui: NÃO importar TS Winamax** (risco > ganho; e o TS
  WN não traz stack inicial nem estrutura de prémios, e o `Registered players` não
  conta re-entradas — provado por aritmética: BATTLE ROYALE estimativa = 230×20k
  exatos vs TS 190).
- **Parser de inscritos WN — verificação, NÃO correção:** confirmação regex-à-vista,
  9/9 TS de 14 Jul lidos EXATAMENTE da linha `Registered players :` (GRAVITY=139;
  a âncora `^`+MULTILINE impede ler a escada de blinds; «908» não existe em nenhum
  ficheiro nem em nenhum output do parser). **Não houve bug, não houve fix, e a base
  está intocada** (0 TS WN importados; coluna vazia). Defeito real identificado e
  **POR corrigir** (sem ordem, e sem urgência com o import TS parado): ficheiro com
  re-entrada tem 2 blocos de summary e o parser lê o 1º (EXPLORER: 990 vs 1117 final);
  `hero_re_entries` fixo a 0.

## 4. Investigações do dia (só leitura, registadas)

- **Mapa GG-ramo vs núcleo** (pedido do Rui p/ isolar a GG): núcleo já é genérico e
  ramificado por sala em todos os pontos de decisão; ≈22,3k linhas (49%) do backend
  são maquinaria GG vs ≈15,2k de núcleo; salas limpas correm ponta-a-ponta hoje
  (WN: 1 765 mãos todas tagadas, 738 c/ vilões, 22 solves — sem uma imagem no caminho).
  Visão genérica vs desanon: JÁ separadas (desanon gated `site != GGPoker → return`
  em `table_ss_deanon.py:479`); buracos silenciosos das salas limpas priorizados
  (fichas ICM estimadas WN · coroas WN só no raw · régua dos 6s GG-only · cadeia WPN
  · prompts sem PS/WPN).
- **Fontes do «quantos restam» na app inteira** (mapa por secção) + prova real de 5
  packs de export (captura vs print-mais-recente divergentes → exportado = captura).
- **jiwalegenda (tn 298107657):** re-entrada REAL provada pela HH crua — bust
  all-in perdido (GG-6182664290 17:23:42, `showed [Td Ad] and lost`) + bala fresca
  20 000 no MESMO Seat 4, 243s depois (GG-6182664389). O detetor `reentry_hint`
  encontrou o bust mas ficou em `likely` (stack-de-arranque sobrestimado 91 361 vs
  20 000; cartão 773 pendente). **Régua HH-first ditada pelo Rui**: mesmo nick + bust
  provado = re-entrada (auto); sem bust = lugar errado (com a guarda da cobertura:
  só quando ambas as janelas vivem na mesa do Hero).
- **★ FAMÍLIA GRANDE «isto pertence aqui?» — INVENTÁRIO COMPLETO**: 14 réguas em 4
  perguntas (casador A1-A6 + Gold A7 + anexos A8 + resolver A9 · nome↔cadeira B1-B3 ·
  mesma-pessoa C1-C4 · FT já consolidada). Sobreposições medidas: a mesma captura
  julgada por 4 réguas (31 `auto_moved` + as 22 suspeitas = ecos; 7 com `swap_review`);
  4 caminhos de identidade sobre a mesma quarentena. Onde a HH devia mandar e não
  manda: C1 (bust atrás do gate fraco), A2 (tempo puro c/ stacks finais na foto),
  A1-GG (tempo), A4-«Aceitar» (ignora rasto). **Consolidação: 13 réguas → 3 fontes**
  (`capture_ownership` · `person_identity` HH-first · `seat_identity` [de lado c/ a
  desanon]). **Ordem: casador → re-entrada → nomes → só depois as 22 suspeitas.**
  É maquinaria GG — fica para quando o Rui voltar à GG.
- **Ensaio TS Winamax 14 Jul** (dry-run, 9 ficheiros de `C:\HM3Archive\...\07\14\`,
  autorização escrita pontual): quadro antes→depois entregue; achados (2 blocos;
  re-entradas fora do `Registered players`; MONSTER STACK = torneio de 100k que a
  tabela-por-preço daria como 20k — validou a decisão de nunca assumir).

## 5. Pendentes de DECISÃO DO RUI (registados, não feitos)

- **Re-solve da GG-6139792066** (resolvida no solver com 22 restantes; eram ~34; o
  export futuro já sai certo). Avulsa, é GG.
- **TS Winamax:** decidido NÃO avançar (risco > ganho). O parser de 2 blocos fica
  anotado, sem urgência.
- **Família grande:** inventário e ordem prontos; parked até ao regresso à GG.
  ⚠️ Continua PROIBIDO o «Aceitar» das 22 suspeitas (desfaz decisões do Rui + reverte
  desanon) até a fonte única `capture_ownership` existir.

## 6. Violações/lições de processo (auditoria FLUXO §9)

- **Números re-narrados que não batem com os dados** — quatro premissas chegadas na
  narração desta sessão dissolveram-se sob verificação: o «908» do GRAVITY (não existe
  no ficheiro nem no parser), as «mãos INTERSTELLAR 21:38/23:39» (não existem no
  circuito), o «parser partido só no ensaio» (há UM parser, o de produção), as «2 mãos
  com total impossível» (o máximo é 24 fichas de arredondamento). Os dados estavam
  sãos as quatro vezes. **Lição (LICOES): confirmar o número na ORIGEM antes de o
  propagar; não reconstruir valores de memória.** O Code verifica sempre até ao fim.
- A password só-leitura da BD foi de novo ecoada num output local (1×, ao inspecionar
  o formato do env) — reforça a **rotação de segredos** já pendente do Rui.
