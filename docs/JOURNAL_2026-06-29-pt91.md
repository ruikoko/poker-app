# JOURNAL pt91 — 29 Jun 2026 (Fase 2 das regras de sizing do HRC — branch, NÃO produção)

As **3 regras de sizing do Rui** + **preservação da ação real** + **mirror do offset** +
**fix do squeeze** entraram na branch `feat/hrc-sizing-rules` (pushed com upstream). O
**smoke visual no Beelink passou** para os sizes. **NÃO foi para produção** — a Fase 2
fica guardada na branch até o índice de navegação (`#IMPLICIT-LINES`) ser confirmado numa
mão com decisão real do Hero. Push da branch é seguro; soltar para produção, ainda não.

> ⚠️ Sessão interrompida por **falha de energia** a meio do último fix (squeeze). Recuperada
> sem perda — o código estava completo no disco, só por commitar. Fecho retomado em seguida.

---

## O que foi feito

1. **3 regras de sizing do Rui + preservação da ação real** (`d64f47e`) — gerador/template
   passam a aplicar as 3 regras do Rui aos sizings, mantendo a **ação real da HH** como 1ª
   opção (não a substituem por defaults).
2. **Mirror do offset para as regras** (`df93989`) — as Regras 1/3 passam a aplicar-se também
   às posições **foldadas** (mirror do offset), alinhando o nó de navegação com o sizing.
3. **`#ICM-CHIPS-USE-TS-FINAL-FIELD-GG`** (`1ba6215`, docs) — registar que o total de fichas
   ICM deve usar o campo final do TS (GG).
4. **Fix do squeeze** (`08757c9`, `#SQUEEZE-LIVE-CALLER-PT91`) — `getSizings3Bets` contava o
   `getFlatCallCount` cru: um shortie **all-in** atrás (ex.: SB auto-all-in pela ante,
   0,05bb) era contado como caller e o nó ia para `getSizingsSqueeze` (sizing **fixo 11**),
   quando na verdade é **side-pot** e deve cair no **3bet normal** (dinâmico, Regra 2). Novo
   helper **`hasLiveNonAllInCaller`** exige um oponente **vivo, não-foldado e não-all-in**
   além do opener para tratar como squeeze a sério.

## Commits (branch `feat/hrc-sizing-rules`)

| Hash | O quê |
|---|---|
| `d64f47e` | feat: 3 regras de sizing do Rui + preservação da ação real |
| `df93989` | feat: mirror do offset para as regras de sizing (Regra 1/3 nas foldadas) |
| `1ba6215` | docs: registar `#ICM-CHIPS-USE-TS-FINAL-FIELD-GG` (total de fichas ICM) |
| `08757c9` | fix: squeeze só com caller vivo e não-all-in (`#SQUEEZE-LIVE-CALLER-PT91`) |

Branch **pushed** com upstream (`origin/feat/hrc-sizing-rules`). **Sem merge para `main`, sem deploy.**

## Validação

- **Smoke visual no Beelink (sizes) — PASSOU:**
  - **HJ 6.15bb** → ficou **só shove** (eff baixa → regra de stack curto, sem mini-raise).
  - **UTG** → **preservou o open real de 2bb** da HH (a ação real entra como 1ª opção, não
    é trocada por default).
- Squeeze: fix coerente e completo no `.js`; cobre o caso do shortie all-in atrás (side-pot
  → 3bet normal em vez de squeeze fixo).

## Pendente — caminho para produção (NÃO fechado nesta sessão)

- **Falta confirmar `#IMPLICIT-LINES`** (índice de navegação) numa **mão com decisão real do
  Hero** antes de a Fase 2 sair da branch. O smoke validou os **sizings**, não o **índice**.
- Próximos passos: validar `#IMPLICIT-LINES` end-to-end → só então ponderar merge para `main`
  + caminho de produção. Até lá, a branch fica como está (guardada e enviada).

## Estado

- Branch `feat/hrc-sizing-rules` = 4 commits à frente de `main`, **pushed**.
- **Produção intacta** (`main` não tocado, sem deploy).
