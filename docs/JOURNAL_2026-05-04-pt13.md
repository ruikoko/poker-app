# Sessão pt13 — 4 Maio 2026

Sessão curta de housekeeping. 1 tech debt fechado.

## Commits
| Hash | Mensagem |
|---|---|
| `554cafb` | chore(#B-NOVO-2) assert defensivo + fechamento + journal pt13 |
| `6ee5e8e` | docs(pt13) hash do commit anterior |
| (a preencher) | chore(#B29) remove paths sem Q6 guard (single source of truth = apply_villain_rules) |

## #B-NOVO-2 — fechado

### Contexto
Tech debt aberto em pt10 (refactor #B23) marcado como "parcialmente
resolvido por #B32, merece investigação dedicada se re-aparecer".

### Investigação
Code investigou o estado actual:
- Bug original #B32 (pt10, commit `5fe2201`): apa placeholder-only
  fazia `_build_anon_to_real_map` retornar `{}` silenciosamente,
  e `match_method='v2'` era gravado na mesma. Hands ficavam presas
  com hashes em vez de nicks.
- Fix #B32 cobriu set + guard de idempotência.
- 23/23 hands afectadas re-curadas em pt10.

### Verificação em prod
Query directa BD prod:
- `degenerate_count` = 0 (esperado)
- `total_gg_enriched_2026` = 24 (bate com snapshot pt12)

### Defesa adicional
Assert defensivo em `_enrich_hand_from_orphan_entry` antes da
chamada a `_build_anon_to_real_map`: levanta `ValueError` explícito
se apa só tem `_meta`. Torna eventual recidiva visível em vez de
silent skip.

### Resultado
#B-NOVO-2 fechado. Backlog reduz de 13 para 12 tech debts abertos.

## #B29 — fechado

### Contexto
Tech debt aberto em pt10 marcado como "Funcional latente —
contadores hands_seen possivelmente inflados em refix".

### Investigação
3 sítios incrementam `villain_notes.hands_seen`:
- A) `services/villain_rules.py:_persist` (canónico, com Q6 guard) ✓
- B) `routers/mtt.py:_create_villains_for_hand` (legacy bulk archive,
  sem guard) ✗
- C) `routers/mtt.py:re_enrich_all` (UPSERT manual após enrich,
  sem guard) ✗

Sítios B e C podiam causar double-count quando hand passava por
mais de um path.

### Estado em prod
- `total_villain_notes` = 42
- `hands_seen>0` = 29 (corresponde a hand_villains únicos)
- inflação total = 0 (curado por `recalculate_hands_seen` em pt12)
- código continua armado para futura inflação

### Fix (opção α)
Remover os 2 blocos UPSERT desprotegidos:
- `mtt.py:_create_villains_for_hand:793-801` (8 linhas)
- `mtt.py:re_enrich_all:2045-2081` (37 linhas — bloco + dead code
  associado)

Substituídos por comentários explicativos `#B29 pt13` documentando
o que foi removido e porquê.

Net: −32 linhas. `apply_villain_rules` é a única fonte de verdade.

### Verificação pós-fix
- `hands_seen>0` = 29 (mantido)
- `hand_villains` = 34 (mantido)

Bate 1:1 com snapshot pt12. Mudança em código sem efeito em dados.

### Resultado
#B29 fechado. Backlog reduz de 12 para 11 tech debts.
