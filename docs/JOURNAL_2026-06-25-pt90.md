# JOURNAL pt90 — 25 Jun 2026 (watcher OCR tree-size + Release watcher-pt90)

Fecho source-side do `#HRC-TREE-GIGANTE`: guarda preventiva de trees gigantes no watcher,
por leitura OCR read-only do painel "Tree Statistics", ANTES da 1ª run. **End-to-end no
`.exe` por validar no Beelink (smoke).** Source no `watcher-gate` (os docs vivem no `main`).

---

## O que foi feito

1. **`tools/watcher_src/tree_stats.py`** (novo) — `capture_tree_stats()` lê `{nodes, gb,
   hrc_available_gb, ok, raw}` via PrintWindow `PW_RENDERFULLCONTENT` + `Windows.Media.Ocr`
   (background-safe, não mexe no rato). **Dual-import** `winsdk` (Py≤3.13) / `winrt` (Py3.14+).
   Nunca lança.
2. **Hook em `patched_funcs.py:_wait_for_finish_ready`** (Fase 2 = tree size já computado):
   regista as stats no `meta.json` (`tree_nodes/tree_size_gb/hrc_available_gb/build_seconds/
   solve_seconds/ocr_ok`) e **aborta** (raise → `.failed`, como a Fase 2 timeout) **só** com
   **DUPLA LEITURA** concordante: ambas `ok` E ambas `gb > TREE_GB_ABORT_LIMIT(15)` OU
   `> hrc_available`. OCR falhado / leituras a discordar → **NUNCA aborta (fail-open)**.
3. **Cross-check do tempo-de-build descartado** — gigantes 2-3s / normais <1s (janela cega).
   `build_seconds` fica registado no `meta.json` só para histórico, já não é gate.

## Commits (watcher-gate)

| Hash | O quê |
|---|---|
| `9609ab6` | feat: tree_stats.py + hook + consts + helpers (4 hunks) + solve_seconds. |
| `7384ed2` | fix: dual-import winsdk/winrt + `write_bytes(bytearray(...))` (validado no Beelink). |

## Validação

- **Smoke #1 (Beelink, Python real 3.14 + winrt):** OCR leu `nodes=507581, tree=3.8GB,
  hrc=19.9GB` certos contra o painel — técnica provada em Python, não só conceito.
- **Harness `swap_and_smoke` (in-process):** ALL OK (45 consts c/ `TREE_GB_ABORT_LIMIT`/
  `OCR_REREAD_GAP_S`; 62 funcs c/ os 3 helpers novos; asserts SWAP/APPEND/CONSTS verdes).
- **Bundle do `.exe` verificado por TOC:** `tree_stats` (de `tools/watcher_src/`) + winsdk OCR
  (`media.ocr`/`graphics.imaging`/`storage.streams`/`globalization`) + `_winrt.pyd` presentes
  → guarda **viva no binário**, não fail-open-morta.

## Build + Release

- **Build:** venv Py 3.12 + `winsdk 1.0.0b10` + `Pillow 12.2.0`; spec com `collect_all('winsdk')`
  + `tree_stats` (pathex/hiddenimport) + PIL submodules; **UPX off** (nativos winsdk).
- **`.exe`:** `SHA256 = 69e741c2f8b80e3f1323aaa1fe6150adb046d3b83ef87debadf7613321cc673c`,
  32 988 546 bytes.
- **Release `watcher-pt90`** (tag no commit `7384ed2`, asset `hrc_watcher.exe`, SHA nas notas):
  https://github.com/ruikoko/poker-app/releases/tag/watcher-pt90

## Pendente (operacional, Beelink — ver `PENDENTES pt90`)

Descarregar da Release + confirmar SHA → instalar (1 só exe; salas fechadas) → **smoke
end-to-end**: mão normal corre; gigante forçada → `.failed`; OCR forçado a falhar → corre.
Só então `#HRC-TREE-GIGANTE` fecha de vez + ponderar merge `watcher-gate`→`main`.
