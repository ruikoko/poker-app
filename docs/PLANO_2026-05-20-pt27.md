# Plano pt27 — 20 Maio 2026

Definido por Rui no fim da sessão pt26 (madrugada 19→20 Maio). Sequência obrigatória — Bloco A antes de Bloco B antes de Bloco C.

## Contexto

Em pt26 foi descoberto que o smoke real end-to-end falhou em duas frentes distintas:
1. `start_calculation_selected_subtree` falha em runtime (peça 1 do Bloco 2 não actua) — questão de código no `.exe`.
2. Vision API do lobby falhou em processar SS de uma mão específica → `players_left=null` → HRC configurado com Other Tables=0 → cálculo ICM inválido (HRC tratou torneio 13-left como 4-handed) — questão de pipeline de dados.

Ambas precisam de fix, mas antes de qualquer debug do `.exe`, é necessário confirmar que o backend está a entregar boa data. Sem isso, debugging do watcher seria inútil.

Para criar baseline confiável, Rui fez push em pt26 de:
- Todos os lobbies do Discord da última semana.
- Todas as mãos Discord da última semana.
- Todas as mãos HM3 da última semana.
- Importação na app de mãos GG da última semana.

## Bloco A — Auditoria das importações da última semana (PRIMEIRO)

Validar que as importações entraram correctamente na BD e classificar falhas.

Etapas:

1. **Inventário do que devia ter entrado**:
   - Lobbys Discord: quantos pushed pelo Rui, quantos chegaram à BD via pipeline Discord.
   - Mãos Discord: quantos entries esperados, quantos `discord_entries` rows criados, quantos `hands` rows com `discord_tags` preenchido.
   - Mãos HM3 (.bat import): quantas mãos no batch, quantas em `hands` table, quantas falharam parsing.
   - Mãos GG (ZIP import via app): mesmo tipo de comparação.

2. **Diagnóstico de falhas por categoria**:
   - Vision API failures (lobbies que não tiveram SS processada — `lobby_processing_log` com erros).
   - Regex/parser failures (mãos com `parse_status != 'ok'` ou similar).
   - Dedup hits (mãos já existentes ignoradas).
   - Network/timeout failures.
   - Pre-2026 ingest barrier rejections.

3. **Output esperado**:
   - Tabela contagens: esperado vs entrado vs falhado.
   - Lista de hand_ids/lobby_ids problemáticos com root cause.
   - Plano de re-tentativa para os recuperáveis.

## Bloco B — Pipeline end-to-end com mão fresca (DEPOIS DE A)

Escolher uma mão jogada nas últimas 48h e validar pipeline completo, etapa a etapa.

Etapas:

1. Confirmar entrada na app (HM3 e/ou GG).
2. Confirmar `discord_tags` aplicado correctamente (com base no canal Discord onde foi tagged).
3. Confirmar SS do lobby associada à mão (timestamp window do canal correcto).
4. Confirmar Vision API processou SS — `lobby_processing_log` com `status='success'` e `parsed_players_left` não-null.
5. Confirmar backend escreve `players_left`, `total_chips`, `equity_model`, `target_node_offset` no `meta.json` quando a mão é puxada via `/api/queue/hrc`.
6. Confirmar adapter no Beelink (`hrc_adapter.py`) pulled a mão e descomprimiu para `Teste completo/<hand_id>/`.
7. Confirmar watcher (futuro `.exe` pt27-v2 com fix) configura HRC com contexto correcto — Other Tables, Players, etc.

## Bloco C — Fix do .exe (DEPOIS DE B)

Só depois de Blocos A e B confirmarem boa data:

1. Diagnóstico + fix de `start_calculation_selected_subtree`. Hipótese principal: falta `_click_calculate_button` antes de `_wait_for_nash_popup`. Code deve auditar a função em `_local_only/watcher_decompile/build_pyi/patched_funcs.py`.
2. Fix de `finalize_after_second_run` para disparar mesmo quando peça 1 degrada (defensive fallback que NÃO mascare problema).
3. Recompilação `.exe` pt27-v2 com PyInstaller.
4. Substituição no Beelink (com backup do pt26 actual).
5. Smoke real completo end-to-end:
   - Mão fresca seleccionada no Bloco B.
   - Validar todos os passos: navigate → set scope Selected Subtree → CI Target refine → calculate → finalize → export.
   - Validar zip final em `done/Exports/`.

## Decisões pendentes (não bloqueiam pt27)

- **CI default mismatch** (`#CI-DEFAULT-MISMATCH`): backend escreve CI=5, Rui esperava CI=10. Investigar mais tarde.
- **FT auto-classification** (`#FT-AUTO-CLASSIFICATION-FROM-CONTEXT`): 4 opções no journal pt26. Decisão product não urgente.

## Tech debts gatekeepers

Estes devem ficar fechados ou aceitos antes de avançar para Bloco C:
- `#VISION-LOBBY-API-FAILURE`: causa-raíz do problema do contexto multi-table. Bloco A vai expor a escala do problema.
- `#HRC-CONTEXT-MISMATCH-PLAYERS-LEFT`: bloqueia validação real do Bloco 2 peça 2 (navigate por offsets).
- `#START-CALC-SELECTED-SUBTREE-NO-POPUP-OPEN`: bloqueia funcionalidade core do Bloco 2.

## Notas operacionais

- Rui está a sair de uma sessão de ~14h de debug. Abrir pt27 com energia fresca; usar journal pt26 como contexto primário.
- O `.exe` actual no Beelink (pt26, SHA `2213aa19...`) tem peça 2 funcional mas peça 1 quebrada. Rollback para pt25d via backup `hrc_watcher_pt25d_20260519_014101.exe` se quiseres jogar antes do fix.
