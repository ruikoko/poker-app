# Journal pt61 — 6 Junho 2026

Sessão **operacional no Beelink**, sem alterações de código no repo **excepto o
glossário** (pt anterior fechado: pt60). O grande achado: o **adaptador HRC nunca
tinha sido instalado no Beelink** — só lá estava o watcher. Era esta a razão de
fundo do "smoke real Beelink" (pt42d) ter ficado pendente desde então. Adaptador
instalado, comportamento do rato do watcher verificado pelo bytecode, fila cortada
a 5 mãos GG e **smoke EM CURSO** (~horas) — resultados a verificar na próxima sessão.

> ⚠️ **Estado:** o smoke das 5 mãos **ainda está a correr** ao fechar a sessão.
> Tudo o que segue foi **observado/verificado hoje**; os **resultados do smoke**
> (bounty/Instant, POST dos done) ficam **por verificar** em pt62.

---

## A) Adaptador HRC instalado no Beelink pela 1ª vez

- **Descoberta:** o adaptador (`hrc_adapter.py`) **nunca** tinha sido instalado no
  Beelink — só lá vivia o watcher (`hrc_watcher.exe`). É **por isto** que o smoke
  real Beelink (pt42d) tinha ficado pendente: havia robot, mas nada que cosesse
  `GET /api/queue/hrc` ↔ filesystem ↔ `POST /api/queue/hrc/results`.
- **Utilizador do Beelink = `riand`.** Watcher em
  `C:\Users\riand\HRCWatch\hrc_watcher.exe` (+ cópia no Desktop).
- **Banner do watcher** confirmou `QUEUE_DIR = C:\Users\Administrator\Documents\Teste
  completo` (= o **default do adaptador**, sem override). Nota operacional: o watcher
  corre **como `riand`** mas lê/escreve a pasta do **`Administrator`** (perfil legacy
  preservado pelo reset Windows; ver pt22).
- **Instalação** em `C:\hrc\adapter\`: `hrc_adapter.py` + `payouts_helpers.py` +
  `requirements.txt` descarregados do **GitHub raw** (branch `main`); **Python 3.14.5**;
  `venv`; `pip install requests` (2.34.2); `HRC_WATCHER_API_KEY` definido via `setx`
  + `$env` (token do Railway `poker-app`).

## B) Comportamento do rato do watcher — verificado pelo bytecode

Verificado a partir do bytecode (`ANALYSIS.md` + disassembly de `setup_hand` +
decompile do launcher), para garantir que arrancar o watcher é seguro:

- O **loop principal só mexe no rato quando há mão pendente** (`if pending:
  try_setup`); **fila vazia = sem rato**.
- `ensure_hrc` abre o HRC via `subprocess` + `EnumWindows` (**sem rato**); o **1º
  clique** (`open_wizard`) está **dentro** do `setup_hand`.
- **Conclusão:** arrancar o watcher com a **fila vazia é seguro** — só toma o rato
  quando começa a processar uma mão. **Confirmado empiricamente:** o banner ficou
  em "A aguardar mãos" sem tocar no rato.

## C) Adaptador validado (watcher off)

Com o watcher desligado, o adaptador **autenticou** (Bearer OK) e **puxou 50 mãos**
para a fila.

- **Erro residual (esperado):** `POST GG-5944816316 done` → **HTTP 404**
  ("hand_id não encontrado"). Causa: um **zip de resultado antigo** na pasta `done\`
  de **antes da lousa limpa** (a mão já não existe na BD pós-reset).
- **Reconfirma** a falha conhecida: *a rotina de reset devia limpar `done\` +
  `lobby_sent.txt`*. **Acção:** zip antigo retirado de `done\` → `C:\hrc\stale_done`.

## D) Glossário de poker (única mudança de código no repo)

`docs/GLOSSARIO_POKER.md` criado (fonte **GTO Wizard**) + **ponteiro no CLAUDE.md**,
como referência/memória de terminologia para as máquinas (Claude Web + Claude Code).
Confirmada a entrada final **"Wheel Ace"** verbatim na fonte → commit + push
(`68f112f`).

## E) Smoke de 5 mãos — EM CURSO

Fila **cortada a 5 mãos GG**: `6027196507`, `6027502592`, `6027503512`, `6027751209`,
`6027752934`. As outras **45 movidas** para `C:\hrc\queue_hold`. Watcher arrancado
sobre as 5 — **a correr** (~horas) ao fechar a sessão.

---

## Pendente para pt62 (verificação)

1. **Verificar os 5 resultados** — tratamento bounty/Instant
   (`#WN-BOUNTY-NULL-IN-HRC-PIPELINE`, `#HRC-BOUNTY-HARDCODED-50PCT`): são **GG**;
   confirmar se PKO e se **Instant = 50%** está certo.
2. **Correr o adaptador** para `POST` dos 5 `done` para a app; confirmar que **entram**.
3. **Repor `queue_hold` (45)** de volta para a corrida completa.
4. **Limpar `GG-5944816316` de `done\`** + ponderar pôr a **limpeza de `done\` na
   rotina de reset**.
5. ~~Push do glossário~~ — **feito** nesta sessão (`68f112f`), após confirmar "Wheel Ace".

---

## Estado git

Sem alterações de código no repo nesta sessão **excepto o glossário** (`68f112f`,
fechado em D). Todo o trabalho A/B/C/E vive no Beelink (`C:\hrc\`), fora do repo.
HEAD: `68f112f`.
