# JOURNAL pt67 — visão do robot + Max Players (lei do Rui) + re-smoke prep (10 Junho 2026)

Sessão de código (backend + watcher) + Release `watcher-pt67`. **Backend 916
PASSED + 102 watcher pytest + in-process smoke ALL OK.** Diffs validados pelo Web
antes do build. Re-smoke real das 2 mãos **pendente** (Beelink).

## 1. Backend — Max Players = SPAN âncora→BB, teto 6 (LEI)

`#HRC-MAX-PLAYERS-SPAN-NOT-PARTICIPANTS`. `derive_max_players` reescrito: já não
conta participantes (`voluntary_before + hero + still_to_act`) mas o **span
posicional** âncora→BB (`max = N − idx_âncora`, via `derive_seats_in_preflop_order`),
com **teto 6** (emenda Rui: `min(span, 6)`, min 2). Regra 1 (herói foldou unopened →
âncora=herói) **e** regra 2 (1ª ação voluntária) uniformes.

15 testes (helper reescrito: botão em Seat #(N−2) → seat i = posição i): 3
cross-check como regressões (**GG-6029013400 → 5** [era 2], GG-6039094225 → 2,
GG-6028190109 → 5), regra 1 (CO→4, UTG1→cap 6), teto (span 9 → 6), space-nicks,
degenerados. LEI: `REGRAS_NEGOCIO §15` + `WATCHER_FLUXO`.

**"In hand: 4"** confirmado SEPARADO: `get_player_count_from_hh` (watcher), não a
derivação backend. Corrigido `\S+`→`.+?` no mesmo pass do watcher
(`#WATCHER-PLAYER-COUNT-SPACE-NICKS`).

## 2. Watcher (`patched_funcs.py`)

- **`#HRC-RUN-WINDOW-DETECTION-BLIND`** — removido o `sleep(30)` cego (causa-raiz:
  engolia runs curtas ~5s) + o `sleep(5)` + o check enganador `_wizard_window_present`
  ("wizard fechou" — com o morph a janela NÃO fecha). `_wait_for_run_completion`
  reescrito: **vigia desde o Finish** (poll 0.5s), capta o **HWND**, espera-o
  desaparecer (`IsWindow` + título; robusto ao morph wizard→run de mesmo título);
  grace de ecrã limpo se nunca aparecer (fail-open). `_RUN_WAIT_GRACE_CLEAN_S=30`
  parametrizado + logging. **Zero heurísticas de tempo/memória.** Novo
  `_find_progress_window_hwnd`.
- **`#HRC-CI-SAFEGUARD-CHILD-CONTROLS`** — `_read_hwnd_text` + `_scan_target_ci_in_window`
  leem o "Target CI" nos **child controls** do dialog (o "MC-CFR [Target CI < 10.00]"
  vive num label interior, não no título). `WM_GETTEXT/WM_GETTEXTLENGTH` re-adicionados.
- Testes watcher: run-wait (hwnd) + CI (child controls) reescritos; +1 teste do
  scan child-controls. In-process smoke: 36 consts / 40 funcs (allowlists do
  trampoline atualizadas).

## 3. Build + Release

- `.exe` PyInstaller 6.20 (host 3.12): **SHA256
  `a9554427979a6de62278aaef3a96ffee686c768cb3c868e35f3edb411b3bc931`**, 12,89 MB.
- **Release `watcher-pt67`** (pública) com **4 assets**: `hrc_watcher.exe`,
  `instala_pt67.bat`, `requeue_pt67.bat`, `requeue_state.py`. Download anónimo do
  exe validado (HTTP 200, SHA round-trip exacto); `requeue_state.py` reachable.

## 4. Re-smoke prep (a armadilha do requeue)

O fix do Max Players é **backend** → os packs das 2 mãos no Beelink têm o meta
**antigo (Max=2)** e o `state.json` do adapter marca-as `done` → não re-puxa. E a
app exclui `hrc_jobs.status='done'` do GET. Solução em **2 lados**:

- **Backend (feito):** `DELETE FROM hrc_jobs WHERE id IN (6,7)` (GG-6029013400 +
  GG-6039094225) — verificado antes (status done) e depois (0). **= a recalc da
  quarentena das 2 mãos da re-smoke.** Confirmado: ambas voltaram a `ELEGÍVEL`.
- **Beelink (scriptado, regra 4):** `requeue_pt67.bat` (asset da Release) **descarrega**
  `requeue_state.py` da Release e corre-o pela venv do adapter → remove as 2 entradas
  do `state.json` + apaga os packs/zips antigos. Sem transferência manual, sem editar
  JSON à mão.

## Ordem dos passos do Rui no Beelink (re-smoke pt67)

1. Instalar o robot pt67 (Downloads + duplo-clique no `instala_pt67.bat`).
2. `requeue_pt67.bat` (Downloads + duplo-clique) → limpa state + packs antigos.
3. Arrancar o adapter (`arranca_adapter.bat`) + o watcher.
4. O adapter re-puxa as 2 mãos → packs **novos (Max=5)** → re-smoke.

**Critérios:** `GG-6029013400` sai com **Max Players=5** no log/wizard + 1ª run
**DETECTADA** ("janela vista a aparecer e a morrer", **sem** WARN de "NUNCA vista")
+ `[scope] idx=1` + exatamente 2 runs + sem `select_bounty_mode` + Bounty Mode=PKO
Instant=50%; `GG-6039094225` = regressão limpa (Max=2, não-KO). O re-POST das 2
sobrescreve a quarentena.

## Pendentes à entrada da próxima sessão

1. **Re-smoke pt67** das 2 mãos (Beelink) — critérios acima.
2. **Soltar a fila ~49** em lotes ao ritmo do Rui, **só** após a re-smoke passar.
3. **Recalcular as quarentenas restantes** `GG-6028190109` + `GG-6027751209` no 1º lote.
4. **Calibrar `_RUN_WAIT_GRACE_CLEAN_S`** se a re-smoke o indicar (logging incluído).
5. Flag deferido: **prova do bounty ≠50%** (sem 0.75/0.40 na fila) — confirm one-off
   quando uma monster/super ko aparecer.

## Notas

- O `.exe` não foi corrido aqui (arrancaria o watcher / tocaria no HRC — unsafe). A
  validação foi o in-process smoke + o build + o SHA round-trip; o resto valida-se na
  re-smoke do Beelink.
- O timing exacto do run-window (velocidade do morph, valor do grace) é o único ponto
  por calibrar — parametrizado + com logging para a re-smoke.

## ⚠️ Erro de sequência (registado — nova regra 10 do FLUXO)

A 1ª re-smoke (18:09) correu com o backend de **produção ainda SEM o fix do Max
Players** — os commits `5f1c8c0`+`ad3274f` nunca tinham sido pushed, a Railway nunca
deployou, e os packs "regenerados" saíram com a derivação **antiga (Max=2)** (Rui
confirmou Max=2 na GG-6029013400). **Só a metade do watcher** (run-window/2 runs/CI)
ficou validada por essa volta. **Lição → `FLUXO §10`:** fix de backend valida-se com o
backend **DEPLOYADO** (a regra "push no fecho" não se aplica quando a produção é o
objeto do teste).

**Recuperação (mesma sessão):** (1) push `5f1c8c0`+`ad3274f`; (2) deploy Railway
confirmado **LIVE** (SUCCESS, commit `ad3274f`); (3) **verificado no backend deployado**
— `GET /api/queue/hrc/hand/GG-6029013400` → `meta.json max_players = **5**` ✓; (4)
DELETE do hrc_job novo (id 8, Max=2 das 18:09) da GG-6029013400 — `GG-6039094225`
(id 9) **fica** (o Max=2 dela é correcto por natureza, não precisa de 3ª volta).

## 3ª volta (só GG-6029013400)

O Rui corre o requeue de **1 só mão** (`requeue_state.py GG-6029013400`). **Critério de
fecho do pt67:** `Hand Mode: Max Players = 5` no `[SETUP]`/wizard da GG-6029013400 +
cadeia completa (1ª run detectada, scope idx=1, 2 runs, bounty). Aí o pt67 fecha
inteiro.

## 3ª volta — resultado: Max=5 ✓ MAS nó de navegação ERRADO (off-by-one)

A 3ª volta validou o **Max Players=5** (prova fotográfica do Rui) **mas** a navegação
da 2ª run aterrou no **nó errado**: Max=5 ✓, agressor HJ jam 9.01bb, **devia** ir ao 8º
nó (R9.01, offset **7**) e foi ao 7º (R2.00 HJ, offset **6**) — **uma linha curto**.

### Diagnóstico (com fonte, não especulado)

Causa: o **`offset_within_bucket`** (`hrc_node_offset.py`). O **pt61** (`940bf36`,
`#HRC-NODE-OFFSET-SB-JAM-OFFBY1`, **"EM BUFFER" nunca validado em smoke**) tinha
colapsado o within-bucket para "o alvo é sempre o 1º nó" (non-SB `0` / SB raise `1`),
assumindo que `build_sizings_overrides` põe a acção original como 1ª opção. A **1ª smoke
real de navegação** (esta 3ª volta) desmentiu: o **jam** é o nó **ALLIN = o maior = o
ÚLTIMO** do bucket (a Strategy Table lista os sizings por ordem ascendente). O within
correcto **depende de all-in**: non-SB small=0/jam=1; SB Complete=0/small=1/jam=2.

### Fix (`9b772ce`) — `#HRC-NODE-OFFSET-OFFBY1-REVERT-PT61`

`offset_within_bucket` volta a usar `_is_all_in_effective` (0.95). Cross-check
(offsets confirmados visualmente pelo Rui / packs 6 Jun), **verificado localmente
ponta-a-ponta via `build_queue_zip` contra a BD de produção**:

| Mão | agressor | esperado | local (fix) | prod (pt61) |
|---|---|---|---|---|
| GG-6029013400 | HJ jam 9.01 (stack 9.16) | **7** | 7 ✓ | 6 ✗ |
| GG-6039094225 | SB jam 6.35 (stack 6.47) | **14** | 14 ✓ | 13 ✗ |
| GG-6028190109 | HJ small 2.0 (stack 34.5) | **2** | 2 ✓ | 2 (não afectada) |

Suite **919 PASSED** (os casos pt61 invertidos + 3 regressões reais novas). Latente
registado `#HRC-NODE-OFFSET-IMPLICIT-LINES` (LOW): assume 2 linhas non-SB / 3 SB;
open-jam de stack muito curto podia sair +1 comprido → **confirmação VISUAL do nó é
critério obrigatório**.

### Deploy (FLUXO §10) — a 1ª build falhou

Push `9b772ce`. A 1ª build da Railway **FALHOU** (infra; a build local + suite estavam
verdes, imports limpos). `railway redeploy` re-corre a última build **SUCCESS** (a antiga
`ad3274f`, offset 6) — **não** apanha a build falhada. Re-trigger via novo push (o commit
dos docs). **Verificação obrigatória antes da 4ª volta:** `GET /api/queue/hrc/hand/
GG-6029013400` → `meta.target_node_offset = 7` (e `GG-6039094225` = 14).

## Âncora da 2ª run — lei estacionada + desempate empírico (decisão Rui via Web)

O Rui propôs a **lei B** (âncora = **1º nó da posição**, não o sizing real), que cobriria
todos os sizings da posição **se** o Selected Subtree incluísse as linhas-irmãs abaixo.
**Estacionada** (consentimento do Rui): essa premissa **não está provada**.

**Verificação dos zips (método auditável, registado em `HRC_ANATOMIA §14.2`):** abriu-se
o zip de resultado do nó-errado (`GG-6039094225` job 9). Achado: é um **Complete Export
da árvore inteira** (`treeconfig.mode=scripted`, `nodes/0.json` sequence vazia=raiz, 141
nós), **sem** qualquer marcador `selected`/`subtree`/`accuracy`. **Conclusão honesta:** o
zip **NÃO prova** qual o nó refinado → **não prova** a exclusão. (Uma afirmação intermédia
minha de "os zips demonstram exclusão" foi **excesso** — corrigida pela regra "não
especular".) A exclusão assenta só no **modelo** (sizings = linhas-irmãs disjuntas).

**Lei provisória (em vigor):** âncora = **sizing real** (o nó da acção exacta do
agressor) — **segura sob ambas as semânticas**. `REGRAS_NEGOCIO §16` + `HRC_ANATOMIA §14`.

**Desempate empírico (4ª volta):** com a âncora no jam, o Rui observa se os valores do
**irmão small-raise** mudam durante/após a 2ª run. Mudam → semântica do Rui → lei B
reactiva (offset trivial) + resultados de hoje reavaliam-se. Não mudam → irmãos disjuntos
→ sizing real vira lei definitiva + o resultado de hoje era lixo genuíno.

## Label do log corrigido (watcher) — `#HRC-NAV-LABEL-MISLEADING`

O label `navigate_to_target_node: … (esperado: HJ raise 9.01bb)` (`patched_funcs.py`)
mostrava a **acção do agressor**, não o **alvo da navegação** — foi o que nos induziu em
erro hoje (lia-se "esperado HJ 9.01" mas a pergunta era o OFFSET). Corrigido para mostrar
o **alvo real**: posição + sizing + **nº da linha (offset)**, para a verificação visual
nunca mais depender de interpretação. Watcher recompilado (Release `watcher-pt67b`).

## Pré-autorizações usadas (Rui ausente; Web validou)

- **DIFF validado pelo Web** → push `9b772ce` (offset fix) ao abrigo da pré-autorização.
- **DELETE job 9 (`GG-6039094225`)** confirmado → mão volta a elegível p/ a 4ª volta. (O
  job 8 da `GG-6029013400`, 3ª volta, já fora apagado na verificação do Max.)
- **4ª volta = AS DUAS mãos** (requeue), ao critério do Rui (hoje ou amanhã de manhã).

## Estado final pré-4ª volta (tudo deployado + verificado)

- **Offset fix** (`9b772ce`): poker-app **SUCCESS** (`6fd6b30`); `GET /hand` →
  `target_node_offset` = **7** (#400) e **14** (#225). FLUXO §10 ✓.
- **Build Railway destravada:** pin Python **3.12** (`2c0823b`, o mise não tinha binário
  p/ 3.13.14) + `audioop-lts` só-3.13 (`bdb3c68`).
- **413 (`#HRC-RESULT-ZIP-413`):** cap `/results` **50 → 200 MB INTERINO** (`6fd6b30`),
  verificado live (POST 60 MB → 400 invalid-zip, não 413). Porta = **app**, não edge
  (POST 120 MB chegou ao uvicorn). #400 (Max=5, ~112 MB) passa agora. Rede de salvação:
  `/hrc-sessions` (`/import`, sem cap → hrc_sessions). **Não definitivo** (72×112 MB≈8 GB
  BYTEA → A/B/C em avaliação).
- **Job 9 (#225) apagado** (`/import` n/a; ambas elegíveis). #400 nunca teve job (413).
- **requeue** robustecido (`9c5fb59`): limpa todos os layouts de `done\`.

## Linhas exactas para o Rui (4ª volta — Beelink, AS DUAS mãos)

**Watcher:** usa o **pt67 já instalado** (`a9554427`) — **NÃO** reinstalar. Só o backend
mudou (o offset vem do `meta.json`; o watcher lê-o). O label corrigido
(`#HRC-NAV-LABEL-MISLEADING`) fica para um rebuild posterior — esta noite verifica-se
pelo ECRÃ, não pelo log.

1. **Requeue das 2 mãos:** Downloads → duplo-clique `requeue_pt67.bat` (limpa state +
   packs antigos de `GG-6029013400` **e** `GG-6039094225`).
2. **Rede de segurança (cola no PowerShell do Beelink, depois do requeue):** apaga
   qualquer zip ANTIGO das 2 mãos sob `done\` (com o cap a 200 MB, um zip de offset
   errado deixado lá seria ENTREGUE). Deve imprimir `limpo`:
   ```powershell
   $d="C:\Users\Administrator\Documents\Teste completo\done"; foreach($h in 'GG-6029013400','GG-6039094225'){ Get-ChildItem $d -Recurse -Filter "$h*.zip" -ErrorAction SilentlyContinue | Remove-Item -Force -ErrorAction SilentlyContinue }; "limpo"
   ```
3. **Arrancar** `arranca_adapter.bat` + o watcher (HRC fechado; **nunca** "Always run in
   background" — RUNBOOK §2.7).
4. O adapter re-puxa as 2 → packs **novos** (offset **7**/Max **5** e **14**/Max **2**).
5. **Verificação visual OBRIGATÓRIA (critério de fecho do pt67):**
   - `GG-6029013400`: a 2ª run aterra no **8º nó / R9.01 HJ** (não no R2.00 = 7º). Max=5.
   - `GG-6039094225`: a 2ª run aterra no **nó ALLIN do SB** (último do bucket SB).
   - **Desempate dos irmãos (foto):** durante/após a 2ª run, os valores da **linha do
     small-raise irmão** (R2.00) **mudam ou não?** → decide a lei da âncora (`§16`).
6. Os resultados **entram agora** em `hrc_jobs` (cap 200 MB). Confirmo eu pelo backend.
