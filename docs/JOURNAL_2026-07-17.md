# JOURNAL — 17 Julho 2026

Dia de **qualidade de dados GG** (coroas) + **cura no core do reconcile**. Sessão longa,
tudo LIVE em `main`. Segue o fecho de 16 Jul (VALE da lei de sizings v3).

> Nota de relógio: o sistema reportou "14 Jul", mas `2026-07-14/15/16` já são journals
> existentes (protótipo Resultados HRC + lei v3). Este trabalho **segue** o fecho de 16 Jul →
> datado **17 Jul**. As LICOES do dia foram corrigidas de 07-14 para 07-17.

## ★ 1. Cura no core do reconcile — PROVADA (27/27 no 1º sweep)

O reconcile que re-casa SS/lobbys pendentes já existia e já cobria pendentes antigas, mas
o gatilho era a **última linha do request de import** → se o import dava timeout (502,
`#SYNC-ENDPOINTS-SYNCHRONOUS-TIMEOUT`) nunca disparava; foi o que prendeu 27 SS WN de 12/07.
Opção A do Rui: **varredor independente** (`start_pending_sweeper` no lifespan — 1 passagem no
arranque + tick periódico, daemon threads) + `reconcile_table_ss(pending_only=True)` (não
re-desanoniza as 1695 success a cada tick) + `retry_failed_table_ss_vision(limit)` (re-Vision
das SS falhadas em lote pequeno). **Prova:** o 1º sweep de arranque recuperou **27/27** os SS WN
(`no_match` 28→1; fica só 1 GG de formato antigo). Regra permanente registada: **toda operação
em lote tem botão de Cancelar** (memória `feedback_batch_ops_need_cancel`).

## ★ 2. Coroas — dois censos = 0, régua do delta corrigida

- **Censo pré-pt95 (table-SS) = 0** e **censo Gold/carry = 0** — o wipe de 8 Jul saldou toda a
  leitura pré-correção-de-prompt (pt95 01/07, anti-chama Gold 05/06 < wipe). A classe viva é o
  **erro por-imagem** (caso 6570), só medível por **amostra**, não por censo.
- **Amostrador de coroas** (`/crown-sample`): modo **"Ver candidatas"** (177→155 KO/PKO, sem
  Vision, sorteio fixo) + releitura opcional por cima + **Cancelar** + banner da limitação (a
  releitura corre sobre a cópia comprimida → "—" fraco, valor→valor forte). Âmbito corrigido:
  o sliver filtrava só por timestamp → arrastava 22 vanilla (Daily Hypers) → filtro de formato.
- **Régua do delta corrigida:** a auditoria aritmética reportou `delta=1` em 149/155 como
  "sistemático" — o Rui verificou **7/7 mãos à vista, todas limpas** → a régua media a chave
  `_meta` (bookkeeping) do `all_players_actions`, não sit-outs. Régua certa = `Seat lines` da HH:
  **150/155 delta 0**. A verificação do Rui estava certa; a régua era o defeito. **Dívida fechada.**

## ★ 3. Forense verde-KO — teoria da soma MORTA (0/7); padrão real = eliminado com coroa NULL

7 espécimes do Rui cruzados com a HH: **0/7** confirmam "coroa do matador = placa + verde
somado" — os matadores mostram o instantâneo fresco exato. O padrão **realmente** confirmado
(4/7): o **ELIMINADO** fica com **coroa NULL** (ao bustar, a dourada própria some; o valor vai a
verde para o matador) — leitura **honesta**, não corrupção. 3 dos 4 são NÃO-sliver → a classe
varre TODAS as KO/PKO.

## ★ 4. Caso 2480 arbitrado pelo Rui NA IMAGEM (só a imagem arbitra)

Eu errei o valor da coroa do matador (AVRELIY) DUAS vezes por raciocínio ($125 pré-KO, depois
$187.5 mecânico). O Rui, à vista: **dourada $265.62 + verde $187.50** → bounty do eliminado
(PhilVsSandw..) = **verde × 2 = $375** (não os $250 "base"; ele não estava fresco). **Nem o
gravado nem a teoria batiam — só a imagem.** LICAO: qualquer fluxo que escreva um bounty mostra
SEMPRE a imagem primeiro e grava o que o olho lê. Estes 2 valores são os **2 primeiros carimbos**
do fluxo de recuperação (a aplicar na Etapa 2).

## ★ 5. Detetor de bounties recuperáveis — construído e validado

`services/crown_recovery.classify_hand` (puro): bust = all-in ∩ "and lost" no SUMMARY; posições
via `_get_position` do parser; classifica coroas NULL em **grupo 1** (bustou+NULL, recuperável) /
**grupo 2** (não-bustou+não-Hero+NULL, falha real) / **over-read** (Seat≠extraídos, à parte).
Raiz apanhada: o `_SEAT_RE` comia as linhas do SUMMARY "Seat N: hash (small blind) … and lost" →
exigir "(N in chips)". Validado **4/4** casos conhecidos; sobre **488** KO/PKO com Gold →
**40 mãos/48 seats grupo 1 · 19 grupo 2 · 11 over-read**.
- **Painel "Bounties recuperáveis" (Etapa 1, só-leitura)** no ar: imagem + eliminado + matador
  apontado (onde ler o verde) + Cancelar; grupo 2 → balde das coroas; over-reads à parte.
- **Fix do matador sem nome:** 20/40 cards tinham matador "—" → o vencedor era o **Hero** (hash
  literal 'Hero'; a entrada dele em players_list vem sem posição). Resolvido pelo nome real +
  badge HERO. Matador "—" agora **0/40**.

## Prova visual (privada) + regra de links
Artifact com 3 casos (imagem + eliminado + matador). **Violação registada:** re-enviei os 6 casos
de validação com um link partido (`feedback_hand_refs_by_link`) → **corrigir amanhã** (ponto 2a
dos pendentes).

## Estado de outras frentes
- **Fase 2 Resultados HRC (Wizard):** ligada e no ar (rota + `openHand`); veredicto do Rui +
  HH viewer + contexto do torneio **pendentes**.
- **golds_unread:** 55 → **0** (fecharam durante o dia).
- **env RO:** rodar do lado do Rui (Railway).

## Commits do dia (main)
`f4572d4` Fase 2 rota+openHand · `b707c90` cura no core (sweep) · `eea72e6`/`d9b7988`/`4794e0c`/`dd4fc15`/`17f9a61` amostrador (painel+cancelar+candidatas+âmbito+selo) · `451e8d6`/`02a70d0`/`990a2c5`/`63f61ef` detetor de bounties recuperáveis (núcleo+endpoints+painel+fix-matador) · docs (LICOES/journal).
