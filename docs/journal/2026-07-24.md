# Journal — 24 Jul 2026 — ★ TOTAL DE FICHAS WINAMAX PELO PRINT DE LOBBY (5 fases construídas)

> Contexto: continua o FOCO WINAMAX (GG em espera). Ontem à noite o lote da pasta de
> prints destravou 12 torneios/24 mãos (balanço no fecho de 23 Jul); hoje o Rui aprovou
> o plano do **total de fichas WN pelo lobby** (`#WN-TOTAL-CHIPS-FROM-LOBBY`) e as 5
> fases foram construídas. A decisão de 22 Jul «TS Winamax NÃO se importa» **mantém-se**
> — esta via é do print de lobby, não do TS.

## 1. A regra (decisões do Rui, 24 Jul)

Prova visual de 2 prints reais (INTERSTELLAR 09/07: Players 63/105, Re-Entries 16;
HIGHROLLER 02/07: Players 23/56, Re-Entries 11, Average 48 695): o lobby WN traz tudo —
e o **2º número de «Players X / Y» já inclui as re-entradas** (o `Registered players`
do TS não as conta e por isso subcontava — 22c). Auto-validação: 56×20 000=1 120 000 vs
48 695×23=1 119 985. Em 205 prints reais: ~95% batem a <0,01%; os desvios existentes são
todos no sentido impossível (avg×restantes > entradas×stack) = misread do average.

- **Escolha do print:** RUNNING mais tardio; senão o mais tardio de todos.
- **Total = entradas totais × starting stack.**
- **Guarda «não-desce»:** entradas nunca descem; se descerem = misread → usa o máximo
  + por-rever (0 casos nos 205 prints).
- **Incoerência do average = SINALIZADOR** (`avg_incoherent`), nunca veto às entradas.
- **«Fichas provisórias»** quando o escolhido não é RUNNING (late-reg/desconhecido).
- **Histórico (opção A):** estado irrecuperável (imagens não guardadas) → mais tardio +
  «estado desconhecido»; **SEM inferência** de fecho por entradas estáticas.
- **Re-Entries INFO-ONLY:** guarda-se (vision_json + coluna `re_entries` no torneio),
  sem consumidores, sem avisos, fora das contas.

**Sem conflito com o provado:** «quantos restam nesta mão» (por-mão, régua 22 Jul) fica
intocado; a regra só toca no total por-torneio (`structures.chips`) — que era definido
por **acidente de ordem de processamento** (último print processado ganhava: HIGHROLLER
02/07 ficou com 819 984 do print das 21:33 quando o das 22:28 dá 1 120 000).

## 2. As 5 fases (código)

- **F1 — Vision** (`lobby_vision.py`): prompt ensina a palavra de estado WN
  (`LATE REGISTRATION`→reg_open=true; `RUNNING`→false — antes: 146/222 prints WN sem
  valor) + campo novo `re_entries` (WN-only). Campos opcionais; GG intocada.
- **F2 — Regra num sítio só** (`services/lobby_chips_rule.py`, novo):
  `compute_wn_total_chips(prints)` pura; consumida pelo live
  (`process_lobby_message`), reconcile (`reconcile_lobby_logs`) e recálculo. Helper
  `_wn_chips_meta_for` junta os prints do log + o corrente; `_apply_wn_chips_to_blob`
  sobrescreve `structures[0].chips`. `upsert_payout` ganha `wn_chips_meta` opcional —
  **None = SQL legado byte-a-byte** (GG/PS); com meta escreve 4 colunas novas
  (`chips_rule_state`, `chips_provisional`, `chips_review`, `re_entries`) via
  `ensure_tournament_payouts_schema` (ALTER ADD IF NOT EXISTS).
- **F3 — Registo Re-Entries:** automático no vision_json + coluna info-only (F2).
- **F4 — Retroativo com ENSAIO:** `wn_chips_recalc(dry_run)` em `lobby_sync` +
  endpoints `GET /api/lobbys/wn-chips/preview` (read-only) e
  `POST /api/lobbys/wn-chips/apply {confirm:true}` (preserva `source`; D11:
  manual/backoffice intactos). **Nada se aplica sem a aprovação do Rui sobre a tabela.**
- **F5 — Automático:** é o F2 ligado ao live+reconcile — cada print WN novo passa pela
  regra.

## 3. Ensaio F4 (tabela dos 96 entregue ao Rui — gerada com o módulo novo, read-only)

96 torneios WN com payout · **85 inalterados** (<0,5%) · **11 corrigem**:
INTERSTELLAR 13/07 **+140,2%** (1,84M→4,42M; o blob tinha o print das 19:04 com 92
entradas) · HIGHROLLER 02/07 +36,6% · INTERSTELLAR 08/06 +12,8% · HIGHROLLER 13/07
+10,1% · GRAVITY 11/07 +5,9% · ZENITH 02/07 −2,6% (avg misread) · GRAVITY FUNDAY +1,9% ·
PRIME TIME 13/07 +1,7% · ODYSSEY 13/07 −0,6% · SS-EXPLORER 14/06 −0,6% · PRIME TIME
22/06 −0,5%. **0 solves feitos nos 11** (os 22 solves WN vivem todos em torneios com
desvio +0,0%) → **sem re-solves**. 4 torneios com sinalizador `avg_incoherent` (o blob
antigo tinha o valor inflacionado do misread).

## 4. Correções de rota apanhadas durante a construção (registadas com prova)

- O campo do estado chama-se **`reg_open`** (não `registration_open`) — a afirmação de
  ontem «estado vazio em 100%» estava errada por chave trocada na query: o real é
  70 true / 6 false / 146 sem valor em 222 prints WN.
- A 1ª versão da regra usava a coerência do avg como VETO ao print → escolhia prints
  velhos (ODYSSEY 13/07: 248 entradas em vez de 433). A física certa: **as entradas não
  usam o avg** e são monotónicas → guarda «não-desce» + avg como sinalizador.

## 5. Testes

`tests/test_wn_chips_rule.py` (novo, 18 testes): regra pura (escolha, não-desce,
sinalizadores, provisórias, opção A sem inferência, re_entries, tz mista) + ligação por
sala no live e no reconcile (WN sobrescreve chips; **GG byte-a-byte**: blob tal-e-qual,
`wn_chips_meta=None`) + recálculo (dry-run nunca escreve; apply preserva `source`).
Gate do Info 8/8 continua verde. Suite completa corrida no fecho.

## 6. F4 APLICADO (mesma tarde — tabela dos 96 aprovada pelo Rui)

Sequência com prova fresca em cada passo: preview pelo endpoint em produção (96
torneios, 11 ≥0,5% — bate com a tabela aprovada) → `POST /wn-chips/apply
{confirm:true}` → **updated=96** → query directa à base:

- **11 corrigidos assentes no blob** (ex.: INTERSTELLAR 13/07 4 420 000 = 221×20k,
  print 21:13; HIGHROLLER 02/07 1 120 000 = 56×20k, print 22:28) — **sources
  preservados** (`file_lobby_vision:`/`reconcile_lobby_vision:` intactos).
- **96 com estado assente:** 4 `running`/finais · 29 `late_reg` + 63 `unknown` = 92
  provisórias · 4 por-rever `avg_incoherent` (1121066627, 1128275325, 1124139158,
  1134433883).
- **22 solves WN done intactos** (último carimbo 13 Jul — o apply só toca
  `tournament_payouts`).
- **0 linhas não-WN** com colunas da regra preenchidas (GG limpa).
- **Nota honesta — `re_entries` vazio no histórico:** os 205 prints antigos foram
  lidos antes de o campo existir no prompt e as imagens não são guardadas → a coluna
  fica NULL nos 96; preenche automaticamente daqui em diante.

**Decisão de 22 Jul («TS Winamax não se importa») ENCERRADA e assinada pelo Rui** —
`REGISTO_CONCEITO 2026-07-24 (b)`: a fonte do total de fichas WN é o print de lobby
pela régua nova; o TS WN continua sem se importar (parser dormante desde 29 Mai).

## 6b. Investigação «SS órfã pós-HH» + causa real do re-Vision (madrugada 24→25)

**Caso da captura BATTLE ROYALE (id 3130):** não havia defeito — a HH entrou às 00:16
e o varredor de 20 min curou-a sozinho às 00:35:14 (vigiado ao minuto); o Rui olhou
dentro da janela. Mapa completo do re-scan (todas as ordens de chegada HH/imagem/lobby
cobertas em desenho) entregue com prova por requisito.

**Causa REAL do «re-Vision morto» (3 conclusões minhas de ontem corrigidas):** não
estava morto, não era env (o Rui provou: a variável nem existe), e a data era outra
(última actividade 15/07 02:48, não 14/07 03:52). O defeito: o retry re-processava a
CÓPIA comprimida com hash próprio → criava rows NOVAS (falhadas e gémeas de sucesso)
em vez de curar a original; quando cada presa passou a ter uma gémea `success` com o
hash exacto do seu produto, o dedup «sucesso prévio» engoliu todas as releituras
seguintes em silêncio — para sempre, sem exceção nenhuma. **Prova 5/5:** o sha256 do
img descodificado de cada presa bate numa gémea success (1256→2294 · 1270→2293 ·
2326→2359 · 2392→2425 · 2492→2525), todas já casadas com mãos reais. Lição dupla
para o rasto: updates movem rows para o fim do heap (a «prova» de que o gatilho não
correu era fraca) e daemons sem rasto persistente = cegueira.

## 6c. Os 5 fixes do re-scan (aprovados e construídos, 25 Jul madrugada)

1. **#REVISION-CURE-IN-PLACE** — o retry do varredor passa a usar `_reprocess_failed_row`
   (a primitiva in-place do endpoint manual): cura NA PRÓPRIA row, zero gémeas novas.
2. **#SWEEP-TRACE** — tabela `sweep_runs` (kind arranque/tick/import:razão + contagens
   por passo + erros); escrita pelo `sweep_pending` e pelo `trigger_import_reconciles`.
3. **Tecto de releituras** — `attempt_count ≥ 5` → `revision_exhausted` («precisa do
   ficheiro original»), sai do lote sem gastar Vision; visível como falha na Saúde.
4. **#SWEEP-ROW-ISOLATION** — try por-linha no `reconcile_table_ss` (uma row podre já
   não derruba a passagem; contador `errors` no retorno).
5. **#REVISION-TWIN-CLEANUP** — `POST /api/table-ss/revision-twins/cleanup` (dry_run
   por defeito): famílias por filename COM prova da relação gémea (decode-hash);
   keeper = upload original; cura do dador + reaponta `hands.context_table_ss_id` +
   apaga gémeas. **Aplicação só após aprovação do Rui sobre o ensaio.**

Testes novos: `test_revision_cure.py` (13). Via GG intocada (re-scan é partilhado;
matchers por sala não foram tocados).

## 7. Régua do «início de dia» 15h→12h + `--day` no .bat do HM3 (mesma tarde)

**Parte 1 (régua):** contagem prévia obrigatória = **0 mãos 2026 entre 12h00–14h59**
(vazio real 09:45→16:24) → mudança indolor. Constante nomeada `GAME_DAY_START_HOUR=12`
nos DOIS pontos de código (`app_import.py` janela de imagens; `import_health.py` Saúde
do Import) + textos (bats, JSX, config, appmaster) + testes (39/39 verdes em 12:00).
Nota registada: houve 1 mão a acabar às 09:45 (o corte continua folgado).

**Parte 2 (.bat HM3):** menu novo com dia específico (`DD/MM`) → `--day` no
`hm3_export.py`, janela `[dia 12h Lisboa, dia+1 12h[` **convertida Lisboa→UTC
DST-aware** (ZoneInfo). **Prova do fuso:** HM3 guarda `handtimestamp` em UTC —
42/42 mãos do ZENITH FUNDAY casadas com a app a **+1h00 exacto**. **Prova da
sessão inteira:** `--day 12/06` → 77 mãos, **52 antes da meia-noite + 25 na
madrugada de 13/06** (última 01:01 Lisboa). O 12/07 afinal não atravessa a
meia-noite (última 22:45) — a prova da madrugada fez-se com 12/06. Guardas do
CLI: `--day`+`--days` → erro; data inválida → erro claro. Detalhe apanhado na
prova: um print novo com «→» rebentava na consola cp1252 do `.bat` — trocado
por `->` antes de fechar. **Fix pós-entrega (1º uso real do Rui):** o teste do
`/` no menu usava `findstr "/"` — o findstr lê padrões iniciados por `/` como
OPÇÕES («Bad command line») e o `19/07` caía no ramo dos dias. Trocado por
substituição de string do próprio batch (`%ESCOLHA:/=%`); 3 ramos re-provados
com harness seco; classe varrida (os findstr do limpa_scratch.bat estão bem).
