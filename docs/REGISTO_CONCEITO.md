# REGISTO DE CONCEITO — alterações ao conceito da app e aos seus elementos

Registo **cumulativo, append-only**. Sempre que uma sessão **altera o CONCEITO da app
ou um dos seus ELEMENTOS** (uma regra de negócio, uma lei, a semântica de um conceito,
uma fonte de input, uma decisão de arquitectura), **acrescenta aqui uma linha** com
**data + resumo + motivo + referência** (ficheiro:secção / journal). Não substituir
entradas antigas — só acrescentar (o histórico é o valor).

**Regra de manutenção (FLUXO):** isto é **checklist obrigatório de fecho de sessão** —
quem mexeu em conceito/elemento e não apendou aqui, não fechou a sessão.

Formato: `- AAAA-MM-DD — <resumo numa linha>. **Porquê:** <motivo>. → <referência>`

---

## 2026-07-01 (Web pt97)

- 2026-07-01 — **O hash GG SEGUE o jogador ENTRE MESAS dentro do torneio (provado, reforça o invariante).** Não é só "fixo dentro do torneio": o mesmo hash = a mesma pessoa mesmo mudando de mesa e posição (Daily Hyper $80 tn 293321688, confirmação visual do Rui). **Desmascara** o falso "GG re-embaralha hashes entre mesas" (o Daily Main deu 0 cruzamentos por ninguém ter seguido o Rui, não por re-embaralho). **Porquê:** a propagação de nome por hash funciona no torneio TODO (~3 mãos tagadas por confirmação; −66% do trabalho). → `DESANON_ANATOMIA §3.3`.
- 2026-07-01 — **Nome do ficheiro: GOLD é premium, IT é frágil (corrige a nota antiga).** GOLD (replayer): TM casa 1:1 EXATO + hora=jogo UTC (−1h vs Lisboa) + blinds em milhares — FIÁVEIS. IT (Intuitive Tables): número NÃO fiável (captura segundos depois; multi-tabling → mão vizinha; só ~72/244 discordâncias são trocas reais). **Decisão pt73 (casar IT por hand-id do nome) ABANDONADA.** Regra do Rui: "quando há Gold, casa-se por aí"; onde o IT falha, descarregar Gold. **Porquê:** o texto antigo dizia "blinds/hora não fiáveis / só o hand-id presta" — isso vale só para o IT. → `CLAUDE.md` (Pipeline SS↔HH passo 1/3); `DESANON_ANATOMIA §2`.
- 2026-07-01 — **Tags: fonte única canónica + 2 regras de incompatibilidade (seladas).** GG e HM3 convergem num vocabulário pequeno ((tipo de spot)×(fase) + nota + resíduos); `pos-nko` = pós-flop Non-KO VANILLA (não "PKO posição"). R1 FORMATO (PKO não coexiste com não-PKO) e R2 FASE (base não coexiste com a sua FT); neutras nota/Timetell/For Review. Fonte única `services/tags_canonical.py` (une normalize+IT_FOLDER_TAGS+aliases+family; normaliza na escrita). Regras definidas mas **ainda não enforçadas** (só sinalizadas na Saúde GG). **Porquê:** o vocabulário estava disperso por 4 sítios e havia grafias tortas; unificar sem partir os gates que lêem normalizado. → `docs/TAGS_CANONICO.md`.
- 2026-07-01 — **Fluxo de tagging do Rui (documentado).** HM3 (WN/WPN/PS): taga dentro do HM3; appimport puxa as tagadas. GG/CoinPoker: taga por PRINT + PASTA (a subpasta = a tag → `discord_tags`). Na GG tag e captura entram sempre juntas → captura trocada leva tag+nomes para a mão errada. **Porquê:** era conhecimento tácito não escrito. → `REGRAS_NEGOCIO §1.4`.

## 2026-06-18 (pt75)

- 2026-06-18 — **Desanon GG por POSIÇÃO (`position_v3`) é o método primário** para gold images: nome+sigla→cadeira via HH, **sem aritmética de stack**. Stack-elimination passa a fallback (entries sem sigla) + continua nas 185 table-SS. A **ordem deixou de importar** (HH-primeiro e imagem-primeiro dão ambos `position_v3`). **Porquê:** a posição é sinal fiável; o stack troca vilões de stacks próximos (bug §3.2). → `DESANON_ANATOMIA §3.2.2`; `JOURNAL pt75`.
- 2026-06-18 — **A "gold image" (descarga completa da mão pelo replayer GG) é a fonte das siglas de posição + a outra metade do registo.** A GG **não desenha rótulo por seat**; as siglas vêm do **LOG DE ACÇÃO** da imagem (transcrever o crachá, nunca inferir por stack/ordem). **Porquê:** funda o `position_v3` e confirma que a imagem tem de ser persistida. → `DESANON_ANATOMIA §2.1/§3.2.2`.
- 2026-06-18 — **A desanon por stack/feltro mostra-se como "por verificar", não como certa.** Mãos GG desanonimizadas por stack (`table_ss`/`anchors*`/`mtt_*`) levam aviso ⚠ "Nomes por verificar" (campo derivado `deanon_status`); só `position_v3` é "verificada". **Porquê:** o stack pode trocar vilões de stacks próximos — sinalizar é mais honesto que mostrar um nome possivelmente errado como certo. → `MAPA §2.1`; `JOURNAL pt75`.

## 2026-06-10 (pt64–pt67)

- 2026-06-10 — **LEI Max Players (HRC)** = **span posicional âncora→BB, teto 6** (não a contagem de participantes). **Porquê:** o HRC Hand Mode tem de refletir os jogadores entre a decisão e a BB; subcontava com herói tardio. → `REGRAS_NEGOCIO §15`; `JOURNAL pt67`.
- 2026-06-10 — **LEI da âncora da 2ª run** = **LEI B: âncora = a POSIÇÃO certa; a linha é indiferente**. **Porquê:** o Selected Subtree recalcula o ponto de decisão inteiro da posição (qualquer linha dá o mesmo âmbito) → o off-by-one within-bucket é inofensivo; o que importa é acertar a posição. → `REGRAS_NEGOCIO §16`; `HRC_ANATOMIA §14`.
- 2026-06-10 — **Semântica do Selected Subtree do HRC** (descoberta, fonte = 3 fotos do Rui na 4ª volta): recalcula o **ponto de decisão INTEIRO da posição-âncora + tudo a JUSANTE; posições a MONTANTE congeladas**. Estratégia = distribuição mista única → redistribui massa por todas as linhas da posição. **Porquê:** funda a LEI B e corrige a leitura errada "irmãos disjuntos". → `HRC_ANATOMIA §14.2`.
- 2026-06-10 — **Regra dos sizings por stack efetiva** confirmada como lei do Rui: raise non-all-in com **eff ≤ 25 BB → emite `[size, ALLIN]`**; **eff > 25 BB → só `[size]`**; open "pequeno" 2 BB só com eff > 8 BB e não-blind. **Stack efetiva** = `min(stack do raiser, maior stack adversário activo) / BB`, por acção. **Porquê:** condiciona a estrutura de betting gerada para o HRC. → `hrc_script_gen.py:51,60`; `HRC_ANATOMIA §14`.
- 2026-06-10 — **Bounty mode** passa a vir da **estrutura importada** (deixa de forçar PKO 50% no robot). **Porquê:** suportar PKO 75/40, Super KO, etc. sem hardcode. → `JOURNAL pt66` (fix d); `HRC_ANATOMIA §13`.
- 2026-06-11 — **FEATURE FUTURA (decidida; desenho adiado): AVALIAÇÃO AUTOMÁTICA do Herói vs HRC.** Ao receber o zip de estratégias de uma mão, a app localiza na árvore o(s) nó(s) da linha real onde o herói decidiu, lê a estratégia do solver para o **combo exacto** do herói (frequências + EVs por ação) e **compara com a ação tomada** → veredicto (em linha / mix / desvio) + **custo em EV**. Output por mão no Estudo + badge/filtro "mãos onde desviei". **Porquê:** fecha o ciclo `mão → solver → avaliação` — o propósito de estudo da app. Constrói sobre a infra do GTO Brain (matching árvore + navegação a nó). → `PENDENTES`.
- 2026-06-11 — **PRINCÍPIO (decidido, a construir): a fila HRC nasce FECHADA; o watcher/adapter NUNCA recebe mãos por iniciativa própria.** O servidor só serve mãos a `GET /api/queue/hrc` após um **DISPARO MANUAL** do Rui (botão na página HRC Queue). Fecha sozinha quando o lote libertado é consumido. **Porquê:** a "fila travada" era só procedimental (`#QUEUE-NO-SERVER-SIDE-GATE`); este gate torna-a real e sob controlo do Rui. Desenho em `PENDENTES`. Entra em vigor quando construído (a corrida de 11 Jun à noite mantém-se).
- 2026-06-11 — **O Intuitive Tables MUDOU o formato de gravação dos SS a meio do percurso.** Antigo: `ShotN-<Site>-<timestamp>.png`. Atual: nome longo com torneio/blinds/mesa (`<Site>-<Título> - Blinds … - Table …(#tn)…-YYYYMMDDHHMMSS-NN.png`). Os **323 SKIP** do dry-run pt68 são o **formato ANTIGO** (ciclo velho + sessão de dia 4) — **não é regressão da app** (o router salta o que não tem a cauda nova). → `FLUXO §11`, routing em `tools/appimport/app_import.py`.
- 2026-06-11 — **Âmbito de disco no PC principal = só os paths listados** (intocáveis + `Batmen\`); procura fora exige autorização escrita do Rui. **Porquê:** o Code varreu pastas fora do âmbito (pt68) à procura da GG. → `FLUXO §11`, `CLAUDE.md` (ÂMBITO DE DISCO).
- 2026-06-11 — **HH/TS do GG vivem no BACKOFFICE do Rui (fora do PC)** — ele descarrega e coloca à mão em `gg_hh`/`gg_ts`; nunca procurar no disco o que é do ritual manual dele. **Porquê:** corrige o entendimento de que a GG estaria nalgum path local. → `FLUXO §11`.
- 2026-06-11 — **Desktop do Beelink (`riand`) = ZONA LIMPA** (só o atalho do HRC + o que o runbook listar). Runtime vive em `HRCWatch\` (watcher), `C:\hrc\adapter\` (adapter), packs (templates). O `instala_ptXX.bat` passa a varrer `*.bak`/`*.exe.backup-*` além de `*.exe`. **Porquê:** 3+ exes-backup renomeados (~13 MB) sobreviveram no Desktop por extensão → violam a regra «1 só exe». → `CLAUDE.md` (REGRA PERMANENTE — 1 só watcher exe).
- 2026-06-11 — **Regra dos 25 BB CONFIRMADA pelo Rui** + stack efetiva = `min(stack raiser, MAIOR adversário activo)/BB` por acção. **Porquê:** desbloqueia o fix do `#HRC-NODE-OFFSET-IMPLICIT-LINES`. → `REGRAS_NEGOCIO §17`.
- 2026-06-10 — **Cap de upload de resultados HRC = 200 MB (INTERINO)**. **Porquê:** árvores Max=5 (Complete Export = árvore inteira) chegam a ~112 MB e batiam no cap de 50 MB (413). Arquitectura **definitiva em aberto** (A/B/C: chunked/compressão/poda — 72×112 MB≈8 GB BYTEA é insustentável); rede de salvação manual = `/hrc-sessions` (`/import`, sem cap). → `queue.py:53`; `#HRC-RESULT-ZIP-413`.
- 2026-06-11 — **LEI do Rui §18 (sizing de opens das blinds + 3-bet da BB + raise sobre opens all-in)**, dita em pt70. Tabela de open da SB por eff (`8<eff<20→2.5×`, `20≤eff<31→3×`, `31≤eff≤100→3.5×`, `>100→4×`; BB sobre limpers = mesma — assunção Web); tabela de 3-bet da BB vs open (`2.5x→2.1×`, `3x→2.5×`, `3.5x→2.7×`, `4x→3.3×`); 3-bet sobre open ALL-IN **existe** e segue a eff do 3-bettor (vs vivos não-all-in): `≤25→[ALLIN]`, `>25→iso 2.5×o-all-in` (multiplicador proposto, a confirmar); ordem **sempre `[size, ALLIN]`**. **Porquê:** busca inversa pt70 destapou 2 bugs reais — "ponto 5" (SB-shove `8<eff≤25` saía `["ALLIN"]` sem size) e a fórmula do 3-bet sobre all-in (`2.3×shove` com eff colapsada). §17 mantém-se intocada. → `REGRAS_NEGOCIO §18`; `hrc_script_gen.py`; `#QUEUE-BETTING-SCRIPT-BUG`.
- 2026-06-12 — **Futura encomenda do Rui — "Mestre único" de import** (registo de conceito; ainda sem código): um **duplo-clique** orquestra de ponta a ponta — HM3 + **appimport ao-vivo (com confirmação)** + Discord sync + rematch + **abrir a Saúde do Import**. Associado: o **Intuitive Tables passa a gravar direto em `Batmen\it`** por definição própria (config da app, **sem código**). **Porquê:** colapsar o ritual manual de fim-de-sessão num só gesto + tirar a cópia manual do IT do caminho. → `PENDENTES` (fila pt71, item 8); `tools/appmaster` (bat-mestre existente é o embrião).

## 2026-06-13 — Desanonimização por SS de mesa (table-SS)

- 2026-06-13 — **NOVA fonte de nomes reais para a GG anónima: a SS de MESA do Intuitive Tables.** A Vision do table-SS passa a ler (nick, stack BB, herói) **por banco**; cruza-se com a HH GG anónima (hashes) já ligada à SS (`context_table_ss_id`) pela maquinaria de match banco-a-banco do pt7 (âncora Hero + folds por stack `hh−ante` + atribuição óptima) → a mão ganha `player_names` + `match_method='table_ss'` e entra no **Estudo/Vilões pela porta normal**, sem excepção ao gate. **Porquê:** dar nomes reais às mãos GG capturadas no IT sem entrada Discord (a captura é o sinal de interesse). → `services/table_ss_deanon.py`; `services/table_ss_vision.py` (seats).
- 2026-06-13 — **APROXIMAÇÃO conhecida do table_ss de-anon** (para reconhecer em segundos um futuro nome estranho): a SS casa com a mão GG **mais próxima no TEMPO**, não a mão exacta da captura → stacks com **deriva de algumas mãos**, resolvida pela **ordem relativa** (estável em poucas mãos). **Guarda anti-envenenamento (afinação Web):** ≥2 bancos NÃO-herói **sem stack utilizável** (ALL-IN/null) ficam **POR MAPEAR** (hash mantido) + `player_names.deanon_partial=true` — nunca se atribui por palpite (nome trocado envenena fichas de vilões via tag 'nota'; nome em falta é honesto). **Porquê:** se um nome sair errado numa mão `table_ss`, a causa é esta deriva/ambiguidade. → `services/table_ss_deanon.py:_filter_ambiguous_stackless`; `test_table_ss_deanon.py`.
- 2026-06-13 — **INVARIANTE (observado-e-vigiado): o hash anónimo GG é FIXO por jogador DENTRO do torneio.** Forense Jun-2026 (3232 mãos GG, read-only): 94% dos hashes persistem ≥2 mãos com trajectória de stack coerente; **0 reutilizações cross-torneio** em 1059 hashes; mudança de mesa = o hash **segue** o jogador (fragmentação benigna, stack transita). **Porquê:** funda a **votação cross-mão por torneio** (maioria corrige swaps do per-mão). **Vigiado** na Saúde do Import (`capture_deanon_agreement`: alerta se as maiorias colapsarem). → `services/table_ss_deanon.py` (`vote_tournament_maps`/`reconcile_tournament_deanon`); `JOURNAL pt71 §C`.
- 2026-06-13 — **Votação cross-mão = camada de consistência canónica do table_ss de-anon.** Cada captura nova de um torneio acrescenta um voto e pode **retro-corrigir** mapeamentos anteriores (coerência a jusante: `hand_villains` da mão são limpos + regras re-disparadas com o nick votado). Empate/sem maioria → por mapear (`deanon_partial`). **Porquê:** o per-mão sozinho troca vilões de stacks próximos; a maioria por torneio resolve. → `reconcile_tournament_deanon`.
- 2026-06-13 — **UX: a imagem da captura AMPLIA ao clicar** (lightbox: clica→grande, Esc/fora→fecha), em toda a app por consistência (`ZoomableImage`; Estudo/detalhe já usavam `ImageGalleryPopup`). **Porquê:** ler os nicks da SS para confrontar com os nomes mapeados. → `components/Lightbox.jsx`.

## 2026-06-14 — Replayer GG descontinuado + pasta-como-tag (pt72)

- 2026-06-14 — **O replayer-image GG está DESCONTINUADO como fonte de desanon.** A GG migrou a página partilhada da pokercraft de server-rendered (com `og:image`) para **SPA Angular sem `og:image`** → o fetch+regex (único mecanismo que sempre existiu; nunca houve browser-capture) extrai 0 em TODAS as idades (confirmado por fetch real maio→junho-13 + inspeção do HTML pelo Rui). A via screenshot/headless foi **investigada e descartada** (pesada, frágil, ToS pokercraft **não-verificado**). **Porquê:** a desanon GG faz-se agora **SÓ por table-SS do IT**; o pipeline replayer (Vision sobre `og:image`) fica morto. → `JOURNAL pt72 §A/§B`; `#REPLAYER-OGIMAGE-DEAD-SPA`.
- 2026-06-14 — **A PASTA de captura do IT passa a SER a tag (substitui o canal Discord, que morreu com o replayer).** O Rui configura o IT para gravar cada captura numa **subpasta de `Batmen\it`** escolhida no momento (ICM / ICM PKO / NPKO Pos / PKO Pos / …); o appimport lê a subpasta de origem → injecta a tag correspondente (tabela de tradução EXTENSÍVEL, a validar pelo Rui) em `discord_tags`, pela porta normal (`apply_villain_rules` + transições de Estudo/Vilões). As capturas continuam mesa/lobby por NOME; a subpasta acrescenta a TAG. **Porquê:** com o replayer morto, a tag (família ICM/PKO + street) já não chega pelo canal Discord — a escolha de pasta do Rui é o novo portador, manual e fiável. **Supersede** o "classificador por Vision-coroa" investigado antes (o Rui decide a família ao escolher a pasta; a app não precisa de ler a coroa). FT (`-ft`) pode ser acrescentado pela app (`len(seats)==players_left`), opcional/prudente. → `JOURNAL pt72 §E/§F`; `tools/appimport/app_import.py`; `routers/table_ss.py`; `routers/capture_triage.py` (`ALLOWED_TRIAGE_TAGS`).

## 2026-06-16 — Match captura-IT ↔ mão GG por hand-id do filename (pt73, DECIDIDO / POR IMPLEMENTAR)

- 2026-06-16 — **Match captura-IT ↔ mão GG (Pergunta 1: QUAL é a mão) passa a ser PRIMARIAMENTE por HAND-ID extraído do nome do ficheiro** — o número TM imediatamente antes do timestamp (ex.: `...6081471864-20260615223557-127.png` → `GG-6081471864`). Determinístico, sem Vision, sem tempo, sem stack. Confirmado nas HH (`#TM6081471864`, `#TM6079987069` batem exactamente). **FALLBACK:** torneio+mesa+tempo SÓ quando o ID falta no nome (formato antigo, ex.: `...Table 171_!)-20260605201233-19`). **SUBSTITUI** o desenho anterior (entrada 2026-06-13 "casa com a mão GG mais próxima no TEMPO" + nome/fingerprint do `PLAN_2026-06-02`), que era **frágil**: a captura é tirada **segundos DEPOIS** do início da mão → em multi-tabling (média 3.2 torneios GG concorrentes na janela ±5min, até 8) a hora sozinha erra a mão por segundos. **Porquê:** o hand-id é determinístico e mata o multi-tabling de raiz; a "deriva de algumas mãos" da entrada 2026-06-13 deixa de existir para P1. **ESTADO: DECIDIDO / POR IMPLEMENTAR — ainda NÃO no código** (não escrever como já-existente). → `DESANON_ANATOMIA.md §2.2`; `routers/table_ss.py` (`compute_table_ss_match`, `parse_table_ss_filename`); `#TABLE-SS-GG-MULTITABLING-MATCH`.
- 2026-06-16 — **Esclarecimento P1 vs P2 (a NÃO confundir):** o hand-id resolve **só a Pergunta 1 (QUAL é a mão)**. A **Pergunta 2 (QUEM senta em que cadeira)** continua SEPARADA e **NÃO resolvida** por isto — o hand-id não traz nicks. P2 é hoje por **stack** (âncora Hero + permutação de stacks), o que causa o **bug dos vilões em cadeiras trocadas**: **detectado visualmente pelo Rui** (`img/89`, `GG-6042783089`) → **scan de fit** (stacks Vision vs HH) corrido por Code sobre a BD pt73 → **66/185 misfit, 6 wrong-hand flagrante, 119 fiáveis** (o 36% é do scan, não estimativa). **Âncora futura de P2: disco do dealer/botão (a especificar pelo Rui) — NÃO fechada.** **Porquê:** registar explicitamente para não criar a ilusão de que o hand-id corrige os vilões trocados. → `DESANON_ANATOMIA.md §3`; `services/table_ss_deanon.py`.
- 2026-06-16 — **PRINCÍPIO da desanon — a HH é a fonte de verdade da ESTRUTURA; a imagem é a OUTRA METADE (e tem de ser persistida).** A HH GG é exacta para posições (SB/BB/BTN/Hero), stacks, ações e ordem; tem **exactamente dois buracos** — (1) vilões em hash (o Herói vem como `Hero`) e (2) **não traz tags** (ICM/PKO/pos-pko/nota). A **imagem é a única fonte de ambos**: a **tag vem da imagem do IT** (a subpasta de captura = a tag) e os **nicks reais vêm da imagem** (IT e/ou replayer). Logo a imagem é a outra metade do registo da mão e **perdê-la = perder tags + nicks para sempre** → **TEM de ser persistida** (o table-SS já guarda `img_b64`; a lane do replayer tem de garantir o mesmo). **Porquê:** funda a regra de persistir a imagem e enquadra P2 como alinhamento (ver entrada seguinte). → `DESANON_ANATOMIA.md §3.2.1`; `PENDENTES` (lane importador replayer).
- 2026-06-16 — **★ Âncora de P2 FECHADA (desenho; substitui o "a especificar pelo Rui" da entrada acima e o §3 "NÃO fechada").** P2 é **ALINHAMENTO, não estrutura**: a HH já conhece todas as cadeiras/posições com certeza; a imagem só dá os nicks; alinha-se a imagem à HH para cada nick aterrar na cadeira (hashed) certa. Uma troca é erro de **alinhamento de nomes**, nunca de estrutura. **Âncora = TRÊS critérios corroborantes** (legíveis na imagem E na HH): (1) **SB+BB** primária (campo próprio dos dois lados; duas cadeiras fixas adjacentes); (2) **botão do dealer** confirmação, via **invariante BTN→SB→BB são três cadeiras seguidas** (botão encostado à SB, do lado oposto à BB); (3) **Herói** referência fixa (centro-baixo + nick; `Hero` na HH). Até 4 cadeiras conhecidas sobre-determinam a roda; o **stack desce a desempate final**. **Corroboração obrigatória:** os critérios têm de concordar; algum não bate → **NÃO atribuir → "por mapear"** (nome em falta é honesto, nome trocado é veneno); a HH fica intacta. **Borda:** heads-up (BTN=SB) à parte. **Novo vs existente:** Herói já é âncora; **SB/BB existiam (pt7) mas o table-SS salta-as = raiz do bug → REPOR**; **botão como confirmação pela invariante = NOVO**. **ESTADO: desenho fechado, IMPLEMENTAÇÃO PENDENTE** (não escrever como já-existente). **Porquê:** acabar com a permutação por stack que troca vilões de stacks próximos. → `DESANON_ANATOMIA.md §3.2.1`; `PENDENTES`; `services/table_ss_deanon.py`.

## 2026-06-23 — Discord descontinuado como entrada; `discord_tags` = coluna partilhada alimentada pela pasta do IT (pt86)

- 2026-06-23 — **O Discord está DESCONTINUADO como pipeline de ENTRADA de mãos (não se sincroniza).** Morreu com o replayer-image GG (`#REPLAYER-OGIMAGE-DEAD-SPA`, pt72): sem `og:image`, o Discord deixou de servir para puxar mãos/replayers GG. O **bot/sync/cross-post ficam DORMENTES no código, intactos** (decisão do Rui — não renomear, não apagar; podem voltar a ser precisos). **Porquê registar:** as tabelas "fontes de input" do `CLAUDE.md` (linha "Discord (canais estudo)") e várias secções do `VISAO_PRODUTO` ainda descrevem o Discord como fonte ativa — passam a ler-se como **históricas** (banners adicionados, sem apagar o texto). → `CLAUDE.md` (`## Modelo de domínio` + `## Bot Discord`); `VISAO_PRODUTO` (banner + anotações nas secções Discord/cross-post/pipeline diário).
- 2026-06-23 — **A coluna `hands.discord_tags` NÃO está morta — virou a COLUNA PARTILHADA das tags de estudo, agora preenchida pela PASTA do Intuitive Tables.** `_apply_folder_tag_to_hand` (`table_ss.py`) escreve o nome da subpasta de captura em `discord_tags` + dispara `apply_villain_rules` — exactamente a porta por onde o canal Discord escrevia. A **pasta `nota` do IT = o antigo canal `nota`**. O nome `discord_tags` é **histórico e enganador**, mas **mantém-se por decisão do Rui** (não vale o refactor para `study_tags`/`tags_canal`). **Supersede** o portador-de-tags: antes = canal Discord; agora = subpasta do IT (continuação directa da entrada 2026-06-14 "a PASTA passa a SER a tag"). **Porquê:** documentar que a coluna é viva e central, para ninguém a tratar como legado morto nem partir as regras que a leem.
- 2026-06-23 — **As regras que leem `discord_tags` continuam VIVAS e corretas — recebem as tags do IT pela mesma porta; NÃO TOCAR.** Regra C dos vilões (`villain_rules.has_nota_intent`), gate de mãos (`hand_service`), equity model HRC (`queue_export._derive_equity_model`), desanon (`table_ss_deanon`). **Porquê:** o nome enganador da coluna podia levar alguém a "limpar" estas leituras julgando-as mortas — são o consumo real das tags do IT.
- 2026-06-23 — **Modelo de domínio da ENTRADA GG (actual):** **mãos** = import **manual** de ZIP/TXT HH (`origin='hh_import'`, anonimizadas); **desanon (nomes reais)** = **SS de mesa do IT**, match por **hand-id do nome** do ficheiro (`#TABLE-SS-...`, ver `DESANON_ANATOMIA §2.2`); **tags** = **pasta do IT** (nome da pasta → `discord_tags`); **lobbys** = **SS de lobby do IT**. **O HM3 é só WN/PS/WPN — não GG.** **Porquê:** consolidar num sítio o retrato pós-Discord, já que o GG deixou de ter qualquer entrada por Discord. → `CLAUDE.md` (`## Modelo de domínio`); `VISAO_PRODUTO` (banner).

## 2026-06-23 — Sizes do HRC: ALLIN implícito 25/30 + #HRC-NODE-OFFSET-IMPLICIT-LINES fechado + âncora cai na raiz (pt86)

- 2026-06-23 — **REGRA CANÓNICA DOS SIZES (ALLIN implícito nos opens): stack INDIVIDUAL ≤ 25 BB geral, ≤ 30 BB só em blind-vs-blind (na tabela de opens = a SB).** Vive no template `mtt_advanced_canonical_2026.js` (`shouldAddPreflopAllIn`/`isBlindVsBlind`; `STACK_BB_FOR_OPEN_ALLIN_OPTION=25`, `…_BVB=30`). **Supersede** o **30-geral do pt29** (despacho Rui). **Distinto do §17/§18** (que são o ARRAY de sizes do backend, por stack EFETIVA): o §19 é a LINHA ALLIN implícita do template em runtime (stack INDIVIDUAL). **Porquê é seguro (não ressuscita a explosão do pt29):** o pt29 colapsava por stack EFETIVA (um short puxava a efetiva e metia ALLIN em deep 100+BB); o filtro continua INDIVIDUAL, por isso os 100+BB continuam excluídos (100>25), e baixar 30→25 fora dos blinds só REMOVE ALLINs (árvore igual ou menor). **Onde vivem as regras de sizing do HRC:** `REGRAS_NEGOCIO §15` (max players), `§16` (âncora 2ª run), `§17` (sizings por eff), `§18` (blinds/3-bet BB/raise-sobre-allin), **`§19` (ALLIN implícito 25/30, NOVO)**. → `REGRAS_NEGOCIO §19`; `mtt_advanced_canonical_2026.js`.
- 2026-06-23 — **`#HRC-NODE-OFFSET-IMPLICIT-LINES` FECHADO (`8096f3c`).** `count_lines_for_position` (`hrc_node_offset.py`) deixou de contar `len(array)` e passou a **espelhar o template** (§19): por posição, `sizings não-ALLIN abaixo da stack + (1 ALLIN se [ALLIN explícito ∨ stack ≤ limiar ∨ size ≥ stack colapso]) + Complete-SB`; limiar **SB→30 / resto→25**; stack individual via `derive_position_stacks_bb` (= `getChipsRemaining/BB`); passada por `compute_target_node_offset`. **Mão de teste `GG-6084189514`** (8-max, agg CO): UTG 15.95 / UTG1 10.96 (≤25 → 2 linhas) → offset **4 (cai em MP, errado) → 6 (cai em CO, o agressor)**. **Escopo real (read-only):** das 45 elegíveis, **12 mudam offset** e **10 movem a âncora de posição** — **bidirecional**: undercount (posições curtas ≤25 antes do agressor) **e** overcount (mesas deep onde o default-2 sobrecontava). **O "521" anterior era da premissa errada** (assumia ALLIN implícito sempre) — **descartado**. `stack_bb=None` mantém o legacy (fallback defensivo; 36 testes antigos verdes). → `TECH_DEBTS pt85–pt86`; `JOURNAL_2026-06-23-pt86b.md`.
- 2026-06-23 — **`#HRC-ANCHOR-FALLS-TO-ROOT-NOT-HERO` (NOVO, aberto).** Sem raise preflop (walk/limp-pot), a âncora do offset cai na **RAIZ** (`positions[0]`, primeira a agir), **não no Hero** — `derive_aggressor_real_action` devolve `None` → `classify_aggressor_source='fallback_root'` → `target_node_offset=0` (`queue_export.py`). A regra "Hero-foldou-unopened → âncora=Hero" existe no `derive_max_players.py` (para o max_players) mas **não** no caminho do offset. **Exposição:** ~**7 mãos / ~10%** das elegíveis. **3 sub-casos:** (a) Hero=BTN → fácil (tem nó de open na tabela; transplanta limpo); (b) **Hero=BB → sem nó de open** na tabela de opens; (c) **pote limpado → a decisão "vs limp" não existe** como nó de open. **Decisão de produto pendente** para (b) e (c). → `TECH_DEBTS pt85–pt86`; `PENDENTES`.

## 2026-06-23 — Âncora no-raise = nó da 1ª decisão do Hero (`#HRC-ANCHOR-FALLS-TO-ROOT-NOT-HERO` Passo 1) (pt86c)

- 2026-06-23 — **REGRA CANÓNICA DA ÂNCORA SEM RAISE: a âncora do `target_node_offset` é o nó que governa a 1ª DECISÃO do Hero** — limp/raise **antes** do turno do Hero → ancora aí (o Hero responde no subtree, LEI B `§16`); **pote por abrir** no turno do Hero → **open do próprio Hero**; Hero **nunca decide** (walk) → **skip**. Estende o caminho "1º raiser" (mãos COM raise), que fica **inalterado**. **Porquê:** o Rui estuda a decisão do Hero — cair na raiz ancorava num nó que não é o do Hero. → `JOURNAL pt86c`; `TECH_DEBTS pt86c`.
- 2026-06-23 — **A moldura "(b) Hero=BB sem nó / (c) vs-limp" do pt86b estava ERRADA — dissolveu-se.** O **complete da SB já é um nó** na Strategy Table (`canFlatCallPreflop` bets==1 → só SB; `offset_within_bucket` SB-complete=0), logo **Hero=BB-vs-SB-limp ancora no Complete da SB** (sem decisão de produto; **0 mãos hoje**, ramo future-proof). As "Hero-primeiro-a-agir" ancoram no **open do próprio Hero** (nó já existe). O **único** caso sem nó é o **limp de NÃO-blind** → `#HRC-ANCHOR-NONBLIND-LIMP` (Passo 2, 1 mão, mexe na árvore). **Porquê:** ler o template + a BD (read-only) desfez a suposição "não há nó". → `JOURNAL pt86c`; `PENDENTES`.
- 2026-06-23 — **Passo 1 IMPLEMENTADO (commit pt86c, sem push; backend-only).** `derive_noraise_anchor` nova em `queue_export.py` (só quando `derive_aggressor_real_action`→`None`; mesmo contrato + `source` noraise_hero_open/noraise_sb_complete) + ramo `noraise_hero` em `build_queue_zip` (computa offset, deixa de forçar 0). **`compute_target_node_offset`, template, watcher e `count_lines` NÃO tocados.** Prova read-only: 5 Hero-open caem na linha do Hero; #3 walk e #7 limp-não-blind → skip. **Validação visual no robot pendente.** → `JOURNAL pt86c`.

## 2026-06-25 — Opens do HRC per-posição (`#GTO-OPEN-SIZE-NOT-PER-POSITION` fechado) (pt89)

- 2026-06-25 — **REGRA CANÓNICA DOS OPENS: per-posição, NÃO bucketed.** Cada posição não-blind tem a sua var `SIZES_OPEN_UTG/UTG1/MP/HJ/CO` (à imagem dos 3-bets de pt42b); o gerador faz override **só** ao bucket do **opener real**, as restantes ficam no default do template `[2]`. Acabou a var partilhada `SIZES_OPEN_OTHERS` como canal de propagação (era o caminho da contaminação: o size real E o ALLIN do opener bleeding para os fundos). **Regra do Rui:** só o opener usa o seu size real; posições à frente usam o standard 2 BB; a SB usa o size de blind; posições atrás (foldadas) indiferentes. **Porquê:** correção para a riqueza dos dados do GTO Brain (um BTN "a abrir 2.5" porque o CO abriu 2.5 era ruído). Commit `90c07ad`, deployed. → `GTO_BRAIN §9`; `JOURNAL_2026-06-25-pt89.md`; `mtt_advanced_canonical_2026.js`/`hrc_script_gen.py`/`hrc_node_offset.py`.

## 2026-06-25 — Guarda OCR do tamanho da tree do HRC (abort preventivo) (pt90)

- 2026-06-25 — **NOVO ELEMENTO no robot: leitura OCR read-only das estatísticas da tree do HRC, com abort preventivo de trees gigantes ANTES da 1ª run.** `tools/watcher_src/tree_stats.py` lê `{nodes, gb, hrc_available_gb}` do painel "Tree Statistics" (PrintWindow `PW_RENDERFULLCONTENT` + `Windows.Media.Ocr`; dual-import winsdk/winrt; nunca lança). Hook no `_wait_for_finish_ready` (após a Fase 2 = tree size já computado): regista as stats no `meta.json` (`tree_nodes/tree_size_gb/hrc_available_gb/build_seconds/solve_seconds/ocr_ok`) e **aborta** (via `.failed` existente) **só** se DUAS leituras OCR seguidas forem `ok` E concordarem que `gb > 15` (`TREE_GB_ABORT_LIMIT`) OU `> hrc_available`. **Porquê dupla leitura e não o tempo de build:** trees gigantes constroem em 2-3s e normais em <1s → o tempo é sinal cego; já um erro de OCR é aleatório e não se repete igual, logo duas leituras concordantes em >15GB são de confiança (discordância/OCR falhado → nunca aborta, fail-open). Shipped no `watcher-gate` (`9609ab6`+`7384ed2`), Release `watcher-pt90` (`.exe` SHA256 `69e741c2…673c`). → `TECH_DEBTS pt90`; `#HRC-TREE-GIGANTE`.

## 2026-07-07 — Convenção do print que marca o ARRANQUE da FT (propagação FT, via 1)

- 2026-07-07 — **CONVENÇÃO CANÓNICA (regra de produto): o print de lobby que marca o ARRANQUE da mesa final é APENAS o que tem a aba "Info" ABERTA** — é onde, no "Table Type", se lê **"N players at the final table"**. Prints do MESMO torneio com outras abas abertas (**Players**, **Prize Pool**, …) são de **fora da FT** ou **já com ela a decorrer** e **NUNCA ancoram a fronteira**. Um torneio pode ter vários prints de lobby na noite → **só o do Info conta como marco**. **Implicação para a Vision (`lobby_vision`):** além do `final_table_size` (o **N**, lido só da aba Info), o print tem de sinalizar **se a aba Info está activa** (campo `open_tab`; a WIP de `lobby_vision` já o tem: `open_tab ∈ {Info, Prize Pool, Other, null}` + `final_table_size` gated ao Info). **Implicação para o motor (`ft_boundary._lobby_ft_boundary`, F2):** a fonte (a) só pode usar como fronteira lobbys com `vision_json.open_tab == 'Info'` (e o N vem daí), **não** o `players_left <= FT_CAP` de qualquer print — a versão actual do motor ainda NÃO faz esta distinção (a corrigir na F2, antes de ligar a escrita). **Porquê:** um print de Prize Pool com poucos jogadores restantes já é FT-a-decorrer (eliminações), não o arranque → ancorava a fronteira tarde/errado. **Controlo negativo real:** Daily Hyper $60 de 2 Jul tem 2 prints — o do **Info às ~19:19 (N=7, 7/172)** = arranque da FT; o das **21:27 (Prize Pool, 169 entradas, 107 restantes)** = edição/momento diferente, **não** pode ser confundido com fronteira nem com a mão errada. → `PENDENTES` (secção FT, via-1); `services/ft_boundary.py`; `services/lobby_vision.py` (WIP `open_tab`/`final_table_size`).

## 2026-07-07 — Papéis dos prints de lobby POR ABA (o print do Info NÃO é fonte de prémios)

- 2026-07-07 — **REGRA DE PRODUTO (Rui): os prints de lobby têm PAPÉIS distintos conforme a aba aberta.** O fluxo normal do Rui é: print(s) com os **prémios visíveis** ANTES/DURANTE o torneio (fonte de `tournament_payouts`), e um print da aba **Info NO ARRANQUE da FT** (sinal da fronteira). Portanto:
  - **Print com `open_tab == 'Info'`** → serve **APENAS** para sinalizar o início da FT (fronteira + `final_table_size`). **NUNCA escreve `tournament_payouts`** — na aba Info os prémios **não são fiáveis** (caso real Daily Hyper $60 2 Jul, print `-101`/19:19:27: leu zeros/fichas + `start_time` 2024, `#LOBBY-VISION-CHIPS-AS-PRIZES`). **Continua** a resolver o `tn` e a gravar `vision_json`/`players_left` (é o que o motor FT lê).
  - **Prints das OUTRAS abas** (Prize Pool/Players/…) → **mantêm o comportamento actual**: fonte de payouts, com as precedências D11.
- **Porquê o gate resolve por DESENHO um bug latente:** o print do Info é **sempre o ÚLTIMO** print do torneio (tirado no arranque da FT) → sem o gate, o seu `upsert_payout` (mesmo tier `file_lobby_vision`, **last-write-wins**, a D11 só protege `manual:`/`backoffice_vision:`) **esmagaria os payouts bons** das abas de prémios **em TODOS os torneios**. Investigação 7 Jul confirmou: o payout bom do 295219051 (escada de 24 lugares, `1998.54…129.0`) veio de um print de prémios (`bfa05aae…`), **não** dos 2 prints do Info — e seria destruído se o Info escrevesse.
- **Implementação:** gate em `process_lobby_message` **E** em `reconcile_lobby_logs` — se `vision_json.open_tab == 'Info'`, resolve o `tn`, grava o log/`vision_json`/`players_left` e **salta o `upsert_payout`** (mesmo padrão do `refresh_vision_only`, que já retorna antes dessa linha). Helper `_is_info_tab`.
- **DUAS defesas independentes (não se substituem):** (1) **este gate do Info** trata o fluxo NORMAL do Rui (Info = último print); (2) a **guarda de coerência dos payouts** (prémio > pool, etc. — pedida à parte) apanha **Vision alucinada em QUALQUER aba**. → `services/lobby_sync.py` (`process_lobby_message`, `reconcile_lobby_logs`, `_is_info_tab`).

## 2026-07-07 — Política da FRONTEIRA da FT: SNAP-TO-N + coerência só na cauda pós-pico

- 2026-07-07 — **POLÍTICA FECHADA (com 5 casos reais: 4 FTs de Jun via-b + a de 2 Jul via-a).** Fecha o desenho da fronteira em `ft_boundary.compute_ft_boundary`:
  - **SNAP-TO-N.** A fronteira computada (via-a print do Info OU via-b coherent) cai **~1 mão TARDE** — o sinal (print/captura) chega **segundos DEPOIS** de a FT arrancar (gaps reais medidos: **38-51 s**). O snap recua até **`SNAP_WINDOW_MIN` = 3 min** e passa a fronteira para o `played_at` da **1ª mão REAL da FT**: a mais cedo com `sentados == N` que **INICIA a drenagem** (==N seguida de mãos ≤N). **Distingue-se da mesa PRÉ-FT do Hero** (que volta a SUBIR, ex. …3→7) pela regra do "início da drenagem" (não-crescente a partir daí). **Se não houver mão ==N na janela → fallback à fronteira computada** (Q-B: segue para o cross-check normal; mismatch → quarentena F3/F4, **nunca promove às cegas, nunca descarta o torneio**). Efeito validado: a de 2 Jul passou de cross-check **7 vs 6 ✗** para **7 vs 7 ✓** (fronteira 19:19:27→19:18:36); as 2 via-b limpas idem (7 vs 7).
  - **Coerência da via-b só na CAUDA DESCENDENTE PÓS-PICO (`_post_peak_tail`).** O **pico** da sequência de `players_left` = **fecho do late-reg** (com re-entradas, o pico FINAL). A **subida ANTES do pico é vida normal do torneio, NÃO incoerência** — o guarda antigo rejeitava o torneio inteiro (`incoherent_signal`) por causa dela (ex. `35→37→40→52…`). Agora avalia só do último pico em diante; **dentro da cauda mantém-se o rigor** (descarte de 1 outlier isolado, `incoherent_signal` só com 2+ saltos). Efeito: um dos late-reg (`293321688`) passou a **ancorar pela cauda**.
  - **`none` é DESFECHO CORRETO, não falha (ponto 8).** Um torneio **sem FT do Rui** (ou cujas capturas IT nunca apanharam o instante de **1 mesa** — `_ft_applies`, sentados==players_left) **não precisa de fronteira**: não há mãos de FT para promover. Ex.: Speed Racer 26 Jun (capturas param nos 10, FT >9-handed / 2 mesas) e `290775453` (capturas param nos 9 = 2 mesas) → `none` honesto. Recuperável no futuro com **print do Info** (via-a). **`none` ≠ erro**; só `incoherent_signal` (cauda com 2+ saltos) é sinal de "investigar".
  - **Limitação conhecida da via-b:** o **N** da via-b é o `players_left` da **captura** que fez `_ft_applies` — pode ser **menor** que o tamanho REAL da mesa final se a única captura de 1-mesa apanhar a FT já mais drenada (ex. `293321688`: FT arrancou 7-handed mas a captura só apanhou 5 → N=5, fronteira no ponto 5-handed, **perde as mãos 7/6-handed anteriores**). É **conservador** (sub-cobre, nunca marca não-FT); a via-a (print do Info, N real) é superior. → `services/ft_boundary.py` (`_snap_to_n`, `_starts_drainage`, `_post_peak_tail`); `PENDENTES` (guarda por `tournament_status`).

## 2026-07-07 — MUDANÇA DE ARQUITETURA: a TAG -ft MANUAL do Rui é a fonte PRIMÁRIA da fronteira

- 2026-07-07 — **DECISÃO DO RUI (substitui a hierarquia anterior "1º manual de exceção, 2º lobby, 3º coerente").** A limitação da via-b (N conservador; `none` quando as capturas não apanham 1 mesa) motivou virar a fonte: **o Rui marca SEMPRE as mãos de FT** (cobertura DUPLA — **pasta `-ft` na GG** → `discord_tags`; **tags HM3 nas outras salas** → `hm3_tags`). Logo a **tag manual do Rui é o sinal mais fiável do início da FT**, não o lobby nem as capturas. Nova hierarquia do `compute_ft_boundary`:
  - **(0) PRIMÁRIA — 1ª mão do torneio com tag `-ft` MANUAL.** A primeira mão (por `played_at`) com **`folder_ft_source = 'manual'`** — definição ESTRITA (a PASTA do IT chamava-se `-ft`, ex. "ICM PKO FT" → o Rui confirmou a mesa final à mão; ver `_folder_tag_ft_source` em `table_ss.py`). **NÃO** se inclui o `'auto'** (`-ft` adivinhado pela Vision da captura), que seria **circular** (a nossa propagação também grava `'auto'`) e re-introduziria a Vision como primária. O **snap-to-N (já implementado) REFINA PARA TRÁS**: se houver mão com `sentados == N` na janela de 3 min antes, a fronteira recua para ela (apanha as 2-3 mãos jogadas antes de o Rui marcar / tirar o print). N = `final_table_size` do lobby Info se existir; senão inferido (máx. de sentados na janela = tamanho de arranque da FT).
  - **(a) SALVAGUARDA / fallback — lobby Info (N + hora).** Ancora quando **não** há tag manual (histórico). Fornece **sempre** o N ao cross-check da fonte (0). **CONCORDÂNCIA** (tag e lobby apontam o mesmo momento, dentro da janela do snap) → promove o que **faltar** (mãos pós-fronteira sem tag). **DISCORDÂNCIA** (tag do Rui vs lobby em momentos **incompatíveis, para lá da janela**) → **quarentena, nada se promove nesse torneio**.
  - **(b) SALVAGUARDA / fallback — capturas coerentes** (`_it_ft_boundary`, com snap + pós-pico). Ancora quando **não** há tag manual **nem** lobby Info. **A CASCATA MANTÉM-SE `(0)→(a)→(b)→none`**: a fonte (0) VAZIA **não mata** o torneio — o histórico sem tags manuais continua a ancorar pela salvaguarda (a via-b **NÃO** morre; só passa a ser o **último** degrau, não o primeiro).
  - **(none) — só quando (0) E (a) E (b) não dão sinal.** `none` honesto (não é falha; ver a política da fronteira acima). `incoherent_signal` (cauda com 2+ saltos) continua a ser sinal de "investigar".
- **PROVA da premissa (Rui, 7 Jul, prints do explorador):** as pastas `-ft` manuais **EXISTEM e estão em uso** (`Batmen\it\ICM FT`, `Batmen\it\ICM PKO FT` com capturas dentro; `Speed Racer FT` a ser criada). As 4 capturas da `ICM FT` são das mesas `61392519xx` = as **mãos da FT do 295219051**. O "zero manual" do 1º dry-run era **atraso de import** (essas capturas ainda não tinham entrado na app). Quando entrarem, o 295219051 tem as **3 fontes em concordância** (tag manual + lobby N=7 + capturas) — o teste perfeito da hierarquia.
- **O que se APROVEITA (a orquestra fica):** `_snap_to_n`, `_starts_drainage`, `_post_peak_tail`, `_coherent_readings`, `_ft_applies`, `_cross_check`, `_lobby_ft_boundary`, `_it_ft_boundary`, o gate do Info (`#LOBBY-INFO-NO-PAYOUT`) e a guarda de payouts (`#PAYOUT-COHERENCE`). **O que MUDA:** **só** se ADICIONA a fonte (0) `_manual_ft_boundary` **no topo** da cascata; `(a)` e `(b)` ficam como estão. **A quarentena (F3/F4) EMAGRECE:** fica só para DISCORDÂNCIAS reais entre fontes e cross-check falhado — **não** para ausência de sinal. **Promoção continua MANUAL** (botão + ensaio + confirm), guardas intactas, dry-run em tudo. → `services/ft_boundary.py`.
- **✅ VEREDICTO `#FT-FOLDER-TAG-ORIGIN-LOST` (7 Jul) — o circuito NÃO perde a etiqueta; a fonte (0) é SEGURA.** Investigação read-only concluiu: **NÃO há caminho `ICM FT`→`icm`.** `_folder_tag_for` é `.get()` EXATO → `ICM FT` dá `'icm-ft'` (desde 1 Jul, `4884f82`) ou **`None`** ("processada SEM tag") antes disso — **nunca `'icm'`**. **PROVA de fidelidade:** as pastas `ICM PKO FT`/`PKO Pos FT` (mapeadas desde pt73) produziram **28 mãos com `folder_ft_source='manual'`** intactas. O que se passou em Junho: a entrada `'icm ft'` só entrou na tabela a **1 Jul**, logo as capturas `ICM FT` de Junho caíram em **`None`** (import-lag do nome, **não** degradação); o `-ft 'auto'` das mãos GG veio de uma captura de pasta `ICM` base + Vision. **Caminho limpo:** re-importar as pastas `ICM FT` com o código atual → entram `icm-ft` + `folder_ft_source='manual'` (como as `ICM PKO FT` já entram) e a fonte (0) apanha-as. **A fonte (0) FOI IMPLEMENTADA** (`_manual_ft_boundary`, topo da cascata); dry-run atual: `manual=None` em todos os GG (ICM FT ainda por re-importar) → cai na salvaguarda, como esperado.

## 2026-07-08 — CONFIRMAÇÃO SISTEMÁTICA do invariante dos hashes GG (teste read-only)

- 2026-07-08 — **O INVARIANTE POSICIONAL DO HASH AGUENTA-SE (confirmação sistemática, estende o de 2026-07-01).** Teste read-only sobre **todos os 25 torneios GG 2026 com ≥100 mãos = 4198 mãos, 38 488 aparições de hash**: **ZERO** casos de um hash em 2 mesas à MESMA hora (o teste "impossível ser 1 pessoa"). E **40 hashes seguem o jogador entre mesas** (ex.: `4deef1ff` nas mesas 125/5/94 com 326 aparições) — a confirmação directa de que **o mesmo hash = a mesma pessoa durante o torneio inteiro, mesmo mudando de mesa**. **Distinção crucial:** os conflitos de NOME que aparecem na desanon (hash→2 nomes diferentes; 1 nome→2 hashes, o veneno tipo `90abe28`/V Bartko) **NÃO são violações do invariante do hash** — são a **desanon a atribuir o nome errado ao mesmo hash** (bug do stack-elimination). O hash é a âncora fiável; o NOME é que está mal atribuído. **Caso prático 293321688:** 6 mãos FT sem nomes (Gold esquecida) têm **4/4 hashes já com nome no mapa do torneio → 100% resolúveis por propagação pura de hash** (ressalva: 3 desses hashes têm nome conflituoso → propagar só depois de resolver o nome-por-hash). **Evidência para a decisão do CORE (apa indexado por HASH + propagação por torneio) — que continua NÃO APROVADA e é do Rui** (`APA_INDEXACAO_E_COLAPSO §B.4`: como convivem o nível-mão [hash] e o nível-jogador [nome]). Números completos em `JOURNAL_2026-07-08.md`. **Nada implementado.**

## 2026-07-08 — ✅ APROVAÇÃO DO CORE: apa indexado por HASH + propagação por torneio (sistema misto, só-tagadas)

- 2026-07-08 — **O Rui APROVOU a mudança do core** (apa indexado pela identidade da HH + propagação de nomes por torneio), com a **evidência do invariante posicional** (25 torneios GG 2026 / 38 488 aparições / **0 violações** — entrada acima + `JOURNAL_2026-07-08.md`). A desconfiança dos "dois níveis de identidade" (`APA §B.4`) ficou resolvida pelo **desenho misto**. Desenho canónico completo em **`APA_INDEXACAO_E_COLAPSO §B.6`**. Síntese:
  - **ÂMBITO:** desanonimizar **APENAS as mãos tagadas GG** (<20% do total); as não-tagadas **não recebem escrita**. O mapa de nomes do torneio **LÊ de todas as mãos com desanon** (conhecimento é conhecimento), mas **ESCREVE só nas tagadas**.
  - **MECANISMO (incremental, por torneio):** uma mão desanonimizada por **via FORTE** (`position_v3` da Gold, OU `verified_by_user` manual) **espalha** os nomes pelos MESMOS hashes nas outras mãos **tagadas** do torneio; cada Gold nova re-espalha. Base: o invariante (hash fixo por jogador, segue entre mesas).
  - **GUARDAS (obrigatórias, dos casos reais):** (a) **só fonte FORTE semeia** — via fraca (`table_ss`/stack-elimination) NÃO propaga (no máx. preenche branco "por verificar" na própria mão; caso `90abe28`/V Bartko errou 7× consistente); (b) **nome-já-usado** — nome já noutro hash do torneio → **branco + quarentena** (caso `TaWebon` em 2 hashes); (c) **conflito no mesmo hash** — forte vence fraca; forte vs forte / fraca vs fraca → **branco + quarentena** (caso `de6cc7ae` Daniel Filipe/Mikhail Petrov); (d) **branco é honesto, nome errado é veneno** (lei).
  - **ALICERCE TÉCNICO:** chave do apa = **hash (GG) / nick real (WN/PS/WPN)**; `real_name` = atributo que pode estar vazio; leitura retro-compatível **`real_name || chave`**; **`_enrich_all_players_actions` deixa de re-indexar por nome** (raiz §A.4). Faseamento **leitores → writer** (mapa §C: `villain_rules`/`ire`/etc. primeiro).
  - **NÍVEIS:** o hash **NUNCA sai do torneio** (veículo interno); os **Vilões continuam a consumir os NOMES** das mãos tagadas — nada muda no nível-jogador.
  - **CLASSIFICAÇÃO FORTE/FRACA (de `deanon_status.py`):** FORTE = `deanon_status == 'verified'` = `match_method == 'position_v3'` **OU** `player_names.verified_by_user == True`; FRACA = `unverified` (`table_ss`, `anchors_stack_elimination_v2`, `v2` — stack/feltro).
  - **FASEAMENTO:** Fase 0 dry-run (números por torneio, o Rui vê antes da fase 1) → Fase 1 leitores (`real_name || chave`) → Fase 2 writer (enrich sem re-index) → Fase 3 propagação (só tagadas, guardas a–d) + **quarentena de nomes na Saúde GG ao estilo da FT**. Escrita SÓ por aprovação. **Critério de sucesso: o reimport produzir tagadas desanonimizadas à primeira.** → `APA_INDEXACAO_E_COLAPSO §B.6`.

## 2026-07-08 — ✅ CORE CURADO: Fase 3 (propagação de nomes por hash + quarentena) LIVE

- 2026-07-08 — **A última fase do core está FEITA e LIVE (`2c8fe8a`).** Fecha o desenho aprovado (`APA §B.6`): motor `services/name_propagation.py` que, por torneio GG, **lê o mapa hash→nome de todas as mãos com desanon mas só a FONTE FORTE semeia** (`position_v3`/`verified_by_user`; guarda a), e **escreve só nos brancos das mãos TAGADAS** (`match_method='hash_propagation_v1'`, "por verificar"), **sem mudar chaves** (Fase 2). Os nomes propagados são `unverified` em `deanon_status` → **não re-semeiam** (sem cascata circular). Guardas: (b) nome-já-usado (2 hashes) → ambos à quarentena; (c) conflito no mesmo hash → **OCR-merge quando inequívoco**, senão quarentena; (d) sem semente forte → branco honesto.
  - **★ Critério de OCR-merge ENDURECIDO (decisão de segurança).** O matcher das coroas Gold (`_same_player`, prefixo de 6 chars) fundiria por erro **"Daniel Filipe"/"Daniel Ferreira"** (mesmo 1º nome) — e a Fase 3 **escreve** (propaga). Substituiu-se por `_ocr_variant`: só funde **truncagem pura** (curto = prefixo do longo) **ou distância de edição ≤1-2**. No laboratório dá **0 falsos** (os 22 auto-merges são todos o mesmo jogador); à prova do reimport. Aplica-se a nomes no MESMO hash (= mesma pessoa pelo invariante), logo o único risco real (desanon a pôr 2 nomes GENUINAMENTE diferentes no mesmo hash, distância grande) vai à quarentena, não se funde.
  - **Quarentena de nomes** `name_quarantine_review` (irmã da `ft_boundary_review`): decisões (escolher/fundir/dispensar) **persistidas e respeitadas por re-runs**. Painel `NamePropagationPanel` na Saúde GG + endpoints `/api/gg-health/names/{preview,quarantine,choose,merge,dismiss,apply}`. **Auto-write dos casos limpos** (gatilhos F&F idempotentes em `import_`/`import_hm3`/reconcile do table_ss + botão "Aplicar propagação"); a **quarentena é sempre manual** (Rui decide o nome certo por hash).
  - **Dry-run laboratório (implementação real):** 494 mãos c/ todos os vilões no mapa, **104 preenchimentos**, 67 ganho, 25 brancos, **22 OCR auto-merged**, quarentena **9** (8 veneno "nome→2-hashes" + 1 OCR ambíguo). **Troféu `293321688`:** 7 mãos fortes semeiam → mapa de 16 hashes, **0 quarentena** (os conflitos do teste do invariante eram de fonte FRACA → não entram, guarda (a) a funcionar); **12/12 mãos tagadas 100% resolvidas** (a Gold esquecida recuperada por propagação: `Karluz`, `absi17-`, `GGlovelu`, `Andre Marqu..`).
  - **Escolhas de scope registadas:** preenche **só brancos** (não sobrescreve nome fraco existente — upgrade fraco→forte fica decisão futura do Rui); um upload Gold-só (sem HH import) propaga pelo botão "Aplicar propagação" (os hooks de import cobrem o fluxo normal). **CORE CURADO (Fases 1-3 LIVE)**; o teste de aceitação é o **wipe+reimport** (as tagadas GG entram desanonimizadas à primeira). Ver `APA_INDEXACAO_E_COLAPSO §B.6`, `DESANON_ANATOMIA §3.4`, `JOURNAL_2026-07-08.md`.

## 2026-07-08 — RE-ENTRADA: o hash e fixo por ENTRADA, nao por pessoa (Olisadebee) + 3 obras

- 2026-07-08 — **CONCEITO NOVO: RE-ENTRADA — 1 pessoa pode ter N hashes.** O invariante posicional (§23, 1-hash=1-pessoa) mantém-se, mas o **inverso** (1-pessoa=1-hash) **NÃO vale em torneios re-entry**: um re-buy do MESMO humano gera **hash NOVO**. **Caso canónico Olisadebee** (Rui resolveu à mão com Gold+HH das 2 mãos, cruzamento posição+stack ao chip): os hashes `b0b40d2c` e `d07fa3d8` são a **MESMA pessoa por re-entry**. **Prova:** aparições de `d07fa3d8` (~20:50, ids 6159/6202/6236) todas **ANTERIORES** às de `b0b40d2c` (~21:25, ids 6515/7027/7043); **nunca co-ocorrem**; **nick EXATO** igual nas 2 Gold (nicks GG únicos por conta); stack do 2º ≈ **bala fresca**. Consequência: o mesmo nome em 2 hashes **nem sempre é veneno**. **3 obras nasceram deste caso:**
  - **OBRA 1 — o conceito no sistema.** Terceiro verbo no cartão «nome→2 lugares»: **"Mesma pessoa (re-entrada)"** (o nome fica válido nos DOIS hashes; caso sai da quarentena, ambos elegíveis p/ propagação). **Classificador** `reentry_hint` (`name_propagation.py`): nick exacto + FORTE dos 2 lados + janelas de aparição SEM sobreposição (nunca co-presentes) → **pré-selecciona** o verbo (MAS a decisão é **sempre clique do Rui**, nunca auto-resolve); nick igual + **co-presentes na mesma mão** → **impossível ser 1 pessoa → veneno duro**. **Guardas de jusante** (`REGRAS_NEGOCIO §24`): re-entrada NUNCA funde os 2 hashes (stacks/mãos/stats separados; só o nome partilha); bounty é **POR ENTRADA** (volta à base); Vilões já agregam por nome → correcto por construção.
  - **OBRA 2 — resolução por evidência DENTRO do cartão.** Cada mão do cartão expande para o **bloco de Seats do raw** (seat/hash/stack, hash em disputa destacado — a matéria do cruzamento). **"+ imagem"** por mão: o Rui anexa a Gold/captura do disco (`POST /api/table-ss/attach-to-hand` → Vision + persist + força link à mão via `_manual_link_ss`), a miniatura aparece no lado respectivo. Caso real: a mão forte `GG-6083716159` tinha **0 imagens** na app; a Gold existia no disco.
  - **OBRA 3 — o selo na mão (circuito nas 2 direcções).** Uma mão envolvida num conflito pendente mostra no detalhe o selo **"⚠ nome em revisão"** com link ao cartão na Saúde GG (`GET /api/gg-health/names/hand-status`). Some quando o conflito se resolve.
  - Endpoints novos: `POST /api/gg-health/names/reentry`, `GET /api/gg-health/names/hand-status`, `POST /api/table-ss/attach-to-hand`. Docs: `DESANON_ANATOMIA §3.3`, `REGRAS_NEGOCIO §24`.

## 2026-07-08 — Deteção AUTOMÁTICA de re-entry por evidência dura (AmigoCrypto virou feature)

- 2026-07-08 — **A verificação manual do AmigoCrypto (bust+bala+gap) virou detector.** O `reentry_hint` passa a dois NÍVEIS: **`confirmed`** (evidência DURA na HH) e **`likely`** (só sinais fracos). Evidência dura = (1) **BUST** da 1ª entrada — na ÚLTIMA mão SEATED do hash early ele foi all-in e o pote foi para outro (`_all_in_and_result`); (2) **BALA FRESCA** da 2ª — stack inicial do late ≈ starting stack do torneio (banda `[0.80, 1.15]×`, generosa em baixo porque o `start_stack` medido = max na 1ª mão conhecida pode vir sobrestimado por um líder); (3) **GAP curto** (<=1h). **Correcção crítica:** usar as mãos **SEATED** (onde o hash está nos seats do raw = jogou) e não as **NAMED** (desanonimizadas) — o bust está na última mão JOGADA, que muitas vezes NÃO está desanonimizada (o 1º cut errado apontava a última mão *com nome*, 30 min antes do bust real). **Nota de honestidade (Rui):** a ausência de bust legível (não-GG, HH em falta, 2ª entrada já crescida no gap de cobertura) **nunca despromove** um `likely` — só a **presença** de dados **promove** a `confirmed`. Cartão: badge "re-entrada confirmada (bust às HH:MM, re-compra +Xm)" com a mão do bust clicável. **Validação read-only:** **AmigoCrypto** (292758023) → `confirmed` (bust `GG-6107814611` 18:18:22 all-in perdido, re-buy +2m24s stack 25 750); **M_R_Z_E_** (292774174) → `confirmed` (bust 20:33, re-buy +54s, bala 100 000 exacta); **Silin O** (292758023) → `likely` (bust legível mas 2ª entrada a 120 141 = líder acumulado, gap 77m > 1h → não pristine); **OHmyBUDDHA** (293643156) → não-re-entry (nicks diferentes). `services/name_propagation.py` (`_all_in_and_result`, `_tournament_start_stack`, `_stack_in`). Ver `DESANON_ANATOMIA §3.3`.

## 2026-07-08 — VERIFICACAO HUMANA GG-6118200072 (prova que valida o scrub do misread)

- 2026-07-08 — **O Rui verificou à vista a `GG-6118200072`** (Gold da mão adjacente `6118199933` + cruzamento de stacks ao chip, **8/8 lugares confirmados**): **Seat 6 = `5ee4d246` = OHmyBUDDHA** → a leitura FRACA "Vadzim Khazanau" nesse lugar está **ERRADA** (confirmado por evidência). O **Vadzim Khazanau REAL é o `93d63976`** (Seat 1, BTN dessa mão, vencedor) — o **padrão nome-real-lugar-errado da via fraca** (stack-elimination): a Vision leu um nick REAL mas atribuiu-o ao seat errado. **Esta é a prova humana que valida o SCRUB** (`propagation_plan.corrected` + `_clean_stale_villains`): ao carimbar o cartão OHmyBUDDHA por re-entrada (nome VERIFICADO), o `5ee4d246` da `6118200072` corrige-se "Vadzim Khazanau"→"OHmyBUDDHA" e a leitura errada deixa de ficar agarrada à mão. **O `93d63976` (Vadzim real) TEM fonte FORTE** ("Vadzim Khaz.." `position_v3` em 2 mãos, incl. a Gold `6118199933`) → a propagação dá-lhe o nome do seed forte, **não fica branco, sem atribuição manual** (tem ainda 4 leituras fracas "Diego Emperador" = outro misread no mesmo hash, questão à parte). Ver `DESANON_ANATOMIA §3.3`.

## 2026-07-08 — NOVO kind de quarentena: strong_weak_mismatch (buraco de core, sobrevive ao wipe)

- 2026-07-08 — **Buraco de core tapado: um hash com leitura FORTE (X) + leitura(s) FRACA(s) divergente(s) (Y!=X) NAO gerava cartao — passava em SILENCIO.** Só os FORTES semeiam o mapa (guarda (a)); as leituras fracas eram ignoradas por completo, logo um misread fraco ficava agarrado à mão sem alarme (caso `93d63976`: FORTE "Vadzim Khaz.." `position_v3` + 4 mãos FRACAS "Diego Emperador" — visto só por acaso). **A lente é o WIPE:** os dados atuais morrem, NÃO se cura o Diego; o que importa é o **PIPELINE** apanhar o padrão nos imports futuros (a via fraca vai repeti-lo). **Novo `kind = strong_weak_mismatch`**, detectado no **`build_name_map`** (o refresh da propagação, corre em cada import): para cada hash que fica no mapa (tem nome FORTE X), se houver leitura FRACA Y com `norm(Y)` fora dos fortes E não OCR-variante de X → **cartão** na quarentena de nomes. **NÃO-bloqueante:** o hash MANTÉM o nome forte no mapa (propaga na mesma); o cartão é só p/ **confirmar o forte → scrub das fracas** (reusa a mecânica: `conflict_sides` como `same_hash`, decisão `chosen`→`_apply_decisions_to_map` clean[hash] VERIFICADO → a correção + `_clean_stale_villains` já construídos limpam as fracas divergentes). Botão do cartão: **"Confirmar o forte (X)"** / Dispensar. **Varrimento read-only da BD atual (validação de borla antes do wipe):** **59 hashes** têm o padrão em **98 torneios GG tagados**, afetando **93 mãos tagadas**. Exemplos: `93d63976` Vadzim->Diego (4), `Karluz`->"Reiner Zufall" (8, FRIEND_HERO!), `Artfuture`->"V Bartko" (5), + truncagens OCR que o merge nao apanhou e ate lixo do parser (`lytxmf`->"Post Blind(s)"). O Rui NAO vai resolver estes (dados condenados) — servem só para **provar que a deteção dispara** (build_name_map emite o cartao do Diego). **A decisão grande do upgrade AUTOMATICO fraco->position_v3 continua DEFERIDA** (PENDENTES); este cartao é a via de resolver JÁ pelo circuito manual normal. `services/name_propagation.py` (`build_name_map` passo 4). Ver `DESANON_ANATOMIA §3.3`.

## 2026-07-08 — DECISÃO DO RUI: defesa das coroas = OPÇÃO A (prompt sistémico + rede visível)

- 2026-07-08 — **Decisão de arquitetura do Rui (via `FLUXO §12`, que nasceu deste caso): a defesa das coroas ($ bounty) fica na OPÇÃO A.** Contexto: durante a triagem das "Mãos suspeitas" (coroa gravada < base÷2) o Code tinha *assumido* uma escolha de arquitetura — "a protecção sistémica é o prompt; a guarda `crown>=base/2` vive só nos backfills + gate do HRC; o enrich live grava cru". O Rui **reabriu** isso como decisão dele (onde vivem as defesas = produto), instituiu a **regra de governança `FLUXO §12`** (mudanças à arquitetura apresentam-se ANTES, em simples, com opções e custo), e **decidiu**.
  - **Quadro apresentado.** Num PKO 50/50 a coroa é o KO instantâneo = **metade** do bounty base → uma coroa **< metade é SEMPRE errada**: ou chama/VPIP mal lida (bug antigo), ou o **bordo coroa-verde** (jogador a ser eliminado: a coroa própria some, vai a verde para a de quem o elimina). **Números** (107 mãos suspeitas, todas cicatriz pré-fix): `{1:40, 2:20, 3:12, 4:7, 5:2, 6:2, 7:9, 8:15}` seats/mão — os **24 de ≥7 seats** = mesa inteira lida como chama (bug sistémico, morre com o prompt novo); os **40 de 1-seat** = população do bordo legítimo que pode reaparecer.
  - **A (escolhida):** prompt = defesa sistémica; o valor lido grava-se cru associado ao jogador e a mão fica **assinalada em "Mãos suspeitas"** (visível > silêncio); o **portão do HRC** continua a proteger o consumidor crítico (nunca resolve com bounty < metade). Custo zero; revisão manual ocasional de um punhado de bordos de eliminação.
  - **B (rejeitada):** guarda universal `crown>=base/2` também no live path. Torna o valor errado impossível de gravar, MAS o bordo de eliminação (coroa própria legitimamente ausente) seria **rejeitado em silêncio** → lugar a $0 sem aviso, a não ser que se construísse um painel "coroas por ler". Trocar visibilidade por silêncio.
  - **Consequência:** a secção "Mãos suspeitas" é a **rede** (por desenho, não bug); o critério de aceitação do reimport mantém-se **~0** (só bordos legítimos). Universalizar a guarda no live path fica como melhoria opcional futura **apenas se** vier acompanhada de superfície para os rejeitados. `screenshot.py:1044` (live, cru) · `screenshot.py:2144/2395` + `queue_export.py:1694` (guardas). Ver `FLUXO §12`, `PENDENTES` (secção Mãos suspeitas).

## 2026-07-09 — REGRA de hierarquia das COROAS: Gold manda, mas $0 do Gold cai para a table-SS

- 2026-07-09 — **Decisão do Rui (Opção C do `#CROWN-VISIBLE-READ-ZERO`).** Regra canónica da fonte-de-verdade
  das coroas ($ bounty) em mãos GG desanonimizadas por **Gold** (`position_v3`): **o Gold manda nas coroas —
  MAS `$0`/ausente do Gold NÃO é uma leitura**; cai para a **table-SS casada** quando esta tem valor **válido
  (≥ base÷2)**. **Nunca inverter uma leitura VÁLIDA do Gold.** Gap-fill, não inversão.
  - **Porquê:** investigação visual das 8 "Mãos suspeitas" (imagem-a-imagem) provou que as coroas $0 vinham da
    **Vision do Gold** (que falha a ler a coroa quando o avatar está tapado por cartas viradas/All-In/cartas do
    Hero), enquanto a **table-SS casada leu a mesma coroa CORRETAMENTE** ($375, $312.5, $105…). O valor certo já
    estava na BD. Âmbito: 25 mãos GG PKO / 118 ocorrências $0; ~6-9 recuperáveis de imediato pela table-SS, o
    resto (Gold-only) por re-leitura do Gold com prompt afinado. Ver `#CROWN-VISIBLE-READ-ZERO` (`PENDENTES`).
  - **Execução (C):** (1) backfill table-SS→mão onde Gold=$0 e a captura tem coroa ≥ base÷2; (2) prompt do Gold
    afinado (coroa = banner ACIMA do avatar, ler sempre mesmo com avatar tapado) + re-leitura das Gold-only; (3)
    filtro separa `coroa-por-ler ($0)` (âmbar) de `valor-impossível (>0 e <base÷2)` (vermelho).

## 2026-07-09 — REGRA verde-KO CONFIRMADA (valor exato) + CURA por guarda HH-determinística

- 2026-07-09 — **REGRA CONFIRMADA (à vírgula) + CURA (curar o core, não os dados): bounty de jogador
  ELIMINADO num KO.** A coroa **própria** do eliminado **DESAPARECE**; **metade** do bounty aparece a
  **VERDE** na coroa de **quem o elimina** → **bounty do eliminado = verde ÷ fração instantânea** (×2 no
  PKO 50/50). Confirmado à vírgula na **GG-6140169166** (verde **$102.27** → **$204.53**).
  - **Defeito anterior:** a leitura atribuía ao eliminado a coroa do seat **VIZINHO** (veneno) — na
    GG-6140169166 o Hero levou **$170.63 = coroa do KamikazzE97**, não a dele.
  - **CURA:** guarda no **ingest** com sinal de eliminado vindo da **HH** (autoritativo — all-in e perdeu,
    stack final 0) — **NUNCA depende da Vision**. Eliminado → **nunca** a coroa da Vision → **verde-derivado**
    se houver 1-verde-limpo, senão **NULL + "por rever"**. Multiway / verde ilegível → **"por rever"** (manual,
    raro, aceite pelo Rui). Vale **Hero e vilões, GG only**. **Chokepoint único:** `services/eliminated_bounty.py`.
  - **Porquê é robusta:** a garantia vive na **HH determinística**, não no prompt frágil (o verde é bónus,
    não o gate do invariante). Cross-ref `REGRAS_NEGOCIO` (verde-KO), `DESANON_ANATOMIA`,
    `#CROWN-VISIBLE-READ-ZERO`, `JOURNAL 2026-07-09`.

## 2026-07-09 (fecho) — CURA verde-KO dos eliminados LIVE e PROVADA em prod

- 2026-07-09 — **FECHO da frente.** A cura da armadilha verde-KO (eliminado leva a coroa do
  vizinho) está **LIVE e PROVADA em produção**: guarda **HH-determinística** nas mãos **tagadas**,
  funil único `services/eliminated_bounty.py` ligado a 5 sítios (2 famílias, cobertura transitiva
  por `_apply_folder_tag_to_hand`), só-tagadas, idempotente (preserva `green_ko`), fail-safe raw,
  log-alto, **gate duro** no scan. **Contaminação das tagadas 29→0** (`vision_origin_contamination`,
  `13c6177`); mão-troféu GG-6140169166 = $204.54 (verde $102.27 ×2), GG-6139653123 = $20; nunca
  $170.63. **Aceitação FINAL = reimporte** (tagadas nascem verde-derivadas ou "por rever"). →
  `JOURNAL_2026-07-09.md`.

## 2026-07-09 (fecho, noite) — chokepoint de bounty no ingest: 3 guardas + gatilho TS-late

- 2026-07-09 — **Conceito do ingest de coroas consolidado.** O bounty por-seat passa por UM
  chokepoint (`services/eliminated_bounty.py:resolve_seat_bounty`, via `scrub_and_persist`,
  só-tagadas, GG), com **3 guardas** determinísticas guiadas pela HH e pelo TS:
  1. **Eliminado (verde-KO)** — seat all-in-e-perdeu nunca leva a coroa da Vision; verde-derivado
     (`green_ko`, instantâneo) ou NULL+"por rever".
  2. **Vivo-$0 (KO)** — torneio com `buy_in_bounty>0` no TS + vivo + coroa lida $0 → NULL +
     `live_crown_read_zero`. **NUNCA deriva da base** (o vivo pode ter KOs acumulados → subvaloriza;
     GG-6132925926: Hero 2 KOs = coroa $100 ≠ base÷2 $50).
  3. **Vanilla** — torneio com TS presente e SEM bounty → um vivo não pode ter coroa → NULL forçado
     (mata as inventadas; GG-6138905902 Daily Hyper $60: $50/$20 alucinados pela Vision do table-SS;
     raiz = `table_ss_deanon._seats_to_vision_data` copiava `bounty_usd` sem gate).
  - **Convenção reafirmada (caso real):** a coroa de um VIVO = **INSTANTÂNEO** (metade do total, PKO
    50/50); Hero com KOs acumulados tem coroa **> base÷2** — LEGÍTIMO. *Branco honesto > valor inventado.*
  - **Gates duros:** `live-crown-zero-scan` (`silent_zero_contamination`) + `spurious-crown-non-ko-scan`
    (`spurious_crown_contamination`) — ambos =0 pós-reimporte.
  - **Editar a coroa do Hero:** `{!isHero}` levantado no `CrownCell` (capacidade permanente).
- 2026-07-09 — **Gatilho TS-late (`#TS-LATE-NO-FORMAT-RECALC`, GG-only).** Ao importar um TS GG,
  reclassifica o formato (rede de segurança) e **re-corre o funil de coroas** das mãos desse torneio
  → as guardas vivo-$0/vanilla disparam mesmo quando o TS chega DEPOIS da HH. `services/ts_reclassify.py`;
  TS-primeiro = no-op (byte-idêntico). Jusante: solves HRC stale listados, não re-solvidos. → `JOURNAL_2026-07-09.md`.

## 2026-07-10 — lote auditoria: fluxo captura-casa (A) + IRE = métrica KO (C)

- 2026-07-10 — **Fluxo captura-casa-mão (mapa).** Captura NOVA → match → `_finalize`: grava log
  (folder_tag) → desanon (só escreve nomes se a âncora confirmar; senão branco honesto) → tag
  (`_apply_folder_tag_to_hand`, sempre). Captura DUPLICADA (mesmo file_hash já-success) → **atalho**
  (`table_ss.py`) sai cedo, nada corre. **A1 (`#DEDUP-DROPS-FOLDER-TAG`):** o atalho deitava fora a
  folder_tag de um re-envio → cura `_reapply_folder_tag_on_dedup` (Opção A): aplica a tag à mão casada
  + grava na row, sem re-correr IA/desanon; idempotente. Tag GG só entra pela pasta do IT → esta é a
  porta. **14 tagadas-anónimas = desanon recusou nomes incertos (salvaguarda, não bug).**
- 2026-07-10 — **IRE é métrica de ESTUDO KO — decisão do Rui: fica como está.** O gate `ire.py`
  (`_has_ko_tag`) só acende com tag de estudo KO (pko/icm-pko/pos-pko/pos-nko/speed-racer); **'nota'
  NÃO conta** ('nota' = ficha de vilão, vai para Vilões, não é spot de estudo KO). O IRE usa a COROA
  (`bounty_value_usd`/ko_units), **nunca** a chama (`bounty_pct` = VPIP). Confirmado que NÃO morreu no
  fix de 1-Jul (taxa de acendimento pré 76% ≈ pós 80%). **Não reabrir** — pedir IRE em mãos só-'nota'
  seria mudar o gate por formato; decisão futura só se o Rui a levantar. → `JOURNAL_2026-07-10.md`.

## 2026-07-10 (lote B) — QUADRO DE DEFINIÇÕES do verde/coroa (âncora $88) — TUDO COERENTE

Frente B da auditoria. **Diagnóstico: coerente ponta-a-ponta, sem fator-de-2 latente → só documentação.**

**Âncora:** Bounty Hunters Deepstack Turbo $88 (tn 295252423) — buy_in_total $88 = entry $40.96 +
**bounty $40** + rake. PKO 50/50 → `_INSTANT_FRACTION = 0.5`.

**Semântica (Rui):** a **coroa dourada visível** de um jogador VIVO = o **INSTANTÂNEO** = metade do
bounty total = o **CASH que quem o eliminar recebe**. O **VERDE** na coroa do eliminador = o
**INCREMENTO** à coroa dele. No **PKO 50/50 os dois valem o mesmo** (metade) — por isso as contas
batem. `bounty_value_usd` guarda **sempre o instantâneo** (a coroa visível / o verde).

**Cadeia ecrã → guardado → derivado (exemplo: fresco num $40-bounty):**
| Passo | O que é | Valor |
|---|---|---|
| coroa visível de um VIVO | instantâneo = metade = cash do eliminador | $20 |
| `bounty_value_usd` (guardado) | a coroa visível = instantâneo | $20 |
| IRE `ko_units` (`ire.py:371`) | coroa / (buy_in_bounty × 0.5) = 20/(40×0.5) | 1.0 (fresco = 1 unidade) |
| queue_export/HRC (`_crown_to_total_factor` = 1/0.5 = 2) | total = coroa × 2 | $40 (total na cabeça) |
| hover (`HandHistoryViewer`) | mostra a coroa $ + ko_units (NÃO duplica) | "$20 · KO 1.0" |
| scan suspeitas / backfill / verde-KO | fresco = base÷2; < base÷2 = suspeita; verde = instantâneo | limite $20 |

**Caso GG-6139653123 (Hero $30):** `bounty_value_usd=$30` (instantâneo acumulado: $20 fresco + $10 do
1º KO) → IRE ko_units 30/20=1.5; HRC total $60. Coerente.

**Consumidor a consumidor — verdict:** IRE (÷bib×0.5), queue_export/HRC (×2), scan suspeitas
(vs base÷2), backfill_crowns (≥base÷2), verde-KO (guarda o instantâneo), hover (mostra cru + ko_units)
— **TODOS tratam a coroa como INSTANTÂNEO**. **Nenhum a assume como total.** Sem fator-de-2 latente.

**Nota latente (menor, sem acção):** o `0.5` está DUPLICADO — `ire.py:77` e `queue_export.py:845`
(re-declarado de propósito). Ambos 0.5 hoje; se um mudar sem o outro → divergência (à la
`#POSITION-LABELS-PYTHON-JS-DRIFT`). Fonte única resolveria; regista-se, não é acção.

## 2026-07-10 (fecho) — reactivação de dispensas FT + certificação pt95

- 2026-07-10 — **Regra da reactivação de uma dispensa FT (`#FT-ZOMBIE-DISMISS-REACTIVATION`):** um
  cartão dispensado só reacorda com sinal **POSTERIOR à dispensa** — regra ÚNICA partilhada pelo
  refresh (background) e pelo painel (`has_new_ft_signal(tn, decided_at)`; antes divergiam → zombie).
  Sinais: **tag -ft manual** (override deliberado do Rui, trumps a dispensa) OU **print Info do lobby
  com `posted_at > decided_at`** (Info genuinamente novo). O Info **pré-existente** (o que motivou a
  dispensa) NUNCA a reacorda. Princípio: *o sinal que motivou uma decisão não pode desfazê-la.*
- 2026-07-10 — **Certificação smoke pt95:** 5/5 solves do robot verificados (estrutura + âncora +
  proveniência de payout limpa); exe certificado; gate #1 fechado. `hrc_jobs`=6 (5 done); 0 solves
  nas edições mal-coladas (nenhum payout errado consumido). → `JOURNAL_2026-07-10.md`.
