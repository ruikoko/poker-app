# Sessão 14-15 Maio 2026 — pt25e (Bloco 1 fechado + Bloco 2 fix urgente)

Sessão pt25e parte 1: **Bloco 1** fechado em 14 Maio com 3 commits (`8eb9d87`, `f7c8833`, `bad2c51`). Source watcher (`tools/watcher_src/patched_funcs.py`) ganhou stubs defensivos para Bugs F/G/H/J + backend ganhou helper novo `derive_aggressor_real_action`. Suite **266→282 PASSED**. `.exe` em produção (Beelink) **intacto pt25d** — Bloco 1 valida arquitectura, não muda comportamento operacional.

Sessão pt25e parte 2: **Bloco 2** começa em 15 Maio com smoke devagar manual pelo Rui no Beelink (mão GALACTICA), sem passar pelo backend (pasta preparada à mão). Este journal cobre os achados até ao fix urgente `#META-AGGRESSOR-POSITION` (commit de hoje, fecha dependência crítica destranca do Bloco 2 wiring).

## Resumo

| Marco | Status | Commits |
|---|---|---|
| **Bloco 1 — backend helper + watcher source stubs** | ✅ Fechado 14 Maio | `8eb9d87` / `f7c8833` / `bad2c51` |
| **Bloco 2 — fix urgente #META-AGGRESSOR-POSITION** | ✅ Fechado 15 Maio | `c6d6c40` / `3e7b5bd` |
| **Smoke 15 Maio manhã + Ctrl+D em Bug J** | ✅ Docs 15 Maio | `90a3b41` |
| **HRC Import Pipeline v1** (★ marco simbólico) | ✅ Fechado 15 Maio tarde | `4fa09be` |
| **Feedback "no redeploy callout"** | ✅ Fechado 15 Maio tarde | `342e292` |
| **Delete session na lista HRC** | ✅ Fechado 15 Maio tarde | `e45e507` |
| **Bloco 2 — wiring watcher G+J + calibração coords** | ⏳ Em curso pelo Rui | smoke devagar manual GALACTICA |

## Detalhe Bloco 1 — 14 Maio (commits `8eb9d87` / `f7c8833` / `bad2c51`)

### Bloco 1 commit 1 — `#META-AGGRESSOR-REAL-ACTION` (8eb9d87)

Helper novo `derive_aggressor_real_action(hh_text, level_sb, level_bb) -> dict|None` em `services/queue_export.py`. Parseia primeira raise/bet preflop, converte chips → bb units, devolve `{"type": "raise"|"bet", "size_bb": float}` ou `None`. Cross-site (PS/GG/WN/WPN) via `find_preflop_marker` (pt25b) + `_PREFLOP_OPEN_RE` (colon opcional). Helper auxiliar `_extract_blinds_from_header` faz cadeia regex Winamax → GG pre-convert → genérico.

Injecção em `build_queue_zip`: manifest entry + payouts.json. Tests pytest 16 casos (4 reais cross-site + 12 sintéticos cobrindo UTG opens variados / all-in shove / limp pot / HU SB / no preflop / invalid bb / empty / build_queue_zip integration). Suite 266→282 PASSED.

### Bloco 1 commit 2 — Bug F split CI Target (f7c8833)

Source-side em `tools/watcher_src/patched_funcs.py`. Adicionados helpers `set_ci_target_initial(wpos, value=5.0)` + `set_ci_target_refine(wpos, value=10.0)` + private `_set_ci_target_common(wpos, value, label)`. Coords `CI_TARGET_FIELD_X/Y` placeholder (0,0) com early-return defensivo + WARN — não-herdados de pt25d porque conceito é diferente (set CI no main UI vs popup Nash).

`setup_hand` ganha call `set_ci_target_initial(wpos, value=ci_target)` antes do 1º `start_calculation`. `set_ci_target_refine` fica disponível para wiring Bloco 2 (chamada em stubs comentados pré-2ª run).

### Bloco 1 commit 3 — Bug H re-order + stubs Bloco 2 (bad2c51)

Source-side. Função nova `finalize_after_second_run(wpos, export_zip)` faz `export_strategies(export_zip)` que era o último passo do `setup_hand` original — agora encapsulado para ser chamado **após** Prune Action + Scope + 2ª run. `setup_hand` retorna `export_zip` sem fazer export (Bloco 1 valida arquitectura; `.exe` em produção continua pt25d). Stubs comentados em `setup_hand` para Bloco 2: `prune_action_on_line` (Bug J), `_set_scope_selected_subtree` (Bug G passo 1), `_select_subtree_root_by_action` (Bug G passo 3), `set_ci_target_refine` (Bug F refine), `start_calculation(10.0)` (2ª run), `finalize_after_second_run` (Bug H).

## Smoke devagar Bloco 2 — 15 Maio (em curso pelo Rui)

Rui correu o `.exe` pt25d **directamente** no Beelink com a mão GALACTICA (`WN-4706316461629505541-158-1778795596`, Winamax, 6-handed, UTG abre 2.5bb, SB 3-bet all-in, BB Hero call all-in). Pasta preparada à mão sem passar pelo backend:

- `hh.txt` — HH bruta Winamax
- `payouts.json` — 94 lugares reais do lobby + `bountyType=PKO` + `progressiveFactor=0.5` (aproximação ao Space KO Winamax, não 100% accurate mas aceitável p/ smoke)
- `script.js` — template Charles "open 2x" com `SIZES_OPEN_OTHERS=[2.5, ALLIN]` + `SIZES_3BET_SB_VS_OTHER=[7.5, ALLIN]`. **Sem prune block** (deliberado: queremos smoke do flow watcher puro, não testar prune).
- `meta.json` — `stage=MTT`, `players_left=88`, `total_chips=14940000`, `ci=10.0`

### Achados até ao briefing (5 itens)

1. **Queue picker eato a 1ª mão errada.** Primeira iteração: watcher pegou em `WN-4699459877053923331-277-1778535900` (mão antiga) em vez da GALACTICA porque ambas estavam na queue. Workaround imediato: mover WN-4699 para fora da pasta queue. Sem repercussão arquitectural (sort lexicográfico).

2. **MTT Stacks panel skip por config de teste.** Sem `stage: "MTT"` explícito no `meta.json`, o caller (`setup_hand` em `patched_funcs.py`) cai no branch `elif equity_model == 'multi_table_icm'` (pt23 Bug E fix) e salta o painel MTT Stacks com `BTN_NEXT` directo, deixando Other Tables em 0. Para mãos MTT correctas o Rui quer "++" + OK (live em `handle_mtt_stacks_page`, branch `if stage == 'MTT' and (players_left or players_in_hand)`). **Fix imediato**: config (`meta.json` com `stage=MTT` explícito + `players_left`). **Fix longo prazo**: provavelmente manter heurística como está e garantir que o pipeline upstream enriquece sempre `meta.json` com `stage=MTT` para mãos MTT — reavaliar em pt25f.

3. **CI=10 confirmado empiricamente.** Com CI=10 a 2ª run baixa de ~7.2h (CI=5) para ~76min. Alinha com a regra semântica correcta (CI baixo = mais refinado = mais lento). Confirma a docstring de `_set_ci_target_common` em `patched_funcs.py`. Implicação para Bloco 2: o set CI no main UI antes do Calculate revela-se **desnecessário** porque o popup Nash que aparece pós-Calculate já tem o campo CI calibrado pt25d (`rect.left + int(w * 0.65)`, `rect.top + int(h * 0.51)`) e o valor vem do `meta.json` via `start_calculation(ci_target)`. Ver `#WATCHER-BUG-F-CI-TARGET-2ND-RUN` reclassificado para 🟢 MOSTLY DEPRECATED em `TECH_DEBTS_INVENTARIO.md`.

4. **Save mid-flow continua a aparecer.** Caixa "Save As" abre entre fim da 1ª run e início da 2ª, fica em wait até estratégias estabilizarem. Bug H stub (`finalize_after_second_run`) ainda não está wired no `.exe` pt25d. Sem prejuízo para Bloco 1; reordering completo só depois de G+J calibrados.

5. **Bug I (Basic Hand Data) por isolar.** Watcher clica num botão errado no 1º painel pós-paste da HH. Repro confirmado visualmente mas sem screenshot/log estruturado ainda. Rui captura amanhã step-by-step.

### Re-priorização pelo Rui — Bug G > Bug J

Argumento: Selected Subtree corta a 2ª run para uma fracção do tempo (centrado no spot real da mão); Prune Action é optimização adicional menos crítica. Mudança aplicada em `TECH_DEBTS_INVENTARIO.md`: Bug G passa para 🔴 HIGHEST entre os 5 bugs do watcher; Bug J reposicionado abaixo, mantendo 🔴 HIGH mas com nota explícita.

### Solução desenhada para Bloco 2 wiring

Watcher faz **OCR confinado à coluna Player** da Strategy Table HRC e clica a primeira linha onde `Player == aggressor_real_action.position`; depois click no botão play (coords pt25d já calibrados); no popup Nash que abre — dropdown Scope → "Selected Subtree" → CI=10 (lido do `meta.json`) → OK.

Vantagem chave: reduz drasticamente o custo de OCR. Vocabulário fechado de ~6 strings curtos (UTG/HJ/CO/BU/SB/BB para a maioria das mesas; +EP/MP/EP1/EP2 para mesas grandes; `BU/SB` para HU) vs OCR genérico sobre toda a tabela. Engineering downstream simples — qualquer linha com `Player == position` serve como subárvore, não precisamos match exacto de sizing (confirmado pelo Rui).

Coords pendentes calibração em smoke devagar pt25e Bloco 2 final:
- column Player da Strategy Table (linha-por-linha OCR)
- dropdown Scope no popup Nash da 2ª run
- opção "Selected Subtree" no dropdown

## Bloco 2 fix urgente — `#META-AGGRESSOR-POSITION` (15 Maio, este commit)

Extensão de `derive_aggressor_real_action` com campo `position` (string maiúsculas — labels canónicos de `_POSITION_LABELS_BY_N`: UTG/HJ/CO/BU/SB/BB/EP/MP/EP1/EP2 + `BU/SB` para HU). Mapping nick → position via `derive_seats_in_preflop_order` (única fonte de verdade do preflop order pt25d) — qualquer mudança de convenção futura fica centralizada lá.

**Follow-up BTN→BU mesmo dia (15 Maio):** confirmação empírica do Rui que a Strategy Table HRC mostra "BU" não "BTN". `_POSITION_LABELS_BY_N` realinhado nos índices 3-9 (`BTN` → `BU`); HU mantém `BU/SB` (já correcto desde pt25d). Suite continua **291 PASSED**. As enumerações abaixo já reflectem o estado final pós-follow-up.

### Schema final

```json
{
  "type": "raise" | "bet",
  "size_bb": float,
  "position": "UTG" | "HJ" | "CO" | "BU" | "SB" | "BB" | "EP" | "MP" | "EP1" | "EP2" | "BU/SB" | null
}
```

`position = None` apenas em casos degenerate (parsing de seats falha, nick do raiser não está nos seats — implica HH inválida). Em condições normais cross-site (PS/GG/WN/WPN) a position é sempre derivável.

### Helper privado `_resolve_position_for_nick`

```python
def _resolve_position_for_nick(hh_text: str, nick: str) -> Optional[str]:
    if not nick:
        return None
    for s in derive_seats_in_preflop_order(hh_text):
        if s.get("nick") == nick:
            pos = s.get("position")
            return pos if isinstance(pos, str) else None
    return None
```

Reutiliza `derive_seats_in_preflop_order` (pt25d). Não duplica lógica de preflop order — qualquer fix futuro na convenção de índices ou labels propaga automaticamente.

### Injecção

Campo `position` viaja nos mesmos sítios que `type/size_bb` (deployed em Bloco 1):
- `manifest.json` → `hands_included[*].aggressor_real_action.position`
- `<hand_id>/payouts.json` → `aggressor_real_action.position`

Sem schema change disruptivo. Forward-compat com o `.exe` pt25d (que ignora `aggressor_real_action` por completo). Forward-compat com Bloco 2 wiring (que vai ler `position` para o OCR match).

### Tests pytest (9 novos, suite 282→291)

| Test | Cobertura |
|---|---|
| `test_aggressor_real_action_PS_sample` (extended) | PS Votsarrr=BU em 5-handed |
| `test_aggressor_real_action_GG_sample` (extended) | GG 221ebf0d=HJ em 5-handed |
| `test_aggressor_real_action_WN_sample_INTERSTELLAR` (extended) | WN blueballs67=UTG em 5-handed |
| `test_aggressor_real_action_WPN_sample` (extended) | WPN DAVIDSBAGOFICE=HJ em 8-handed full |
| `test_aggressor_real_action_UTG_raise_2bb`/`2.5bb`/`3bb`/`all_in_shove` (extended) | UTG sintético em 8-max via `_hh_8max_btn4` |
| `test_aggressor_real_action_HU_SB_raise` (extended) | HU SB raise → `BU/SB` label canónico |
| `test_aggressor_real_action_5handed_UTG_open` | N=5 sintético UTG |
| `test_aggressor_real_action_5handed_HJ_open` | N=5 sintético HJ |
| `test_aggressor_real_action_5handed_BU_open` | N=5 sintético BU |
| `test_aggressor_real_action_5handed_SB_open` | N=5 sintético SB (idx N-2) |
| `test_aggressor_real_action_4handed_UTG_open` | N=4 sintético UTG (labels UTG/BU/SB/BB) |
| `test_aggressor_real_action_4handed_BU_open` | N=4 sintético BU |
| `test_aggressor_real_action_4handed_SB_open` | N=4 sintético SB |
| `test_aggressor_real_action_HU_BB_3bet_after_SB_call` | HU BB raise após SB completa → BB label |
| `test_aggressor_real_action_HU_SB_raise_synthetic` | HU SB raise sintético dedicado → `BU/SB` |
| `test_build_queue_zip_injects_aggressor_real_action_in_manifest_and_payouts` (extended) | Integração build_queue_zip cobre o campo |

3 helpers privados novos no test file: `_hh_5max_btn1`, `_hh_4max_btn1`, `_hh_hu` (paralelo a `_hh_8max_btn4` existente).

### Cobertura cross-site validada

| Site | Sample | Aggressor | Position esperada | Position derivada |
|---|---|---|---|---|
| PokerStars | PS-260299428000 | Votsarrr (Seat 5) | BU (5-handed, hrc_idx=2) | ✅ BU |
| GGPoker | GG-5939385803 | 221ebf0d (Seat 8) | HJ (5-handed, hrc_idx=1) | ✅ HJ |
| Winamax | WN INTERSTELLAR (4699...) | blueballs67 (Seat 5) | UTG (5-handed, hrc_idx=0) | ✅ UTG |
| WPN | WPN 2735377673 | DAVIDSBAGOFICE (Seat 7) | HJ (8-handed full, hrc_idx=3) | ✅ HJ |

### Sintéticos por N

| N | Coberto | Labels |
|---|---|---|
| 2 (HU) | ✅ SB raise + BB raise após SB-completa | `BU/SB`, `BB` |
| 4 | ✅ UTG / BU / SB opens | `UTG`, `BU`, `SB`, `BB` |
| 5 | ✅ UTG / HJ / BU / SB opens | `UTG`, `HJ`, `BU`, `SB`, `BB` |
| 6 | (coberto via PS/WN reais 5-handed; full 6 não-prioritário) | UTG/HJ/CO/BU/SB/BB |
| 8 | ✅ UTG (sintético `_hh_8max_btn4`) + WPN real HJ | UTG/EP/MP/HJ/CO/BU/SB/BB |

## Smoke 15 Maio (manhã) — confirmações empíricas adicionais

Segunda passagem do Rui no Beelink com a GALACTICA na manhã de 15 Maio, agora com `meta.json` corrigido (`stage=MTT` + `players_left=88`). Confirma ou ajusta vários pontos do briefing da tarde anterior. Sem mudanças de código nesta entrega — só docs.

### 1. Smoke GALACTICA bem-sucedido (zip exportado)

`.exe` pt25d completou flow ponta-a-ponta sob a config corrigida. Resultado: `C:\Users\Administrator\Documents\Teste completo\done\Exports\WN-4706316461629505541-158-1778795596.zip` criado. Pasta original da mão **não** foi movida (continua em `Teste completo\` raiz, fora de `done\`) — comportamento esperado do `.exe` pt25d, watcher source apenas exporta o `.zip`.

### 2. MTT Stacks workaround validado empiricamente

Com `meta.json` declarando `stage: "MTT"` + `players_left: 88` explícitos, o watcher entra no caminho **`handle_mtt_stacks_page` caminho 1** do código: clica em "Remaining Players" em (977, 330) e preenche correctamente com 88. Sem essa declaração (smoke 14 Maio), caía no branch skip (pt23 Bug E fix) que deixava Other Tables em 0. Confirma a recomendação de fix imediato do briefing anterior — `meta.json` é a alavanca certa, sem necessidade de mexer no source do watcher.

### 3. CI=10 reconhecido empiricamente no popup Nash (log estruturado)

Log do smoke regista `CI Target: 10.0` após `[NASH] CI click=(939,485) OK=(895,568)`. Confirma o que estava deduzido em 14 Maio: o popup Nash já lê o valor do `meta.json` via `start_calculation(ci_target)` e o campo CI calibrado pt25d é honrado. ETA cai de **~7.2h (CI=5) → ~76min (CI=10)** — alinhamento total com a regra semântica (CI baixo = mais refinado = mais lento). Mantém `#WATCHER-BUG-F-CI-TARGET-2ND-RUN` em 🟢 MOSTLY DEPRECATED.

### 4. BU confirmado visualmente como label canónico HRC + MP para N=7+

Screenshot de mão 7-handed (Q6.hrcz) mostra a coluna Player da Strategy Table com sequência **UTG / MP / HJ / CO / BU / SB / BB**. Confirmação empírica directa do follow-up BTN→BU do dia anterior (commit `3e7b5bd`) — Strategy Table HRC usa "BU" não "BTN". 

**Implicação adicional:** para `N=7+` aparece "MP" (Middle Position) entre UTG e HJ. Verificar em pt25e Bloco 2 wiring (ou pt25f follow-up) se `_POSITION_LABELS_BY_N` cobre MP correctamente para N=7. Para N=8/9 os labels actuais usam EP/MP/EP1/EP2 — pode ser necessário alinhamento adicional consoante o que a Strategy Table HRC mostra empiricamente em mesas grandes.

### 5. Atalhos de teclado da Strategy Table HRC (impactam Bloco 2 Bug J)

Descobertos via inspecção da UI HRC:

| Atalho | Acção |
|---|---|
| `Ctrl+D` | Prune Action |
| `Ctrl+Shift+D` | Prune Children |
| `Ctrl+Shift+A` | Add Action |
| `Alt+L` | Lock/Unlock Range |
| `Ctrl+C` / `Ctrl+Shift+C` | Copy Range / Strategy |
| `Ctrl+V` / `Ctrl+Shift+V` | Paste Range / Strategy |

**Implicação para Bug J (Prune Action per line):** quando chegar a hora do wiring de Prune Action, o watcher pode usar `Ctrl+D` directo após seleccionar a linha em vez de navegar context menu (right-click → "Prune Action"). Caminho preferido — calibração muito mais simples (sem coords de menu, sem armadilha das 2 entradas "Prune" no menu), maior robustez face a mudanças de UI. Tech debt `#WATCHER-BUG-J-PRUNE-ACTION-PER-LINE` actualizado com esta nota.

### 6. Bug I sem evidência na corrida desta manhã

O log do smoke 15 Maio não revela anomalia no 1º painel (Basic Hand Data). Pode ter sido sintoma de outra mão ou config específica do smoke 14 Maio. Deixar em **watch** — não fechar o tech debt sem mais corridas que confirmem ausência consistente do sintoma.

### 7. Save mid-flow continua a aparecer (esperado)

Caixa "Save As" continua a abrir entre a 1ª run e o fim do flow — export feito antes da 2ª run terminar. Comportamento documentado, sem fix por agora — Bug H stub (`finalize_after_second_run`) ainda não está wired no `.exe` pt25d. Reordering completo só vem com Bloco 2 wiring + recompile.

## Próximos passos imediatos pelo Rui

1. **Smoke devagar Bloco 2 wiring** — Rui executa manualmente com prints e regista coords para:
   - column Player da Strategy Table (linha-a-linha OCR)
   - dropdown Scope no popup Nash da 2ª run
   - opção "Selected Subtree" no dropdown
2. **Bug I repro step-by-step** — capturar exactamente qual botão errado o watcher clica no 1º painel (Basic Hand Data).
3. **Bloco 2 source-side wiring** — uncomment + ajustar stubs em `setup_hand` para chamar `_set_scope_selected_subtree` + `_select_subtree_root_by_action` (lê `position` do payouts.json) + `start_calculation(10.0)` + `finalize_after_second_run`.
4. **Recompilar `.exe`** — PyInstaller em build local Rui ou pipeline automatizado (decisão pendente).
5. **Smoke real final pt25e** — pipeline end-to-end backend → adapter → `.exe` recompilado → HRC engine → 2ª run em Selected Subtree → export → POST results.

## Tarde 15 Maio — pivot para HRC Import Pipeline v1 (marco simbólico)

A tarde de 15 Maio fechou o **primeiro ciclo completo HRC→app**: zip exportado pelo Rui no HRC desktop → upload via UI → parsing backend → DB → range matrix 13×13 renderizada na UI. É a primeira vez que o output do solver volta a entrar na app de forma estruturada e queryable. Marco simbólico — embora o pipeline FASE 3 (watcher Beelink) ainda não esteja a fechar em automático, agora há um caminho manual válido para qualquer sessão HRC entrar no sistema.

### Entrega 1 — HRC Import Pipeline v1 (commit `4fa09be`)

Schema novo (`hrc_sessions` + `hrc_nodes`, ambas idempotentes via `ensure_hrc_schema()` no lifespan). Endpoint `POST /api/hrc/import` faz multipart de zip Complete Export, valida (`settings.json` + `equity.json` + `nodes/0.json` obrigatórios), parseia 9521 nodes lazy via generator, e bulk-inserta em batches de 500 via `psycopg2.extras.execute_values`. Endpoints adicionais: `GET /sessions`, `GET /sessions/:id`, `GET /sessions/:id/nodes/:idx`.

UI standalone `/hrc-sessions`: drop zone + tabela de sessions importadas, link "Abrir" → página detalhe com summary panels (Mesa: stacks/blinds/ante/engine; Estrutura: equity model/bounty/chips/top-5 prizes) + **range matrix 13×13** do nó 0 com coloração por acção dominante (F=cinza, C=verde, R=gradiente laranja→vermelho por size relativo) e hover tooltip com `played[]`/`evs[]` por acção.

Validação local: parser em 0.02s (preflight) / 1.57s (iter full); E2E TestClient com DB mocked em 2.70s. **Validação real Rui no Railway pós-deploy: 14.9s** end-to-end para o `teste app.zip` (9521 nodes, 26MB) — dentro do target <30s upload. Suite **291 → 310 PASSED** (+19 tests novos: parser 11 cases, router 8 cases).

### Entrega 2 — Feedback "no redeploy callout" (commit `342e292`)

Após o report da Entrega 1 ter listado "1. Redeploy Railway service `poker-app`" como passo, o Rui sinalizou que era ruído — Railway auto-deploya push para `main` e `ensure_*_schema()` corre sozinho no boot. Guardado como feedback memory + clarificada a entrada "Deploys" em `docs/PAPEIS_E_RESPONSABILIDADES.md` (linha 137): aprovação prévia ao push continua, mas listar "redeploy" como passo separado no report final é redundante; só destacar quando houver env var nova, manual restart obrigatório, ou outra acção fora do padrão.

### Entrega 3 — Delete session na UI (commit `e45e507`)

Caso de uso real: o Rui importou o zip de teste para validar e queria limpar. `DELETE /api/hrc/sessions/{id}` em `routers/hrc.py` (cascade automático para `hrc_nodes` via `ON DELETE CASCADE` já existente no schema; 404 se não encontrar com rollback). UI ganha coluna "Acções" com `Abrir | Apagar`; click no Apagar abre modal centrado (overlay rgba escuro, click-outside fecha) com nome da sessão e aviso "não pode ser desfeito"; após confirm, a row é filtrada do state local sem refetch (UI actualiza sem flicker). Suite **310 → 313 PASSED** (+3 tests: happy path, 404, 401 sem auth).

### Estado do ciclo HRC→app pós-tarde 15 Maio

Visão de funil — 3 caminhos paralelos para o output do solver entrar na app:

| Caminho | Status | Comentário |
|---|---|---|
| **Upload manual via UI** | ✅ Fechado pt25e tarde | Drop zone `/hrc-sessions`; testado real com `teste app.zip` em 14.9s |
| **POST automatizado watcher Beelink** (`POST /api/queue/hrc/results`) | ✅ Backend pronto pt21 (`2fa1f60`) | Tabela `hrc_jobs` distinta de `hrc_sessions`; watcher ainda bloqueado por bugs A/B/C |
| **Render visual da estratégia** | ✅ v1 nó 0 fechado pt25e tarde | Postflop nodes + navegação pela árvore + replayer overlay deferidos para v2 |

`hrc_sessions`/`hrc_nodes` (Complete Export, 9521 nodes preflop em árvore) e `hrc_jobs` (zip do watcher pós-2ª run, ainda só metadata em `meta_json`) **não estão ligados** por enquanto. Quando o watcher fechar smoke funcional (pt26+), podemos decidir se o `result_zip` em `hrc_jobs` é parseado para `hrc_sessions` automaticamente, ou se mantemos os dois fluxos separados conforme a granularidade que se quiser para cada caso.

## Commits pt25e em main (cronológico)

```
8eb9d87  pt25e bloco 1 #META-AGGRESSOR-REAL-ACTION: helper + injection manifest/payouts
f7c8833  pt25e bloco 1 Bug F: split set_ci_target em initial/refine (watcher source)
bad2c51  pt25e bloco 1 Bug H: re-order setup_hand + stubs Bloco 2 (watcher source)
c6d6c40  pt25e bloco 2 fix urgente #META-AGGRESSOR-POSITION: campo position + 9 tests
3e7b5bd  pt25e bloco 2 follow-up: BTN → BU em _POSITION_LABELS_BY_N (vocab HRC)
90a3b41  pt25e bloco 2 docs: smoke 15 Maio (manhã) achados + Ctrl+D em Bug J
4fa09be  hrc import pipeline v1: schema + 4 endpoints + UI /hrc-sessions   ★ marco
342e292  docs: clarificar que 'redeploy' nao e passo separado em reports
e45e507  hrc: DELETE /api/hrc/sessions/:id + UI botao apagar com modal confirm
```

★ = primeiro ciclo completo HRC→app fechado (zip → upload → parsing → DB → range matrix UI).
