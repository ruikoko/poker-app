# JOURNAL — 12 Julho 2026

Sessão longa (contínua desde a noite de 11 Jul). Duas grandes frentes fechadas de ponta
a ponta — **bloco das coroas** e **swap Hero↔vilão** — mais a **consolidação da Saúde
Import** e o início da **verificação visual painel a painel**. Tudo LIVE em `main`
(`abd6a51` … `d715e42`). Registos de conceito/lição em `REGISTO_CONCEITO.md` e `LICOES.md`.

## ★ Bloco das COROAS — fechado ponta a ponta

**Causa-raiz (vista pelo Rui na sessão visual):** **não existe ícone de coroa dourada no
avatar da GG** — o bounty vive SEMPRE na **placa retangular de $ POR CIMA do avatar**; a
única badge no avatar é a **chama laranja = VPIP**. Os prompts da Vision (Gold + table-SS)
descreviam um "gold crown badge" **imaginário** → trocas (agarra a chama), omissões
(procura a coroa, não acha) e acertos por acaso. Este prompt sobreviveu a 2 curas por
ninguém ter comparado a letra com uma imagem real (LICOES).

**Sequência (tudo commitado):**
- **Prompts reescritos** sobre o ecrã real (Gold `screenshot.py` + table-SS
  `table_ss_vision.py`): bounty = $ na placa acima do avatar; chama = SEMPRE VPIP; sem
  placa legível → NULL. Parser Gold: `NULL`/vazio → `None` (por rever), nunca 0. Letra
  aprovada pelo Rui antes do deploy.
- **Teste de aceitação ao vivo** (Vision real + imagens reais, harness read-only
  `/crowns/test-reread`): **GG-6113996183 8/8 EXATO** face às placas (Lauro 50 · Lucas
  259.37 · Short Bus 168.75 · TpApK1m 200 · 4_TheGlory 200 · Volodymyr 250 · Producer65
  137.5 · Lionevil 168.75) + 3 controlo. Passa 100%.
- **Guarda A (base÷2 só)** — `_guard_suspect_crowns`: coroa < base÷2 = impossível (chama
  lida como coroa) → NULL + `crown_review`. **A grelha aritmética dyadic MORREU no teste
  real** (NULLava o 259.37 verdadeiro; coroas progressivas/rake não são dyadic). Detetor
  `==chama` fica só como SINAL no painel.
- **Botão de re-leitura** no painel Coroas (`/crowns/reread`, prompt novo, escreve +
  guarda base÷2). O Rui disparou as 46; **109 seats corrigidos** (as chamas → placas
  reais). Restou só o Hero tapado.
- **Grupo "Valor alto — confirmar"** (`detect_bounty_above_3x`, coroa > 3×base): 7 mais
  altas verificadas à vista pelo Rui — **7/7 REAIS** (até 21×base, Puntti $5324 num HR
  Main Event $525). Auto-carimbo por consistência (`/crowns/high-reread-confirm`,
  re-leitura == valor gravado, tol. de cêntimos → `bounty_confirmed`): **206 seats
  auto-carimbados** das 74; 9 sobram (divergência OCR, ex. O Sander $819≈$813, guardada em
  `crown_reread`, lado a lado no painel). Guarda de export `bounty_above_3x_unconfirmed`
  (sem carimbo não solve).
- **Fallback SS por seat** (`/crowns/fallback-fill`): coroa NULL/$0 vai à outra testemunha
  (Gold primária, senão table-SS) da MESMA mão; `bounty_source`; base÷2 aplica-se;
  `no_witness_has_plate` só sem testemunha nenhuma. Fechou **3** dos 14; **11 ficam por
  rever** (placa tapada em TODAS as capturas — Hero com cartas/all-in por cima).

**Números finais do bloco:** flame-as-crown-scan **46 → 11** (todos honestos: `$0` por ler,
avatar tapado; zero chama-como-coroa) · grupo Valor alto **74 → 9** · **11 seats por rever**
(sem testemunha) · **9 cliques Valor alto** por dar (Rui). Incidente do saldo Anthropic no
meio (créditos esgotaram e o Rui recarregou — nada escrito/danificado; Vision vazia → sem
match → sem write).

## ★ Swap Hero↔vilão — `#DESANON-HERO-ANCHOR-VALIDATION`

Painel "Mãos suspeitas": 7 mãos com o nome do Rui num hash-vilão. **6** (torneio 293616019,
HR $525): hash `2223abd8` — swap de 2 vias `Hero(Lauro) ↔ R Sanchez`. **1** (`GG-6084190544`,
Daily $88): `2c9d2243` ↔ `buildthepot`. **Raiz (confirmada em 3 imagens):** a Vision marcou
um **vilão como `is_hero`** (baixo-centro); a âncora confiava cegamente e trocava
Hero↔vilão. Nas 6, **1 leitura má venceu 5 boas** — a propagação por hash espalhou
`2223abd8 = Lauro` do único capture mal lido para os outros 5. Nenhuma salvaguarda travou
(rotação certa; só o ponto de âncora errado).

**Correção pela raiz** (via `/set-anon-map`, estendido para repor `pn.hero`): as 6 numa
operação + a 7ª. **Guarda de prevenção** na âncora (`_vision_hero_nick_is_rui`): `is_hero`
∉ HERO_NICKS GG → alarme, mão para revisão, não escreve. **Tolerante a truncação** da
Vision (prefixo, min 3 chars) para não falsos-alarmar. **Verificação:** painel **7 → 0** ·
`pn.hero`/`Hero`/`apa.Hero` todos Lauro · **0 `hand_villains`** com nome do Rui (nada
pendurado). 158 testes verdes.

## Consolidação Saúde Import

**13 painéis, 3 secções, sidebar −3** — "Saúde GG" → **"Saúde Import"**; migrados
Import-Health / Marcadas-por-captura / Suspeitas para dentro; problema das coroas suspeitas
só no painel Coroas; grupo "Valor alto" novo. Painel Coroas mostra origem por linha
(table-SS/Gold/carry) + re-leitura lado a lado no grupo alto.

## Verificação visual painel a painel (em curso)

**✓ Edições · ✓ Coroas · ✓ Suspeitas.** Pendentes: **Marcadas 432** (origem das tags a
responder ao Rui) · **conflitos 1+11+3** por ver · **amostragem final**.

## Regra do Rui (nova, permanente)

**SEM imports/processamentos massivos até a app estar 100%** — afinação primeiro, volume
(reimport, robot em lotes) só com **ordem explícita** do Rui. O robot HRC fica em espera
por esta regra (a "regra de largada" técnica está destrancada, mas o gatilho é do Rui).

## ★ Marcadas 432 — RESPONDIDO (origem das tags)

Pergunta do Rui: das 432 mãos na fila "Marcadas por captura" (GG desanon por table-SS, sem
tag), quantas vieram de subpasta etiquetada (propagação falhou) vs raiz/não-ordenado?

**Resposta (cruzada em 3 ângulos, read-only):** **todas as 432 têm `folder_tag=NULL`** →
**0 falhas de propagação** (a máquina `_apply_folder_tag_to_hand` está intacta: etiquetou 361
outras). (1) BD: 0/432 com folder_tag na captura ligada. (2) Via: todas `manual_upload` sem o
campo folder_tag. (3) **Disco** (`Batmen\done\it\`): **427/432 na RAIZ** (planas, não
ordenadas), **0 em subpasta etiquetada**, 5 não encontradas. São mãos de Junho (4–26)
re-carregadas 8–11 Jul pela raiz do `it\`. **302** em 15 torneios que já têm tags do Rui
(referência), **130** em 22 torneios sem tags. Triagem manual do Rui, a 1 clique. CSVs em
`_local_only\` (por_torneio + detalhe).

## ★ Hero-alheio (3→0) — ângulo cego do swap, fechado ponta a ponta (LIVE `f8ce806`)

Variante do swap de ontem apanhada na verificação das "Suspeitas de troca": prints **pós-bust**
(o Rui já não está na mesa) em que a Vision marcou um **vilão** como `is_hero` e a âncora deu
esse nick ao **HERO** da mão. O nome do Rui fica **AUSENTE** (não num vilão) → o detetor de
ontem ("o teu nome num vilão") **não o apanha**.

- **Scan estendido** (`pn.hero ∉ HERO_NAMES_ALL`) = **exatamente 3 em toda a BD**: GG-6132506765,
  GG-6077753233, GG-6077315638. Só o **515** overlapa o painel "Suspeitas de troca"
  (`num_matches=False`); as outras 2 têm filename certo → invisíveis a esse painel.
- **Diagnóstico:** as 3 **pré-guarda** (a guarda `_vision_hero_nick_is_rui` entrou `31436dc`
  **11 Jul 17:31**; uploads 8/10 Jul) → **veneno antigo, guarda SEM furo** (num reprocessamento
  apanha-as). Único ponto de de-anon (`build_anon_map_by_hero_button:471`) tem a guarda lá.
- **★ Achado que muda a correção:** só **1 das 3** está mesmo envenenada. 212 e 734 têm o
  `apa['Hero']` = **Lauro Dermio** correto (a propagação por hash de 8 Jul curou o apa); só o
  campo-resumo `pn.hero` ficou com o nick do vilão = **cosmético**. A **515** tem o Lauro
  **ausente da imagem** (pós-bust) → `apa['Hero']=aaooiu` = **veneno real**.
- **Correção (endpoints, com aprovação do Rui + antes/depois):** 212/734 → `/set-anon-map`
  re-aplica o mapa existente (repõe `pn.hero=Lauro`); 515 → `/revert-to-anon` (anónima toda:
  `player_names={}`, apa hash-keyed do raw, villains apagados, `context_table_ss_id=NULL`).
  **Scan pós-fix = 0.**
- **3 mudanças LIVE (`f8ce806`):** (1) **Guarda 4** da âncora
  (`#DESANON-ANCHOR-REQUIRES-HERO-IN-IMAGE`) — a de-anon exige >=1 seat = conta do Rui; senão
  `deanon_skipped_no_hero_in_image` (casa a imagem, fica anónima). Mais forte que a guarda do
  PONTO de âncora (cobre o pós-bust). (2) **Veneno 3** `hero_alheio` no painel Mãos suspeitas
  (health-check permanente; distingue cosmético vs veneno pelo apa). (3) endpoint
  `POST /api/table-ss/revert-to-anon` (expõe a primitiva `_revert_hand_to_anonymous`, que
  faltava — pt97). Suite: +8 testes; módulos tocados 136 passed.

## Commits do dia (main)
`abd6a51` prompt+guarda+harness → `5d024a6` mime → `6a56fcb` guarda A + botão →
`ff67635`/`b2bbaa4` registos + lado-a-lado → `e04e6e2` grupo Valor alto + guarda export →
`addc2e5` números → `1366025`/`b5a673d` high-confirm + fallback → `31436dc` guarda âncora +
pn.hero → `d715e42` LICOES âncora → **`f8ce806` Guarda 4 âncora + veneno 3 hero-alheio +
revert-to-anon (Marcadas 432 respondido; Hero-alheio 3→0)**.
